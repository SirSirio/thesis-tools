---
status: complete
completed: 2026-07-13
commits: [2338acc, 9d70873, 9b28b91]
---

# Summary: proto-02 page restructure

All 7 requests done, verified live in the browser (Playwright screenshots).

1. **Images repositioned** — ShrinkTest10cm moved from the section tail up to the
   shrink-coupon results; BearingPocketTest below its ladder table; both capped
   at 540 px (`figure-compact`).
2. **0.085 mm marked estimate** — footnote in HTML + PROTOTYPE.md §11.7.11
   (bore 5.000 − peg ~4.915, peg read on the cone's narrow region).
3. **Creep paragraph simplified** — plastic = deliberate expedient (fast proof of
   concept + learning one material/process); final pump machined in metal.
4. **Print model introduced properly** — characterize-the-printer motivation,
   honest "educated guess, not a theory of FDM" disclaimer, annotated visual
   equation (chips per term, operators bound to terms, sign legend). Stale
   "shrink is nozzle-independent" stat-label marked as revised.
5. **TOC added** — glass Contents card, 3 colored groups, 13 anchor ids, JS
   auto-expands the containing panel on click (verified).
6. **Collapsible version panels** — design / v2.1 / v2.2 `<details>` windows,
   v2.2 open by default; stale status callout and inline chips removed.
7. **Head schematic rebuilt** — true cross-section at one roller: hatched walls
   (orange rotor / blue head), steel bearing, squeezed tube, pad reaching
   between the flanges; right-half-only shown via CL centreline + mirrored
   ghost + top-view A–A locator inset; "exaggerated, not to scale" noted.
