---
quick_id: "260625"
slug: deck-sections
description: Restructure lab-meeting deck into sections with dividers, AI/GSD slides, alignment video, image-first animations
date: 2026-06-25
status: complete
---

# Quick Task 260625 — Deck sectioning & content rework

Extension of Phase 05 (presentation deck). Rework `decks/lab-meeting-2026-06/index.html`.

## Locked decisions (from discussion)
- **Section order:** Alignment → AI → Pump (user choice).
- **Divider style:** Full-bleed image tint (large title, eyebrow section number, dark left scrim, no bullets).
- **Animation rule:** Image/media animates in FIRST, then writing — replayed on every slide activation.

## Final slide order
1. Title (agenda line updated to the 3 sections)
2. Divider 01 — Alignment Module (bg: alignment_poster.jpg)
3. Alignment Module — title + embedded `Alignment_Module_V2.mp4`, no bullets
4. Divider 02 — AI for Engineering (bg: Prototype1_Displaced_Volume_Tool.png)
5. AI as a tool — Claude Code splash (terminal frame) + brief points (research partner / build accelerator)
6. GSD — rewritten: spec-driven development (discipline) vs GSD (framework); Discuss/Plan/Execute cards
7. Divider 03 — Pump Module (bg: Prototype1_Render_Open.png)
8. Rotor Solver (live iframe) — iframe-first
9. Displaced-Volume Model (live iframe) — iframe-first
10. proto-01 — render + real photo side-by-side, image-first, bullets as fragments
11. proto-02 — hump-curve SVG, image-first
12. Test-campaign app — image-first

## Technical approach
- **Image-first animations:** add `.auto-anim` (+`.delay-N`) classes (keyframe `avIn`) in the deck `<style>`. Inactive slides sit at `opacity:0` (not `display:none`), so CSS animations fire once at page load — to replay per activation, add an **inline** MutationObserver in the deck that restarts `.auto-anim` elements (and plays/pauses `video[data-deck-video]`) when a slide gains/loses `slide--active`. Shared `assets/deck.js` is NOT modified (D-01 respected).
- **Reduced motion:** CSS forces `.auto-anim{opacity:1}` so content stays visible.
- **Dividers:** full-bleed `<img class="divider-bg">` + scrim + content; consistent across all three.
- **GSD cards:** replicate the `.sdd-phase` card look from `tools/gsd-workflow-guide` inline.

## Assets
- New: `decks/lab-meeting-2026-06/assets/alignment_poster.jpg` (extracted frame @4s)
- Track: `decks/lab-meeting-2026-06/CaludeCode_image.png` (currently untracked)

## Docs to update
- `decks/lab-meeting-2026-06/SPEC.md` — new slide inventory + animation model
- `.planning/notes/2026-06-24-presentation-guidelines.md` — section-divider guidance + image-first rule
- `CLAUDE.md` — note the new deck asset folder content (if needed)

## Tasks
1. Rewrite deck `index.html` (structure, dividers, AI/GSD/alignment slides, image-first anims, inline replay script).
2. Update SPEC.md + presentation-guidelines.md.
3. Commit (atomic).
