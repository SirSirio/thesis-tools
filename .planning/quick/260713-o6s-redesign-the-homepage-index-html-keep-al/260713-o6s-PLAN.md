---
task: Redesign homepage index.html — premium/modern layout, keep all features incl. EN/IT translation
quick_id: 260713-o6s
date: 2026-07-13
mode: quick (inline plan per feedback-planner-overhead — single-file change, no planner/executor subagents)
files_modified:
  - index.html
---

# Quick Task 260713-o6s: Homepage redesign

## Scope

Single file: `index.html`. `assets/style.css` stays untouched — it is shared by tool
pages and the lab-meeting deck. All new/override styles go in the page's inline
`<style>` block (consistent with project rules).

Addresses STATE.md backlog item: "[ui] Redesign and restructure landing page for many
tools — flat card grid needs rethinking now that 4+ tools + prototype space exist."

## Must keep (feature parity)

- EN/IT translation with `localStorage` key `lang` (values `en`/`it`) — shared with rotor-solver
- All 6 resource links (3 calculators, GSD guide, prototype space, presentations)
- Dark glassmorphic theme, ambient blobs, entrance animations, footer content
- No CDN/external dependencies; works offline; no horizontal scroll

## Redesign

1. **Fixed glass top nav** — brand wordmark left, segmented EN/IT language switch right
   (replaces the floating single toggle button; same storage behavior).
2. **Hero** — badge, larger gradient title, author divider, subtitle, plus a row of
   three translated meta chips (browser-based / offline-USB / no install).
3. **Editorial section headers** — index number (01–04) + label + gradient hairline
   rule + description.
4. **Premium cards** — icon in a glass gradient tile, top hairline accent on hover,
   mouse-tracking spotlight (CSS vars + tiny JS), arrow that slides on hover.
5. **Wide horizontal card variant** for single-card sections (Guides, Prototypes,
   Presentations) so they no longer float in an empty 3-column grid.
6. **Scroll-triggered reveals** via IntersectionObserver (gated on `.js` root class so
   no-JS still shows content); hero keeps load-time stagger.
7. **Subtle background grid overlay** (pure CSS, radial-masked) for depth.
8. **Full i18n coverage** — add the missing keys (cards 2–4, guides, prototypes,
   presentations section, all card links, meta chips) to both EN and IT dictionaries.

## Verification

- Serve locally, screenshot via Playwright: EN + IT, desktop + narrow viewport
- Console free of errors; language persists via localStorage; all links resolve
