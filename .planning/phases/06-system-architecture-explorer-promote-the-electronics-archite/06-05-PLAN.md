---
phase: 06-system-architecture-explorer
plan: 05
type: execute
wave: 3
depends_on: ["06-01", "06-03"]
files_modified:
  - tools/system-architecture-explorer/index.html
autonomous: true
requirements: [ARCH-07, ARCH-08]
must_haves:
  truths:
    - "Each component row shows an optional source field (vendor URL and/or short price note) paired with a High/Medium/Low confidence tag; the ILI9341 screen is the High-confidence sourced anchor (bitbyg, 174 DKK ≈ €23) (D-07, D-11)"
    - "User-typed source-note text is rendered safely (textContent / escaped) and never interpolated raw into an innerHTML template string (Security)"
    - "Brain/MCU candidates record usable-GPIO, RAM and PSRAM with a UI-fluidity note; at least one bitbyg ESP32-with-integrated-screen variant (ESP32-2432S024, 9 IO, High confidence for the IO count) is present (D-10, D-12)"
    - "Each variant shows pins used / available / free with overruns flagged; the flag carries a confidence marker, not just a bare number (D-09, D-11)"
    - "The screen's Layer-A pin cost is selectable between the SPI and 8-bit-parallel scenarios, defaulting to SPI tagged Low confidence (RESEARCH recommendation); the end-of-phase human-check has the reviewer physically inspect the owned ILI9341 board and flip the default + confidence tag if it is actually 8-bit parallel (D-09, D-11 verification aspect, formerly plan 06-02)"
  artifacts:
    - path: "tools/system-architecture-explorer/index.html"
      provides: "Source/confidence fields, integrated-screen + RAM/PSRAM candidate data, and pin-budget feasibility readout"
      contains: "pinsOf"
  key_links:
    - from: "pinsOf(v)"
      to: "BRAIN pin map + SCREEN_PINS[scenario] + Layer-B/Layer-C pin costs"
      via: "per-variant pin aggregation mirroring costOf(v)"
      pattern: "pinsOf"
    - from: "source-note field"
      to: "DOM"
      via: "textContent / escaped insertion (not raw innerHTML)"
      pattern: "textContent|escapeHtml"
---

<objective>
Add the new evaluation data the "Explorer" promise depends on, all as additive fields on the existing `DEFAULTS`/`VARIANTS` model (D-08: this inline script stays canonical): a per-component source field + High/Medium/Low confidence tag (D-07/D-11) rendered XSS-safely, brain-candidate RAM/PSRAM/usable-GPIO specs with a UI-fluidity note plus the bitbyg integrated-screen variant(s) (D-10/D-12), and a pins-used/available/free feasibility readout with overrun flags that carry their confidence (D-09/D-11). The screen's Layer-A pin cost is a selectable SPI-vs-parallel scenario defaulting to SPI tagged Low confidence; the physical resolution of the vendor-page contradiction is an end-of-phase human-check (per `workflow.human_verify_mode: end-of-phase`), not a mid-flight checkpoint.

Purpose: Turns the matrix from a cost table into a decision aid that surfaces which variants are actually buildable with the fixed hardware and how well-sourced each number is — the core reason the tool exists (CONTEXT domain). This is the biggest new-data ask; confidence tagging is load-bearing, not decorative (RESEARCH Pitfall 1).
Output: Extended data model + pin-budget engine + sourcing UI in the tool file.
</objective>

<execution_context>
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/workflows/execute-plan.md
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-system-architecture-explorer-promote-the-electronics-archite/06-RESEARCH.md
@.planning/phases/06-system-architecture-explorer-promote-the-electronics-archite/06-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add per-component source field + confidence tag, rendered XSS-safely (D-07, D-11)</name>
  <files>tools/system-architecture-explorer/index.html</files>
  <behavior>
    - A component with a source note renders the note text; if the note contains `<script>` or HTML, it appears as literal text (escaped) — no element is injected into the DOM.
    - A component with a vendor URL renders a clickable link (href set safely; javascript: schemes rejected/ignored).
    - The ILI9341 screen row shows confidence "High" and its bitbyg source; unsourced components show "Low" (estimate) by default.
    - Editing/adding a source note and reloading keeps it (persisted alongside prices via the existing sae-* mechanism) — optional if trivially covered; at minimum it renders and escapes correctly.
  </behavior>
  <read_first>
    - tools/system-architecture-explorer/index.html (DEFAULTS entries, renderComps() building the component table via innerHTML template strings from code-controlled labels — the source-note field is the FIRST user-typed string rendered, so it needs different handling)
    - .planning/phases/06-system-architecture-explorer-promote-the-electronics-archite/06-PATTERNS.md (Shared Patterns → "XSS-safe rendering for user-typed text": escape or set via textContent, do not copy the ${c.label} interpolation for this field)
    - .planning/phases/06-system-architecture-explorer-promote-the-electronics-archite/06-RESEARCH.md (Security Domain: the source-note innerHTML threat and mitigation; D-11 confidence scale definitions High/Medium/Low)
  </read_first>
  <action>
    Extend each DEFAULTS entry with optional `source` (a `{url, note}` or the fields you pick per D-07 discretion) and `confidence` ('High'|'Medium'|'Low'). Seed the ILI9341 `screen` entry with confidence 'High' and its bitbyg URL (`https://bitbyg.dk/shop/3-2-inch-tft-lcd-display-module-spi-touch-screen-onboard-temperature-sensor-pen/`, 174 DKK ≈ €23); leave other components at 'Low' (estimate) until sourced — do NOT attempt to source all ~20 (that is deferred). Add a Source/Confidence column (or paired cells) to the component table in renderComps(). Render the confidence as a small pill (reuse the existing pill styling; add a Low/Medium/High colour if not present).

    CRITICAL (Security V5/XSS): the source note is user-typed. Do NOT interpolate it into the `innerHTML` template string like `${c.label}`. Either (a) build the row with innerHTML for the code-controlled cells then set the note via `noteCell.textContent = c.source.note` on a dedicated node, or (b) pass it through a small `escapeHtml()` helper (replace & < > " ') before insertion. For a URL, set it as an anchor href only if it starts with `http://` or `https://` (reject `javascript:`); render the visible link text via textContent. Add a legend somewhere near the component table defining the High/Medium/Low scale (D-11).
  </action>
  <verify>
    <automated>grep -Eq "textContent|escapeHtml" tools/system-architecture-explorer/index.html && grep -qi "confidence" tools/system-architecture-explorer/index.html && grep -q "High" tools/system-architecture-explorer/index.html</automated>
  </verify>
  <acceptance_criteria>
    - DEFAULTS entries carry `source` and `confidence`; the `screen` entry is High-confidence with the bitbyg URL.
    - The component table renders a source note + confidence pill per row; a legend defines High/Medium/Low.
    - Source-note text is set via `textContent` or passed through an escape helper — grep confirms the note is NOT concatenated into a template-string `innerHTML` beside `${...}` raw. A note containing `<b>x</b>` displays as literal text, not bold (behaviour check).
    - Vendor URLs render as links only for http(s) schemes; a `javascript:` URL is not made clickable.
  </acceptance_criteria>
  <done>Component rows show sourced, confidence-tagged data with safe rendering of user-typed text.</done>
</task>

<task type="auto">
  <name>Task 2: Add brain RAM/PSRAM/GPIO specs + the bitbyg integrated-screen variant(s) (D-10, D-12)</name>
  <files>tools/system-architecture-explorer/index.html</files>
  <read_first>
    - tools/system-architecture-explorer/index.html (DEFAULTS brain entries: esp32, rp2040, stm32, promini, nano; VARIANTS array to extend)
    - .planning/phases/06-system-architecture-explorer-promote-the-electronics-archite/06-RESEARCH.md (Brain/MCU candidates table: usable-GPIO / RAM / PSRAM / bitbyg price / confidence per candidate; D-10 integrated-screen candidates table — ESP32-2432S024 = 9 IO High confidence; the "do NOT present non-bitbyg boards as bitbyg-sourced" pitfall; RAM/PSRAM UI-fluidity framing)
    - .planning/phases/06-system-architecture-explorer-promote-the-electronics-archite/06-CONTEXT.md (Specifics: ESP32 brain is OPEN/not-yet-chosen — ESP32 is not one fixed part; RAM/PSRAM drive UI fluidity)
  </read_first>
  <action>
    Add to each brain-class DEFAULTS entry the specs from RESEARCH's Brain/MCU table: `gpioUsable` (with its confidence), `ram`, `psram`, and a short `uiNote` implementing D-12's fluidity framing ("PSRAM: no → single-buffered UI comfortable, no full-screen animation" for base ESP32; "PSRAM: 8MB → framebuffers fit, comfortable" for WROVER-class). Each spec carries a confidence tag per D-11 (most are MEDIUM/ASSUMED — tag honestly).

    Add at least one integrated-screen variant sourced from bitbyg: the ESP32-2432S024 (2.4" LVGL touch module, 9 available IO ports — High confidence for the IO count per its bitbyg product page; display resolution/touch chip = Low). Model it as its own brain component (one part replacing separate MCU+external screen — fewer pins spent on the display) and add a VARIANTS row (or rows) using it, tagged with confidence. Do NOT add the "Cheap Yellow Display" ESP32-2432S028 as a bitbyg-sourced variant (it is not in bitbyg's catalogue) — if referenced at all, label it "reference only, not bitbyg-stocked" (RESEARCH Pitfall 4). Keep the variant set leaning toward what bitbyg actually stocks (D-11).
  </action>
  <verify>
    <automated>grep -qi "psram" tools/system-architecture-explorer/index.html && grep -Eqi "2432S024|integrated" tools/system-architecture-explorer/index.html && grep -qi "ram" tools/system-architecture-explorer/index.html</automated>
  </verify>
  <acceptance_criteria>
    - Brain-class DEFAULTS entries carry `gpioUsable`, `ram`, `psram`, and a `uiNote`, each with a confidence tag.
    - At least one ESP32-integrated-screen variant (ESP32-2432S024, 9 IO, High for IO count) exists in VARIANTS and renders in the matrix with a confidence marker.
    - No non-bitbyg board (e.g. ESP32-2432S028 "CYD") is presented as a bitbyg-sourced priced variant.
    - RAM/PSRAM specs surface in the UI (e.g. in the component/BOM detail) with the fluidity note.
  </acceptance_criteria>
  <done>Brain candidates carry RAM/PSRAM/GPIO with fluidity notes; a bitbyg integrated-screen variant is present and honestly tagged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Compute and display pins used / available / free per variant with confidence-tagged overrun flags (D-09)</name>
  <files>tools/system-architecture-explorer/index.html</files>
  <behavior>
    - pinsOf(v) returns {used, avail, free, overrun} where used = fixed loads (screen per selected scenario + touch + LM75) + Layer-B bus pins × node logic + Layer-C driver-link pins, avail = the variant brain's usable-GPIO.
    - A variant whose used > avail is flagged overrun (visibly distinct); free = avail - used shown for others.
    - Switching the screen-interface scenario (SPI ↔ 8-bit parallel) changes the fixed pin cost and can flip a borderline variant's overrun state.
    - The overrun/free readout displays a confidence marker reflecting the lowest-confidence input feeding it (the screen interface is Low until physically verified at end-of-phase).
  </behavior>
  <read_first>
    - tools/system-architecture-explorer/index.html (costOf(v) — the aggregation shape to mirror; renderMatrix() where the new pin column/readout is added; the brain specs added in Task 2)
    - .planning/phases/06-system-architecture-explorer-promote-the-electronics-archite/06-RESEARCH.md (Pin Budget Research: fixed-load pin counts for both screen scenarios; Layer-B pins/node table I²C=2/RS-485=3/CAN=4-5; Layer-C driver-link pin costs per topology class; the pinsOf() code sketch mirroring costOf(); Open Questions (RESOLVED) Q1 — SPI is the Low-confidence default, end-of-phase human-check flips it if inspection shows parallel)
    - .planning/phases/06-system-architecture-explorer-promote-the-electronics-archite/06-CONTEXT.md (D-09 fixed vs variant loads breakdown)
  </read_first>
  <action>
    Add pin-cost data: a `SCREEN_PINS` object keyed by scenario `{spi: N, parallel: M}` seeded from RESEARCH (SPI ≈6 display +2 touch; parallel ≈13); Layer-B per-node pin costs (I²C 2, RS-485 3, CAN 4-5); Layer-C driver-link costs per topology class (shared STEP/DIR+EN ≈8, per-motor STEP/DIR ×6 =12 on the node MCU, TMC2209 UART ≈4, TMC5160/5072 SPI daisy 3+CS, printer boards N/A — bus-only). Attach the driver/bus pin costs to the relevant DEFAULTS/VARIANTS fields so `pinsOf(v)` sums them the same way `costOf(v)` sums euros.

    Implement `pinsOf(v)` returning `{used, avail, free, overrun}`: used = `SCREEN_PINS[interfaceMode]` (fixed) + LM75 (0 extra if I²C bus exists, else +2) + Layer-B bus pins for the variant + Layer-C driver-link pins; avail = the variant brain's `gpioUsable`. Add an interface-scenario toggle (control) that flips `interfaceMode` and re-renders. DEFAULT the toggle to the **SPI scenario, tagged Low confidence** (RESEARCH.md recommendation — the vendor page contradicts itself, so SPI-per-the-title is the working assumption until the physical board is inspected; the end-of-phase human-check below resolves it). Label the toggle so the ambiguity is visible in the UI (e.g. "screen interface: SPI (unverified) / 8-bit parallel"). Add a "Pins free" / overrun column (or per-row readout) in renderMatrix(): show `free` when OK and a distinct OVERRUN badge (reuse the .p6/red pill style) when `used > avail`. Attach a confidence marker to that readout reflecting the lowest-confidence input (e.g. superscript/tooltip) — do not present the pin number as more certain than its sourcing (RESEARCH Pitfall 1). For printer-board variants, only Layer-B host pins count (Layer-C is "plug into socket").
  </action>
  <verify>
    <automated>grep -q "function pinsOf" tools/system-architecture-explorer/index.html && grep -Eqi "SCREEN_PINS|interfaceMode" tools/system-architecture-explorer/index.html && grep -Eqi "overrun|pins free|Pins free" tools/system-architecture-explorer/index.html</automated>
    <human-check>Physically inspect the owned bitbyg ILI9341 3.2" module (the board Sirio already owns — vendor page: https://bitbyg.dk/shop/3-2-inch-tft-lcd-display-module-spi-touch-screen-onboard-temperature-sensor-pen/) to resolve the SPI-vs-8-bit-parallel contradiction flagged in 06-RESEARCH.md Open Questions (RESOLVED) Q1: (1) Read the silkscreen header labels — TRUE SPI shows SCK/CLK, MOSI/SDI, MISO/SDO, CS, DC/RS, RST (+ touch T_CLK/T_CS/T_IRQ sharing the SPI bus; Layer-A ≈6+2 pins); 8-bit PARALLEL (Uno-shield class) shows a bank of 8 data pins plus RS/DC, CS, RD, WR, RST and often an SD slot (Layer-A ≈13+ pins). (2) If the board is SPI: raise the SPI scenario's confidence tag from Low to High in DEFAULTS and SPEC.md — no other change needed. (3) If the board is 8-bit parallel: flip the interfaceMode default to 'parallel', tag it High, and re-check which 6-parallel variants now show OVERRUN (the readout should flip automatically). (4) Also verify in the running tool: switching the scenario toggle visibly changes the pins-free numbers, and the OVERRUN badge carries its confidence marker.</human-check>
  </verify>
  <acceptance_criteria>
    - `pinsOf(v)` exists and returns used/avail/free/overrun; the matrix shows a per-variant pin readout.
    - An interface-scenario toggle exists; switching SPI↔parallel changes the fixed pin cost and re-renders (a borderline 6-parallel variant visibly flips overrun state between scenarios — behaviour check).
    - Default scenario is SPI, tagged Low confidence, and the toggle labelling makes the unverified status visible in the UI.
    - Overrun/free readout carries a confidence marker (not a bare number) reflecting the lowest-confidence input.
    - Printer-board variants count only Layer-B host pins for the brain (Layer-C treated as sockets).
  </acceptance_criteria>
  <done>Pin-budget feasibility is a first-class, confidence-tagged readout alongside cost and complexity, with both screen scenarios selectable, SPI/Low as the default, and the physical verification queued as the end-of-phase human-check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-typed source note / URL → DOM | The D-07 source-note field is the first genuinely user-typed string rendered by this tool |
| Number inputs → pin/cost engine | Any editable numeric fields feed arithmetic |
| localStorage → page | Source notes, if persisted, deserialize on load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Tampering / Elevation | source-note / URL field rendered into DOM | mitigate | Render note via `textContent` or an `escapeHtml()` helper (never raw `${note}` in an innerHTML template); accept URLs only with http(s) scheme, reject `javascript:` |
| T-06-07 | Tampering | persisted source notes deserialized on load | mitigate | Reuse 06-01's try/catch + type guards; treat restored notes as strings and escape on render |
| T-06-08 | Spoofing | pin/RAM/PSRAM numbers appearing authoritative | mitigate | Carry D-11 confidence tags to the displayed pin-budget readout (Pitfall 1) — not a code exploit but a data-integrity control; screen-interface default stays Low confidence until the end-of-phase physical inspection |
| T-06-SC | Tampering | dependency installs | accept | No installs; nothing to audit |
</threat_model>

<verification>
- Source note containing HTML renders as literal text (no injection).
- Switching the screen scenario flips at least one borderline variant's overrun state.
- Pin-budget readout always shows a confidence marker.
- End-of-phase human-check (Task 3) resolves the screen interface and adjusts default + confidence.
</verification>

<success_criteria>
The matrix carries sourced/confidence-tagged prices, RAM/PSRAM-aware integrated-screen candidates, and a confidence-tagged pin-budget feasibility readout with both screen scenarios selectable (SPI/Low default, physical verification at end-of-phase) — all as additive fields on the canonical inline data model.
</success_criteria>

<output>
Create `.planning/phases/06-system-architecture-explorer-promote-the-electronics-archite/06-05-SUMMARY.md` when done. Record the final variant list (esp. new integrated-screen rows), the SCREEN_PINS values, and the human-check outcome (or "pending end-of-phase review") for the SPEC.md plan.
</output>
