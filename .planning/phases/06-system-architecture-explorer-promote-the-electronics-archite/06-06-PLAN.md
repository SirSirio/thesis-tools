---
phase: 06-system-architecture-explorer
plan: 06
type: execute
wave: 4
depends_on: ["06-05"]
files_modified:
  - tools/system-architecture-explorer/index.html
autonomous: true
requirements: [ARCH-03]
must_haves:
  truths:
    - "Clicking a matrix row selects it (row highlights) and redraws a live SVG system diagram in the id=diagram region for that variant, while also expanding its BOM as before (D-02)"
    - "The diagram shows Layer A brain↔screen (fixed), Layer B the system bus drawn with the variant's real node count, Layer C the variant's pump-controller topology + the 6 driver→motor links, the constant alignment node, and a power block sized 60W vs 150W with the 12V/24V dual rail on common ground (D-03)"
    - "The diagram is the live thing that earns the tool the name 'Explorer' and makes ARCHITECTURE.md's long-standing 'see the diagram in index.html' promise true (D-01)"
    - "Any diagram redraw transition is gated behind prefers-reduced-motion: no-preference"
  artifacts:
    - path: "tools/system-architecture-explorer/index.html"
      provides: "buildDiagram(v) live SVG + row-click selection wiring"
      contains: "buildDiagram"
  key_links:
    - from: "matrix row click"
      to: "buildDiagram(v) in #diagram"
      via: "selection handler that highlights, redraws, and expands BOM"
      pattern: "buildDiagram"
---

<objective>
Build the flagship capability: a live, variant-driven SVG system diagram (D-01) that redraws when a matrix row is clicked (D-02), showing the three comms layers, the per-variant bus node count and driver topology, the constant alignment node, and the power block (D-03). Use the rotor-solver's proven build-a-template-string-SVG-on-every-change technique with a small set of hand-authored layouts keyed by topology class (fused-single-node · +1-satellite-node · +6-satellite-nodes distributed · printer-board), injecting the variant's real labels/counts — not fully generative layout from raw bom iteration (RESEARCH recommendation; avoids unreadable diagrams).

Purpose: The diagram is the thing the table cannot say — it makes power (the hidden cost of parallelism) and bus/driver topology visible, and it earns the "Explorer" name. It also resolves ARCHITECTURE.md's dangling "see the diagram" promise (now repointed by 06-03 to `#diagram`).
Output: `buildDiagram(v)` + row-selection wiring rendering into the `#diagram` region.
</objective>

<execution_context>
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/workflows/execute-plan.md
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-system-architecture-explorer-promote-the-electronics-archite/06-RESEARCH.md
@.planning/phases/06-system-architecture-explorer-promote-the-electronics-archite/06-CONTEXT.md
@.planning/phases/06-system-architecture-explorer-promote-the-electronics-archite/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement buildDiagram(v) — hand-authored template layouts by topology class (D-01, D-03)</name>
  <files>tools/system-architecture-explorer/index.html</files>
  <read_first>
    - tools/rotor-solver/index.html (buildFigure(g, L) lines ~574-668: the template-string SVG technique — fixed pixel viewport, computed node positions, per-node captions, the feasibility status-text/colour pattern to mirror for layer captions and any overrun highlight)
    - tools/system-architecture-explorer/index.html (the id=diagram region reserved by 06-01; the VARIANTS array with per-row `at`/`b`/`c`/`driver`/`dk`/`bom`; the pinsOf(v) result from 06-05 to optionally surface an overrun cue in the diagram)
    - .planning/phases/06-system-architecture-explorer-promote-the-electronics-archite/06-PATTERNS.md (Pattern 2: 4-5 template layouts keyed by topology class, fixed node positions per class, data-driven labels; the prefers-reduced-motion CSS guard)
    - .planning/phases/06-system-architecture-explorer-promote-the-electronics-archite/06-CONTEXT.md (D-03: the exact layers/elements the diagram must show — A/B/C, alignment node, power block 60W/150W, 12V/24V dual rail common ground)
  </read_first>
  <action>
    Write `buildDiagram(v)` returning an SVG template string sized to a fixed viewBox (mirror buildFigure's fixed-viewport approach so the figure never overflows). Classify the variant into a topology class from its fields (fused single node = ESP32 only; +1 satellite = ESP32+rp2040/stm32 or a dedicated node; +6 distributed = promini×6; printer-board = ramps/skr present) and select the matching hand-authored layout — fixed node positions per class, with the variant's real labels/counts injected: Layer A drawn as brain↔screen (SPI/parallel per interfaceMode, fixed); Layer B drawn as the system bus (I²C/RS-485/CAN from `v.b`) with the variant's REAL node count; Layer C drawn as the driver topology (`v.c`/`v.driver`) with the 6 driver→motor links; the alignment node drawn constant across all variants; a power block sized `psu60` vs `psu150` (from the variant bom) showing the 12V/24V dual rail on common ground. Use the site accent/token colours and the existing status-colour vars. Label each layer (A/B/C) so the diagram is self-describing. Optionally surface the 06-05 pin overrun as a red cue on the affected node. Add the CSS `@media (prefers-reduced-motion: no-preference){ #diagram svg { transition: opacity .15s ease } }` guard (default = no transition, satisfying reduced-motion trivially).
  </action>
  <verify>
    <automated>grep -q "function buildDiagram" tools/system-architecture-explorer/index.html && grep -q "prefers-reduced-motion" tools/system-architecture-explorer/index.html && grep -Eq "viewBox" tools/system-architecture-explorer/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `buildDiagram(v)` exists, returns an SVG string with a fixed viewBox, and renders Layer A, Layer B (with the variant's node count), Layer C (driver topology + 6 motor links), an alignment node, and a power block labelled 60W/150W with the 12V/24V dual rail.
    - Distinct topology classes produce visibly distinct layouts (e.g. a distributed promini×6 variant looks different from a fused ESP32 variant).
    - A `prefers-reduced-motion: no-preference` media query gates any diagram transition; with reduced motion the diagram still updates (just without transition).
    - The figure fits without horizontal scroll at 1280px and 375px.
  </acceptance_criteria>
  <done>A live SVG diagram renders the D-03 layers/power/alignment for a given variant via hand-authored topology-class layouts.</done>
</task>

<task type="auto">
  <name>Task 2: Wire row-click selection to redraw the diagram, highlight the row, and expand the BOM (D-02)</name>
  <files>tools/system-architecture-explorer/index.html</files>
  <read_first>
    - tools/system-architecture-explorer/index.html (renderMatrix(): the existing `tr.addEventListener('click', ...)` that toggles the detail/BOM row — extend it to also drive selection + buildDiagram; the id=diagram region)
    - .planning/phases/06-system-architecture-explorer-promote-the-electronics-archite/06-CONTEXT.md (D-02: selection = click a matrix row; one click, one architecture; no second control to keep in sync — do NOT add a dropdown or a follow-cheapest mode, both explicitly rejected)
  </read_first>
  <action>
    Extend the existing row-click handler in renderMatrix() so that clicking a row: (1) sets a `selectedVariant`/highlights that row (add/remove a `.selected` class; style it with the accent), clearing highlight on other rows; (2) calls `document.getElementById('diagram').innerHTML = buildDiagram(v)` to redraw for that variant; (3) keeps the existing BOM detail expand behaviour. Selection is the ONLY diagram control (D-02) — do not add a dropdown or follow-cheapest mode (both rejected/deferred). On initial render, either draw a default/first variant into #diagram or show a "click a row" prompt. Ensure re-sorting/filtering the matrix does not break selection wiring (rebind on each renderMatrix()). Add a `.selected` row style in the inline `<style>`.
  </action>
  <verify>
    <automated>grep -q "buildDiagram" tools/system-architecture-explorer/index.html && grep -Eqi "selected|selectedVariant" tools/system-architecture-explorer/index.html</automated>
  </verify>
  <acceptance_criteria>
    - Clicking a matrix row highlights it, redraws `#diagram` for that variant, and still expands its BOM detail (behaviour check — no console errors).
    - Only one row is highlighted at a time; clicking another row moves selection and redraws.
    - No second diagram control (dropdown / follow-cheapest) is present.
    - Selection and diagram redraw still work after sorting or filtering the matrix.
  </acceptance_criteria>
  <done>Row-click selects a variant, redraws the live diagram, highlights the row, and expands the BOM — a single synchronized interaction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None introduced — `buildDiagram(v)` builds an SVG string from code-controlled variant data only (labels, counts, topology from DEFAULTS/VARIANTS), not from user-typed text.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Tampering | SVG built via innerHTML | accept | Diagram strings interpolate only code-controlled variant fields (v.b, v.c, counts); no user-typed input reaches buildDiagram — the one user-typed field (source note, 06-05) is not rendered here |
| T-06-SC | Tampering | dependency installs | accept | No installs; hand-built SVG, no charting/diagram CDN library added |
</threat_model>

<verification>
- Clicking each topology-class variant renders a legible, distinct diagram with all D-03 elements.
- Reduced-motion honoured; no horizontal scroll.
</verification>

<success_criteria>
A live variant-driven SVG diagram redraws on row-click, shows all three comms layers with real node counts + power + alignment, and is the tool's single diagram control — fulfilling the "Explorer" promise.
</success_criteria>

<output>
Create `.planning/phases/06-system-architecture-explorer-promote-the-electronics-archite/06-06-SUMMARY.md` when done.
</output>
