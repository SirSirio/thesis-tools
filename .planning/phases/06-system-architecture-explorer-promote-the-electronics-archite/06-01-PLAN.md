---
phase: 06-system-architecture-explorer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/system-architecture-explorer/index.html
  - prototypes/System-Architecture/index.html
autonomous: true
requirements: [ARCH-01, ARCH-03]
must_haves:
  truths:
    - "Opening tools/system-architecture-explorer/index.html shows the tool in the site dark-glass theme with an ← All tools nav bar"
    - "All prior behaviour works: edit component price (€ or DKK), DKK↔EUR converter, sort by cost/complexity/concurrency, filter by max price/complexity/concurrency, expand a row for its BOM math, shared-block whole-system vs controller-only toggle (D-03 behaviour retained)"
    - "Edited prices and the DKK→EUR rate survive a page reload; Reset restores DEFAULTS and clears the persisted values (D-06)"
    - "The page is structured in two parts with a Part-01 theory shell (id=theory) preceding the interactive matrix (id=matrix) and a diagram region (id=diagram) (D-04)"
    - "No prototypes/System-Architecture/index.html remains after the move"
  artifacts:
    - path: "tools/system-architecture-explorer/index.html"
      provides: "Promoted, reskinned, persistent cost-matrix tool with two-part shell"
      min_lines: 300
      contains: "sae-prices"
    - path: "prototypes/System-Architecture/index.html"
      provides: "DELETED after move — must not exist"
  key_links:
    - from: "tools/system-architecture-explorer/index.html"
      to: "assets/style.css"
      via: "link rel=stylesheet ../../assets/style.css"
      pattern: "\\.\\./\\.\\./assets/style\\.css"
    - from: "tools/system-architecture-explorer/index.html"
      to: "localStorage"
      via: "persist()/loadPersisted() try-catch with sae-prices / sae-rate keys"
      pattern: "localStorage\\.(get|set)Item\\(['\"]sae-"
---

<objective>
Promote the ad-hoc cost/complexity matrix from `prototypes/System-Architecture/index.html` (310 lines) into a first-class tool at `tools/system-architecture-explorer/index.html`: move the entire cost engine verbatim, reskin it onto `assets/style.css` design tokens (delete its private `:root` block), wrap it in the standard tool chrome (nav + header) and a two-part shell (theory Part-01 placeholder → interactive Part-02), add localStorage price/rate persistence (D-06), and delete the old page.

Purpose: Establishes the tool file with full behaviour parity so later plans can layer theory prose (06-03), new data/pin-budget (06-05), and the live diagram (06-06) onto a stable, correctly-themed foundation. Locks the anchor ids (`#theory`, `#matrix`, `#diagram`) and localStorage keys (`sae-prices`, `sae-rate`) that downstream plans and the trimmed decision records depend on.
Output: `tools/system-architecture-explorer/index.html` (behaviour-complete + persistent); old `prototypes/System-Architecture/index.html` deleted.
</objective>

<execution_context>
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/workflows/execute-plan.md
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-system-architecture-explorer-promote-the-electronics-archite/06-CONTEXT.md
@.planning/phases/06-system-architecture-explorer-promote-the-electronics-archite/06-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the promoted, reskinned two-part tool file with the engine ported verbatim</name>
  <files>tools/system-architecture-explorer/index.html</files>
  <read_first>
    - prototypes/System-Architecture/index.html (the exact source: DEFAULTS, COMP, SHARED_BOM, VARIANTS, costOf, stars, conPill, renderComps, syncCompRow, refreshAllDkk, renderMatrix, bomHtml, all control wiring — port this whole engine unchanged)
    - assets/style.css (canonical design tokens: --bg, --accent, --accent-2, --accent-glow, --glass-bg, --glass-border, --text, --text-muted, --radius, --font — the tool must consume these, not redefine them)
    - tools/peristaltic-roller-displaced-volume-model/index.html (the two-part precedent: tool-nav / tool-main / tool-header markup, .part-label with .num pill, .theory-card / .theory-section, .glass-panel with id anchor for the interactive part)
    - tools/rotor-solver/index.html (sibling tool: exact nav-bar label/markup and the ../../assets/style.css link line to mirror)
    - .planning/phases/06-system-architecture-explorer-promote-the-electronics-archite/06-PATTERNS.md (Shared Patterns: the old→new CSS var-name mapping and the nav/part-label excerpts)
  </read_first>
  <action>
    Create tools/system-architecture-explorer/index.html. Link the shared stylesheet with `<link rel="stylesheet" href="../../assets/style.css" />` (relative depth is identical to the old location — 2 levels — so ../../ is correct). Add the standard tool chrome copied from a sibling: a `.tool-nav` bar whose back-link points to `../../index.html` with the same label sibling tools use (match rotor-solver / displaced-volume-model exactly) and a `<span>` reading "System Architecture Explorer"; a `.tool-main` > `.tool-header` with a `.section-label` and an `<h1>` titled "System Architecture Explorer".

    Port the ENTIRE existing engine from prototypes/System-Architecture/index.html UNCHANGED in logic (D-08 makes this inline script the canonical data source): DEFAULTS, `COMP = JSON.parse(JSON.stringify(DEFAULTS))`, SHARED_BOM, all 17 VARIANTS, state vars, costOf, stars, conPill, renderComps, syncCompRow, refreshAllDkk, renderMatrix, bomHtml, and every control listener (rate, convDkk/updateConv, fShared, fPrice/maxPriceLbl, fCx, fCon, reset, sortable headers). Keep all existing DOM ids (rate, convDkk, convEur, fShared, fPrice, maxPriceLbl, fCx, fCon, reset, compBody, matrixBody, rowCount) so the ported listeners bind unchanged. Preserve the DKK↔EUR converter, sort/filter, per-row BOM expand, and shared-block toggle behaviour exactly.

    Reskin the CSS: DELETE the source file's private `:root` block entirely (do not keep a second `:root`). In the inline `<style>`, replace design-system token references using this exact mapping — `--card`→`--glass-bg`, `--line`→`--glass-border`, `--txt`→`--text`, `--mut`→`--text-muted`, `--acc`→`--accent`, `--red`→`--accent-2`, `--bg` stays `--bg`. Keep the tool-specific status colours that are NOT part of the design system (`--ok` #3ec06b, `--warn` #e0a53c, `--blue` #4a90d9 used by the concurrency pills and driver-class colouring) as local declarations inside the inline `<style>` — these are legitimately tool-specific per CLAUDE.md, only the shared design tokens move to style.css. Ensure no horizontal scroll at 1280px and 375px (tables already use `overflow-x:auto` wrappers — keep them).

    Wrap the content in the two-part structure (D-04), mirroring the displaced-volume-model: emit a Part-01 `.part-label` (`<span class="num">Part 01</span><h2>…</h2>`) with an empty/placeholder `.theory-card` carrying `id="theory"` (06-03 fills the prose — leave a one-line "theory content follows" placeholder paragraph so the section is not empty). Then a Part-02 `.part-label` for the interactive half; give the matrix/controls container `id="matrix"` and add an empty `<div id="diagram"></div>` region reserved for 06-06's SVG. Do NOT include a KaTeX include (no LaTeX needed). English-only, no data-i18n on tool content (matches the displaced-volume-model precedent).
  </action>
  <verify>
    <automated>test -f tools/system-architecture-explorer/index.html && grep -q 'id="matrix"' tools/system-architecture-explorer/index.html && grep -q 'id="theory"' tools/system-architecture-explorer/index.html && grep -q 'id="diagram"' tools/system-architecture-explorer/index.html && grep -q '\.\./\.\./assets/style\.css' tools/system-architecture-explorer/index.html && grep -qc 'const VARIANTS' tools/system-architecture-explorer/index.html</automated>
  </verify>
  <acceptance_criteria>
    - File `tools/system-architecture-explorer/index.html` exists and links `../../assets/style.css`.
    - Grep finds NO second `:root` block redefining `--card`/`--line`/`--txt`/`--mut`/`--acc`/`--red`: `grep -Ec -- '--(card|line|txt|mut|acc|red)\b' tools/system-architecture-explorer/index.html` returns 0 (the old token names are fully replaced; local status colours use distinct names --ok/--warn/--blue).
    - Grep confirms the ported engine identifiers present: `DEFAULTS`, `SHARED_BOM`, `const VARIANTS`, `function costOf`, `function renderMatrix`, `function bomHtml` all found.
    - Anchor ids `theory`, `matrix`, `diagram` all present; Part-01 and Part-02 `.part-label` blocks both present.
    - Opening the file in a browser shows the matrix, editing a component price updates the Cost column, sort headers re-sort, the three filters and the shared-block toggle work, and clicking a row expands its BOM (behaviour parity — no console errors).
  </acceptance_criteria>
  <done>Tool file exists, themed on style.css tokens, two-part shell with locked anchors, full engine behaviour retained.</done>
</task>

<task type="auto">
  <name>Task 2: Add localStorage price/rate persistence (D-06) and delete the old page</name>
  <files>tools/system-architecture-explorer/index.html, prototypes/System-Architecture/index.html</files>
  <read_first>
    - tools/system-architecture-explorer/index.html (the file just created — the COMP object, rate state var, renderComps/renderMatrix/reset handler to hook persistence into)
    - tools/rotor-solver/index.html (the site-wide localStorage try/catch idiom around the `lang` key — mirror its shape with new keys)
    - .planning/phases/06-system-architecture-explorer-promote-the-electronics-archite/06-RESEARCH.md (Pattern 3: loadPersisted()/persist() reference shape; Pitfall 3: key-collision — keys MUST be sae-prices / sae-rate, never lang)
  </read_first>
  <action>
    Add two functions to the inline script: `loadPersisted()` reads `localStorage.getItem('sae-prices')` (JSON of `{compKey: eur}`) and `localStorage.getItem('sae-rate')`, all inside a single `try{…}catch(e){}` (mirror the site `lang` idiom). For each restored price, assign to `COMP[k].eur` ONLY if `COMP[k]` exists AND the value passes `Number.isFinite(v) && v >= 0` (guard against hand-edited/malformed storage — Security V5/tampering). For the rate, apply only if `Number.isFinite(parsed) && parsed > 0`. `persist()` writes a `{k: COMP[k].eur}` snapshot to `sae-prices` and `String(rate)` to `sae-rate`, wrapped in `try{…}catch(e){}`.

    Call `loadPersisted()` at init BEFORE the first `renderComps(); renderMatrix();`. Call `persist()` inside the existing price-edit input handler (after `COMP[k].eur` is set), inside the rate input handler (after `rate` is updated), so every edit persists immediately. Extend the existing Reset handler so that after restoring `COMP[k].eur=DEFAULTS[k].eur` it also removes/clears both keys via `try{ localStorage.removeItem('sae-prices'); localStorage.removeItem('sae-rate'); }catch(e){}` and re-renders — Reset is the escape hatch back to DEFAULTS (D-06). Keep the existing `parseFloat(e.target.value)||0` number-input guard on the price/DKK/rate fields.

    Then DELETE `prototypes/System-Architecture/index.html` (the move is complete; the engine now lives in tools/). Do not delete the three `.md` records in that folder — only the `index.html`.
  </action>
  <verify>
    <automated>grep -q "sae-prices" tools/system-architecture-explorer/index.html && grep -q "sae-rate" tools/system-architecture-explorer/index.html && grep -q "Number.isFinite" tools/system-architecture-explorer/index.html && [ ! -f prototypes/System-Architecture/index.html ] && ls prototypes/System-Architecture/*.md</automated>
  </verify>
  <acceptance_criteria>
    - `loadPersisted` and `persist` functions exist; both wrap all localStorage access in try/catch.
    - Restored values are validated with `Number.isFinite` and a non-negative/positive guard before assignment (grep finds `Number.isFinite`).
    - Editing a price then reloading the page shows the edited price retained; pressing Reset restores DEFAULTS and a subsequent reload shows DEFAULTS (keys cleared).
    - `prototypes/System-Architecture/index.html` no longer exists; `ARCHITECTURE.md`, `PUMP-CONTROL-CONCEPTS.md`, `SOLUTION-MATRIX.md` still present in that folder.
    - Keys used are exactly `sae-prices` and `sae-rate`; grep confirms the string `lang` is NOT used as a storage key in this file.
  </acceptance_criteria>
  <done>Prices and rate persist across reloads with validated deserialization; Reset clears storage; old index.html removed with the three records intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → running page | Persisted `sae-prices` / `sae-rate` values are deserialized on load; a user could hand-edit them (self-inflicted, client-only) |
| Number inputs → cost engine | Price / DKK / rate fields feed arithmetic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `loadPersisted()` deserializing sae-prices/sae-rate | mitigate | Wrap JSON.parse + getItem in try/catch; assign only values passing `Number.isFinite(v) && v>=0` (rate `>0`) before touching COMP/rate |
| T-06-02 | Tampering | price / DKK / rate number inputs | mitigate | Keep existing `parseFloat(value)||0` guard; negative/NaN coerced to safe default |
| T-06-SC | Tampering | dependency installs | accept | No npm/pip/CDN installs in this phase — nothing to audit; static vanilla JS only |
</threat_model>

<verification>
- Manual: load tool, edit prices, reload → prices retained; Reset → DEFAULTS restored and persisted values cleared.
- Grep: no dangling references to the deleted prototypes/System-Architecture/index.html remain inside the new tool file.
- No horizontal scroll at 1280px and 375px.
</verification>

<success_criteria>
Tool exists at the canonical path, themed on shared tokens, two-part shell with locked anchors, full prior behaviour retained, prices/rate persisted with safe deserialization, old page deleted with records intact.
</success_criteria>

<output>
Create `.planning/phases/06-system-architecture-explorer-promote-the-electronics-archite/06-01-SUMMARY.md` when done. Record the final nav-bar label used and confirm anchor ids `#theory`, `#matrix`, `#diagram` and keys `sae-prices`, `sae-rate` for downstream plans.
</output>
