---
phase: 06-system-architecture-explorer
plan: 07
type: execute
wave: 4
depends_on: ["06-05"]
files_modified:
  - tools/system-architecture-explorer/SPEC.md
autonomous: true
requirements: [ARCH-05]
must_haves:
  truths:
    - "A co-located SPEC.md documents the component price table (with source/confidence columns), the 17+ variant BOMs, the three comms layers A/B/C, the pin-budget model and its assumptions, the power/PSU model (60W/150W), and the cost-model assumptions (SC-5, ARCH-05)"
    - "SPEC.md carries a High/Medium/Low confidence-tag legend (D-11) and flags the screen SPI-vs-parallel ambiguity as an open item"
    - "SPEC.md states the localStorage keys (sae-prices, sae-rate), English-only interface, and that all logic/styles are inline with no new shared files"
  artifacts:
    - path: "tools/system-architecture-explorer/SPEC.md"
      provides: "Canonical tool spec per the site tool-spec standard"
      min_lines: 60
      contains: "Confidence"
  key_links:
    - from: "tools/system-architecture-explorer/SPEC.md"
      to: "tools/system-architecture-explorer/index.html"
      via: "spec documents the co-located tool"
      pattern: "system-architecture-explorer"
---

<objective>
Author the co-located `SPEC.md` per the site tool-spec standard, documenting the component price table (with source/confidence), the variant BOMs, the three comms layers, the pin-budget model + assumptions, the power/PSU model, and the cost-model assumptions (ARCH-05 / SC-5). Written after 06-05 so the variant list, SCREEN_PINS values, and confidence tags match the shipped tool.

Purpose: Every tool on the site has a SPEC.md as its canonical reference; this one also records the pin-budget assumptions and the screen-interface open question so a thesis reader can audit the cost/feasibility model.
Output: `tools/system-architecture-explorer/SPEC.md`.
</objective>

<execution_context>
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/workflows/execute-plan.md
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-system-architecture-explorer-promote-the-electronics-archite/06-RESEARCH.md
@.planning/phases/06-system-architecture-explorer-promote-the-electronics-archite/06-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write tools/system-architecture-explorer/SPEC.md (ARCH-05)</name>
  <files>tools/system-architecture-explorer/SPEC.md</files>
  <read_first>
    - tools/rotor-solver/SPEC.md (the section-structure template: Purpose · Inputs · Outputs · Feasibility checks · Diagram section · Known values · Assumptions · Language support — adapt these headings)
    - tools/system-architecture-explorer/index.html (the SHIPPED data: final DEFAULTS with source/confidence, the full VARIANTS list incl. integrated-screen rows, SHARED_BOM, SCREEN_PINS, pinsOf/costOf, the interface-scenario toggle — document what actually exists)
    - .planning/phases/06-system-architecture-explorer-promote-the-electronics-archite/06-RESEARCH.md (the pin-budget tables, Layer-A/B/C pin costs, RAM/PSRAM framing, and the Assumptions Log / Open Questions to transcribe into the SPEC's assumptions section — esp. the screen SPI/parallel ambiguity)
    - prototypes/System-Architecture/SOLUTION-MATRIX.md (the source component-price list + comms-layer descriptions — SPEC is where the authoritative model is documented, per D-08)
  </read_first>
  <action>
    Create tools/system-architecture-explorer/SPEC.md following the rotor-solver SPEC structure, adapted to: Purpose & scope · Component price table (component, role, default €, source, confidence — reflecting the shipped DEFAULTS) · Variant BOMs (the full shipped VARIANTS list, one line each: id, at-once, driver, bus, C-link, complexity, BOM) · The three comms layers A (brain↔screen, fixed, SPI/parallel scenarios) / B (system bus I²C/RS-485/CAN, pins per node) / C (driver link topologies + pin costs) · Pin-budget model — how pinsOf(v) sums fixed + Layer-B + Layer-C pins against the brain's usable GPIO, the SCREEN_PINS values for both scenarios, and the OVERRUN rule · Power/PSU model (60W for 1-2 at once, 150W for all-6; 12V/24V dual rail, common ground) · RAM/PSRAM & UI-fluidity note (framebuffer arithmetic once, then per-brain "PSRAM yes/no → comfortable/tight") · Cost-model assumptions (hobby-class ±20%, shared-block composition) · Confidence-tag legend (High/Medium/Low per D-11) · Open questions (screen SPI-vs-parallel ambiguity flagged, TMC2209 UART mode) · Persistence (localStorage keys sae-prices/sae-rate) · Language support (English-only per site precedent for post-i18n tools). Tag pin-budget/hardware numbers with their confidence honestly (most MEDIUM/ASSUMED). Cross-link back to the three decision records in prototypes/System-Architecture/.
  </action>
  <verify>
    <automated>test -f tools/system-architecture-explorer/SPEC.md && grep -qi "confidence" tools/system-architecture-explorer/SPEC.md && grep -qi "pin" tools/system-architecture-explorer/SPEC.md && grep -q "sae-prices" tools/system-architecture-explorer/SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    - `tools/system-architecture-explorer/SPEC.md` exists with sections covering: component price table (incl. source/confidence), variant BOMs, three comms layers, pin-budget model + SCREEN_PINS both scenarios + OVERRUN rule, power/PSU model, RAM/PSRAM note, cost-model assumptions, confidence-tag legend, open questions (screen ambiguity), persistence keys, and language support.
    - The documented variant list and price data MATCH the shipped index.html (spot-check the integrated-screen variant and the screen's High confidence are present).
    - A High/Medium/Low confidence legend is present; hardware numbers carry confidence tags.
    - The screen SPI-vs-parallel ambiguity is documented as an open item.
    - SPEC cross-links the three decision records in prototypes/System-Architecture/.
  </acceptance_criteria>
  <done>SPEC.md documents the full cost + pin-budget + comms-layer model with confidence tags, matching the shipped tool.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None introduced — SPEC.md is documentation only, no runtime surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Information Disclosure | SPEC content | accept | Documents a hobby-electronics BOM; no secrets, no sensitive data |
| T-06-SC | Tampering | dependency installs | accept | No installs; documentation only |
</threat_model>

<verification>
- SPEC matches the shipped data model; confidence legend and screen ambiguity documented.
</verification>

<success_criteria>
The tool has a co-located SPEC.md meeting the site tool-spec standard and documenting the cost, pin-budget, comms-layer, and power models with confidence tags.
</success_criteria>

<output>
Create `.planning/phases/06-system-architecture-explorer-promote-the-electronics-archite/06-07-SUMMARY.md` when done.
</output>
