---
phase: 06-system-architecture-explorer
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - tools/system-architecture-explorer/index.html
  - prototypes/System-Architecture/ARCHITECTURE.md
  - prototypes/System-Architecture/PUMP-CONTROL-CONCEPTS.md
  - prototypes/System-Architecture/SOLUTION-MATRIX.md
  - prototypes/PROTOTYPES.md
autonomous: true
requirements: [ARCH-04]
must_haves:
  truths:
    - "Part 01 of the tool (id=theory) reads as a self-explanatory reasoning section: the three comms layers A/B/C, the 'the driver matters more than the MCU' insight, and the U5 concurrency axis as the dominant cost driver (D-04)"
    - "The tool is canonical for that reasoning prose; the three decision records are trimmed to pointers for the overlapping explanatory passages and link INTO the tool's theory section, while keeping their raw exploration/audit-trail content (D-05)"
    - "SOLUTION-MATRIX.md's static table carries an explicit preface that the live tool computes the authoritative numbers and this is a reference view (D-08)"
    - "Every former in-record link to the deleted index.html now points to ../../tools/system-architecture-explorer/index.html with the correct anchor; no dangling reference remains (RESEARCH Pitfall 2)"
    - "prototypes/System-Architecture/ is cross-linked from PROTOTYPES.md as an architecture study, mirroring the multi-liquid-architecture precedent"
  artifacts:
    - path: "tools/system-architecture-explorer/index.html"
      provides: "Filled Part-01 theory content under id=theory"
      contains: "Layer"
    - path: "prototypes/System-Architecture/ARCHITECTURE.md"
      provides: "Trimmed to pointer for overlapping prose; link repointed to the tool"
    - path: "prototypes/System-Architecture/SOLUTION-MATRIX.md"
      provides: "Reference-view preface pointing to the tool as authoritative (D-08)"
  key_links:
    - from: "prototypes/System-Architecture/ARCHITECTURE.md"
      to: "tools/system-architecture-explorer/index.html"
      via: "repointed diagram/tool link"
      pattern: "\\.\\./\\.\\./tools/system-architecture-explorer/index\\.html"
    - from: "prototypes/PROTOTYPES.md"
      to: "prototypes/System-Architecture/"
      via: "architecture-study cross-link row"
      pattern: "System-Architecture"
---

<objective>
Make the tool canonical for the architecture reasoning prose (D-05) and repair every cross-link broken by 06-01's file move. Author Part 01 of the tool (the `id="theory"` region) with the reasoning half — the three comms layers, the driver-vs-MCU insight, and the U5 concurrency axis — then trim the three decision records to pointers for the passages that now live in the tool, repoint their links from the deleted `index.html` to the new tool path, add the D-08 "reference view / tool is authoritative" preface to SOLUTION-MATRIX.md, and add an architecture-study cross-link row in PROTOTYPES.md.

Purpose: A thesis reader arriving via QR code sees a self-explanatory tool (D-04), one home per idea with no drift (D-05/D-08), and no orphaned/dangling links after the move (RESEARCH Pitfall 2, ARCH-04).
Output: Filled theory section in the tool; three trimmed+repointed records; a PROTOTYPES.md cross-link row.
</objective>

<execution_context>
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/workflows/execute-plan.md
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-system-architecture-explorer-promote-the-electronics-archite/06-CONTEXT.md
@.planning/phases/06-system-architecture-explorer-promote-the-electronics-archite/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Part-01 theory/reasoning section in the tool (D-04)</name>
  <files>tools/system-architecture-explorer/index.html</files>
  <read_first>
    - tools/system-architecture-explorer/index.html (the placeholder id=theory .theory-card created by 06-01 — fill it, do not restructure the Part shell)
    - prototypes/System-Architecture/PUMP-CONTROL-CONCEPTS.md (prime source: the 7-concept menu, serial↔6-parallel, dumb↔smart↔motion drivers, the "who makes the step pulses?" mental model → the driver-vs-MCU insight)
    - prototypes/System-Architecture/ARCHITECTURE.md (the three comms layers A/B/C definitions, fixed components, U5 concurrency axis, power rails)
    - prototypes/Prototype-1-Pump-Module/multi-liquid-architecture/ARCHITECTURE-DECISION.md (owns U5 — the concurrency axis this section frames as the dominant cost driver)
    - tools/peristaltic-roller-displaced-volume-model/index.html (.theory-card / .theory-section h3 markup to mirror for the theory panels)
  </read_first>
  <action>
    Fill the `id="theory"` `.theory-card` with reasoning prose organised into `.theory-section` panels (each with an `<h3>`), section order at your discretion per D-04 but covering all three of: (1) "The Three Comms Layers" — Layer A brain↔screen (SPI/parallel, fixed ILI9341), Layer B the system bus (I²C / RS-485 / CAN, the open choice, drawn with real node count), Layer C the pump-controller↔driver topology (STEP/DIR vs UART vs SPI); (2) "The driver matters more than the MCU" — the insight from PUMP-CONTROL-CONCEPTS.md that a smart/motion driver offloads step generation, changing what the brain must do (e.g. TMC2209 lets the ESP32 stay fused even at 6-parallel); (3) "The U5 concurrency axis" — that how many pumps run at once is the dominant cost driver (PSU 60W→150W, more nodes, more bus complexity), cross-referencing the multi-liquid architecture study that owns U5. Keep it thesis-readable prose, English-only, using the site's .theory-card/.theory-section/h3 pattern. This prose becomes canonical (D-05) — write it as the single home for these explanations. Do not add LaTeX/KaTeX.
  </action>
  <verify>
    <automated>grep -qi "Layer A" tools/system-architecture-explorer/index.html && grep -qi "Layer B" tools/system-architecture-explorer/index.html && grep -qi "Layer C" tools/system-architecture-explorer/index.html && grep -qi "concurrency" tools/system-architecture-explorer/index.html && grep -qi "driver" tools/system-architecture-explorer/index.html</automated>
  </verify>
  <acceptance_criteria>
    - The `id="theory"` region contains multiple `.theory-section` panels (no longer the 06-01 placeholder).
    - Prose explicitly names Layer A, Layer B, Layer C and describes each; states the "driver matters more than the MCU" insight; frames U5 concurrency as the dominant cost driver with a link to the multi-liquid architecture study.
    - English-only, no data-i18n keys, no KaTeX include added.
    - No horizontal scroll introduced at 1280px / 375px.
  </acceptance_criteria>
  <done>Part 01 reads as a self-contained reasoning section covering the three layers, the driver insight, and the U5 axis.</done>
</task>

<task type="auto">
  <name>Task 2: Trim the three decision records to pointers, repoint links to the tool, add SOLUTION-MATRIX reference-view preface, and add the PROTOTYPES.md cross-link row</name>
  <files>prototypes/System-Architecture/ARCHITECTURE.md, prototypes/System-Architecture/PUMP-CONTROL-CONCEPTS.md, prototypes/System-Architecture/SOLUTION-MATRIX.md, prototypes/PROTOTYPES.md</files>
  <read_first>
    - prototypes/System-Architecture/ARCHITECTURE.md (links at lines 14, 43-44, 110 point to the deleted index.html — repoint; trim the comms-layer prose that now lives in the tool to a pointer)
    - prototypes/System-Architecture/SOLUTION-MATRIX.md (links at lines 16-17, 23, 64, 126 reference index.html / the docs; add the D-08 reference-view preface)
    - prototypes/System-Architecture/PUMP-CONTROL-CONCEPTS.md (link at line 155 references index.html; trim overlapping concept prose to a pointer into the tool theory section)
    - prototypes/Prototype-1-Pump-Module/multi-liquid-architecture/ARCHITECTURE-DECISION.md (precedent: a study folder under prototypes/ that is NOT a proto-NN, keeps raw exploration content, feeds a canonical artifact elsewhere)
    - prototypes/PROTOTYPES.md ("Architecture studies (not part of the proto-NN streak)" table — add a row for System-Architecture/ in the same shape)
  </read_first>
  <action>
    In all three records, repoint every link that targets the now-deleted `index.html` to `../../tools/system-architecture-explorer/index.html` with the appropriate anchor (`#matrix` for the live matrix/converter references, `#diagram` for "See the diagram" at ARCHITECTURE.md:110, `#theory` for concept-explanation pointers). Both folders sit 2 levels deep so `../../assets/` depth is unchanged — only these inter-file links change. The specific line references: ARCHITECTURE.md lines 14, 43-44, 110; SOLUTION-MATRIX.md lines 23 and 126; PUMP-CONTROL-CONCEPTS.md line 155. (Cross-links BETWEEN the records — ARCHITECTURE↔PUMP-CONTROL↔SOLUTION-MATRIX at SOLUTION-MATRIX.md:16-17,64 and PUMP-CONTROL-CONCEPTS.md:17 — stay same-folder relative and are unchanged.)

    Trim per D-05 (direction locked, aggressiveness is your judgment with the prose side by side): replace passages in ARCHITECTURE.md and PUMP-CONTROL-CONCEPTS.md that DUPLICATE the tool's new theory section (three comms layers, driver-vs-MCU, concurrency framing) with a one-line pointer into `#theory`, but KEEP each record's raw exploration/audit-trail content that has no home in the tool — fixed components list, open questions (U5, screen type, physical layout), and the "different Arduino per pump" verdict. Do not gut the records; only de-duplicate the overlapping explanatory prose.

    In SOLUTION-MATRIX.md, add a D-08 preface sentence at the top of the static matrix/price table making explicit that "the live tool at ../../tools/system-architecture-explorer/index.html computes the authoritative numbers; this table is a human-readable reference snapshot."

    In prototypes/PROTOTYPES.md, add a cross-link row under the "Architecture studies (not part of the proto-NN streak)" table pointing to `System-Architecture/` and noting the live tool now lives in tools/, mirroring the existing multi-liquid-architecture row shape.
  </action>
  <verify>
    <automated>grep -rl "System-Architecture/index.html" prototypes/System-Architecture/*.md; test $? -ne 0 && grep -q "system-architecture-explorer/index.html" prototypes/System-Architecture/ARCHITECTURE.md && grep -q "system-architecture-explorer/index.html" prototypes/System-Architecture/SOLUTION-MATRIX.md && grep -q "system-architecture-explorer" prototypes/System-Architecture/PUMP-CONTROL-CONCEPTS.md && grep -q "System-Architecture" prototypes/PROTOTYPES.md</automated>
  </verify>
  <acceptance_criteria>
    - No `.md` record links to a bare same-folder `index.html` anymore: `grep -rn "(index.html)" prototypes/System-Architecture/*.md` shows every remaining index.html reference is the `../../tools/system-architecture-explorer/index.html` form (grep for `](index.html)` returns no matches).
    - ARCHITECTURE.md:110's "See the diagram" now targets `../../tools/system-architecture-explorer/index.html#diagram`.
    - SOLUTION-MATRIX.md contains a preface sentence naming the tool as authoritative and itself as a reference view (D-08).
    - All three records still contain their retained raw content (fixed components / open questions / "different Arduino per pump" verdict — spot-check present).
    - PROTOTYPES.md has a new row referencing `System-Architecture/`.
    - Repo-wide `grep -rn "System-Architecture/index.html"` across the repo returns no live references to the deleted file (RESEARCH Pitfall 2).
  </acceptance_criteria>
  <done>Records trimmed to pointers with links repointed, SOLUTION-MATRIX reference-view preface added, PROTOTYPES cross-link row present, no dangling references remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None introduced — Task 1 adds static, code-authored HTML prose (no user input); Task 2 edits Markdown docs only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | static theory HTML / markdown edits | accept | Content is code-authored, no user-supplied data rendered; no runtime surface added |
| T-06-SC | Tampering | dependency installs | accept | No installs; nothing to audit |
</threat_model>

<verification>
- Repo-wide grep confirms no reference to the deleted `System-Architecture/index.html` survives.
- Theory section covers all three required topics; records retain their raw content.
</verification>

<success_criteria>
Tool Part-01 reads as canonical reasoning; the three records point into it without duplication; every cross-link resolves to the new tool path; PROTOTYPES.md cross-links the study.
</success_criteria>

<output>
Create `.planning/phases/06-system-architecture-explorer-promote-the-electronics-archite/06-03-SUMMARY.md` when done.
</output>
