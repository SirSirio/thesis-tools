---
phase: 06-system-architecture-explorer
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - index.html
  - README.md
  - ROADMAP.md
  - CLAUDE.md
autonomous: true
requirements: [ARCH-02, ARCH-06]
must_haves:
  truths:
    - "The landing page (index.html) has a card linking to tools/system-architecture-explorer/index.html, styled like the other tool cards (D-04 tool reachable)"
    - "README.md tool table has a row for the System Architecture Explorer terminated with a Live status"
    - "Repo-root ROADMAP.md Shipped table lists the tool"
    - "CLAUDE.md folder structure documents tools/system-architecture-explorer/ (index.html + SPEC.md) and reflects that prototypes/System-Architecture/ now holds only the three .md records (index.html removed)"
  artifacts:
    - path: "index.html"
      provides: "Landing-page card linking the new tool"
      contains: "system-architecture-explorer"
    - path: "README.md"
      provides: "Tool-table row"
      contains: "system-architecture-explorer"
    - path: "CLAUDE.md"
      provides: "Updated folder tree entry for the new tool"
      contains: "system-architecture-explorer"
  key_links:
    - from: "index.html"
      to: "tools/system-architecture-explorer/index.html"
      via: "tool-card anchor href"
      pattern: "tools/system-architecture-explorer/index\\.html"
---

<objective>
Wire the promoted tool into the site chrome and docs: add a landing-page card (ARCH-02), a README tool-table row, a repo-root ROADMAP Shipped entry, and update the CLAUDE.md folder tree (ARCH-06) to show `tools/system-architecture-explorer/` and that `prototypes/System-Architecture/` now holds only the three decision records.

Purpose: The tool is only "first-class" once it is reachable from the landing page and listed like every other tool. This plan touches only chrome/doc files (root `index.html`, `README.md`, `ROADMAP.md`, `CLAUDE.md`) — disjoint from the tool file — so it runs in parallel with 06-03.
Output: One new landing card, one README row, one ROADMAP row, updated CLAUDE.md folder structure.
</objective>

<execution_context>
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/workflows/execute-plan.md
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-system-architecture-explorer-promote-the-electronics-archite/06-PATTERNS.md
@README.md
@ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the landing-page tool card (ARCH-02)</name>
  <files>index.html</files>
  <read_first>
    - index.html (repo root: the existing `.tool-card` blocks, the `<section aria-label>` + `.section-head` (`.section-index`) + `.section-desc` wrapper convention, and the LANG dictionary key convention `card-title-*` / `card-desc-*` / `card-link-*` — check how the prototypes/Prototype Design Space card handles text so the new card renders correctly in both EN and IT)
    - .planning/phases/06-system-architecture-explorer-promote-the-electronics-archite/06-PATTERNS.md (landing card section: exact `.tool-card` markup to copy and the note on English-only card text vs the bilingual landing page)
  </read_first>
  <action>
    Add one `.tool-card` (compact variant, not `--wide`) linking `href="tools/system-architecture-explorer/index.html"`, mirroring the existing rotor-solver card markup: a `.card-icon` glyph, a `.card-title`, a `.card-desc` one-liner (e.g. "Interactive cost / complexity / pin-budget matrix for the control electronics — 17 architecture variants, editable BOM, DKK↔EUR, live system diagram"), and a `.card-link` with the arrow. Place it in an existing tool section grid (adding one card to the current grid — the landing-page redesign is explicitly out of scope). Because the landing page is bilingual: follow the existing card convention — if the surrounding cards use `data-i18n` keys, add matching `card-title-*/card-desc-*` EN+IT entries to the page's LANG dictionary using the established naming (e.g. `card-title-sysarch`); if the section uses plain static text (as the prototypes card does), match that. Do not introduce a new shared file or restructure the grid.
  </action>
  <verify>
    <automated>grep -q 'tools/system-architecture-explorer/index.html' index.html && grep -qi 'class="tool-card' index.html</automated>
  </verify>
  <acceptance_criteria>
    - A `.tool-card` anchor with `href="tools/system-architecture-explorer/index.html"` exists in the landing page.
    - The card renders with icon + title + description + link, visually consistent with sibling cards.
    - If sibling cards use `data-i18n`, matching EN and IT dictionary entries exist for the new card keys (no missing-key blanks when toggling language); if they use static text, the new card matches that pattern.
    - No horizontal scroll at 1280px / 375px; card follows the existing reveal/hover behaviour.
  </acceptance_criteria>
  <done>Landing page links the new tool via a themed card, bilingual-consistent with the page.</done>
</task>

<task type="auto">
  <name>Task 2: Add README row, ROADMAP Shipped entry, and update the CLAUDE.md folder tree (ARCH-02, ARCH-06)</name>
  <files>README.md, ROADMAP.md, CLAUDE.md</files>
  <read_first>
    - README.md (the `## Tools` table, rows 15-21 — copy the row shape: `| [Name](path) | one dense sentence of capability + interactive elements + interface language | ✅ Live |`)
    - ROADMAP.md (repo root: the `## Shipped` table rows 9-17 — copy the one-line-per-feature-cluster shape)
    - CLAUDE.md (the folder-structure block: the existing `tools/<slug>/` entries with one-line purpose comments per file, and the `prototypes/System-Architecture/` entry if present — update it to reflect index.html removed, three records retained)
  </read_first>
  <action>
    README.md: add one `## Tools` table row for `[System Architecture Explorer](tools/system-architecture-explorer/index.html)` describing it as the control-electronics cost/complexity/pin-budget selector — editable BOM prices with source/confidence tags, DKK↔EUR converter, 17-variant sortable/filterable matrix, live variant-driven system diagram, English interface — terminated `| ✅ Live |`.

    ROADMAP.md (repo root): add a `## Shipped` row (or two if the diagram + pin-budget read as separable capabilities, mirroring how rotor-solver got multiple rows) for the tool. Optionally note it partially retires the "Bill of materials / component selector" backlog item.

    CLAUDE.md: add a `tools/system-architecture-explorer/` block to the folder tree in the same shape as the other `tools/<slug>/` entries (index.html one-liner + SPEC.md one-liner). Update the `prototypes/System-Architecture/` folder-tree entry to show it now contains only `ARCHITECTURE.md`, `PUMP-CONTROL-CONCEPTS.md`, `SOLUTION-MATRIX.md` (the `index.html` is gone), cross-linked to the tool. Keep all edits inside the existing folder-structure block; do not add a new shared file (ARCH-06: all logic/styles remain inline).
  </action>
  <verify>
    <automated>grep -q 'system-architecture-explorer/index.html' README.md && grep -qi 'System Architecture Explorer' ROADMAP.md && grep -q 'system-architecture-explorer' CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md `## Tools` table has a new row linking `tools/system-architecture-explorer/index.html`, ending in `✅ Live`.
    - Repo-root ROADMAP.md `## Shipped` table lists the tool.
    - CLAUDE.md folder tree shows `tools/system-architecture-explorer/` (index.html + SPEC.md) AND the `prototypes/System-Architecture/` entry no longer lists an `index.html` (three `.md` records only).
    - No new shared file introduced by any of these edits.
  </acceptance_criteria>
  <done>README, ROADMAP, and CLAUDE.md all reflect the shipped tool and the completed move.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None introduced — all edits are static HTML (a code-authored card, no user input) and Markdown docs.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | landing card / doc edits | accept | Static code-authored content; no runtime surface or user input added |
| T-06-SC | Tampering | dependency installs | accept | No installs; nothing to audit |
</threat_model>

<verification>
- Landing card link resolves to the tool; README/ROADMAP rows present; CLAUDE.md folder tree updated and accurate.
</verification>

<success_criteria>
The tool is reachable from the landing page and documented in README, repo ROADMAP, and CLAUDE.md, with the folder move reflected.
</success_criteria>

<output>
Create `.planning/phases/06-system-architecture-explorer-promote-the-electronics-archite/06-04-SUMMARY.md` when done.
</output>
