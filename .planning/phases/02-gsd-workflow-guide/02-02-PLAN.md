# Plan 02-02: GSD Workflow Guide — Integration

**Phase:** 2 — GSD Workflow Visualizer  
**Plan:** 2 of 2  
**Goal:** Create SPEC.md, add landing page card, add README row  
**Depends on:** 02-01-PLAN.md (tool page must exist)

---

## Outcome

- `tools/gsd-workflow-guide/SPEC.md` exists with canonical tool specification
- `index.html` (landing page) has a new tool card linking to the GSD Workflow Guide
- `README.md` (repo root) has a new row in the tool table
- `CLAUDE.md` folder structure section is updated to show the new tool folder

---

## Tasks

### Task 1 — Create `tools/gsd-workflow-guide/SPEC.md`

**File:** `tools/gsd-workflow-guide/SPEC.md` (create new)

```markdown
# SPEC: GSD Workflow Guide

## Purpose

Interactive reference page displaying the GSD (Get Shit Done) spec-driven development
workflow as a visual diagram. Demonstrates the discuss → plan → execute loop used to build
this thesis tools site. Serves as a personal methodology reference and thesis showcase artifact.

## Inputs

None — static content only, no user-configurable inputs.

## Outputs / Content Areas

### Core Loop (top-to-bottom flow with CSS arrows)

Seven command nodes rendered in order:

| Order | Command | Type | Slash |
|-------|---------|------|-------|
| 1 | spec-phase | optional | `/gsd:spec-phase N` |
| 2 | discuss-phase | core | `/gsd:discuss-phase N` |
| 3 | plan-phase | core | `/gsd:plan-phase N` |
| 4 | execute-phase | core | `/gsd:execute-phase N` |
| 5 | verify-work | optional | `/gsd:verify-work N` |
| 6 | code-review | optional | `/gsd:code-review N` |
| 7 | ship | optional | `/gsd:ship N` |

### Utility Commands Sidebar

Five utility command nodes in a sticky sidebar column:

| Command | Slash |
|---------|-------|
| progress | `/gsd:progress` |
| quick | `/gsd:quick [task]` |
| fast | `/gsd:fast` |
| debug | `/gsd:debug [description]` |
| capture | `/gsd:capture [topic]` |

## Interaction Model

- Each node displays command name (monospace, accent color) + one-line brief description (always visible)
- Clicking a node toggles it open/closed (CSS `max-height` + `opacity` transition)
- Open state: full description paragraph + exact slash command are revealed
- Multiple nodes can be open simultaneously
- Visual distinction: optional nodes use `border-style: dashed`; core nodes use solid border

## Layout

- Two-column grid: flow column (left, flexible) + sidebar (right, 280px fixed)
- Breakpoint: single-column below 720px viewport width
- Arrows: CSS pseudo-elements, no SVG

## Assumptions

- Content is static; no dynamic calculation
- GSD command names and descriptions sourced from the open-gsd/get-shit-done-redux repository (2026-05-30)
- Commands may be updated in future GSD versions; this spec reflects the version used during thesis development

## Constants / Hardcoded Values

- Sidebar width: 280px
- Mobile breakpoint: 720px
- Node expand transition: max-height 0.3s ease, opacity 0.25s ease
- Flow arrow height: 28px
- Optional badge: "optional" label, uppercase, `var(--text-muted)` color, dashed border
```

---

### Task 2 — Add tool card to `index.html` (landing page)

**File:** `index.html`

Locate the comment `<!-- Future tools — duplicate the block above and update href, title, desc -->` inside `.tools-grid`.

Insert a new `<a>` tool card block immediately before that comment:

```html
<a href="tools/gsd-workflow-guide/index.html" class="tool-card animate-in" style="--delay: 0.35s;">
<div class="card-icon">⬡</div>
<div class="card-body">
<h2 class="card-title">GSD Workflow Guide</h2>
<p class="card-desc">Interactive diagram of the spec-driven development workflow used to build this site. Explore the discuss → plan → execute loop, optional quality gates, and utility commands — click any node to see what it does.</p>
</div>
<span class="card-link">Open tool &rarr;</span></a>
```

Note: This card does NOT need `data-i18n` attributes — per D-12, new tools going forward do not get the language switcher. English-only static text is correct.

---

### Task 3 — Add row to `README.md` (repo root)

**File:** `README.md`

Find the tool table in the README. Add a new row for the GSD Workflow Guide after the existing rotor-solver row:

```markdown
| [GSD Workflow Guide](tools/gsd-workflow-guide/index.html) | Interactive diagram of the GSD spec-driven development workflow (discuss → plan → execute loop, optional steps, utility commands). Click any node to expand. | — |
```

The existing table format has columns: Tool | Description | Status (or similar). Match whatever format the existing README table uses.

---

### Task 4 — Update `CLAUDE.md` folder structure

**File:** `CLAUDE.md`

In the `## Folder structure` section, update the `tools/` directory listing to include the new tool folder. After the `rotor-solver/` block, add:

```
│   └── gsd-workflow-guide/
│       ├── index.html      GSD Workflow Guide — interactive diagram
│       └── SPEC.md         Tool spec — content, interaction model, layout constants
```

---

## Verification Checklist

- [ ] `tools/gsd-workflow-guide/SPEC.md` exists and matches the tool as implemented
- [ ] Landing page (`index.html`) shows the new GSD Workflow Guide card in the tools grid
- [ ] Clicking the card navigates to `tools/gsd-workflow-guide/index.html` (relative path correct)
- [ ] Card has no `data-i18n` attributes (no language toggle on new tools)
- [ ] `README.md` tool table has a new row for the GSD Workflow Guide
- [ ] `CLAUDE.md` folder structure is updated
- [ ] No changes to `assets/style.css`
- [ ] No new shared files
