---
phase: 06.1-architecture-refinement-full-system-module-schema-design-dir
plan: 04
subsystem: docs
tags: [spec-sync, bom, power-rail, mpr121, consistency-pass]

# Dependency graph
requires:
  - phase: 06.1-01
    provides: 26-key DEFAULTS/SHARED_BOM cost model (vibMotor/vibDriver/capTouch/buck12/buck5, 2x align28byj), extended pinsOf(), two-buck-converter diagram Power block, PRICES_VERSION '2026-07-16-06-1-whole-system'
  - phase: 06.1-02
    provides: Part 00 #system MODULES object, buildSchema(), module purpose text (D-17 note, storage container-material caveat)
  - phase: 06.1-03
    provides: directionOf() classifier, DIRECTIONS object, exact 2/2/8/8 direction-to-variant-ID mapping
provides:
  - "SPEC.md's every count/price now matches the shipped index.html: 20 variants, 17-of-26 High-confidence components, E18.26/E31.66 PSU prices, E149.65 shared-block total"
  - "New SPEC.md sections: Whole-device module schema (Part 00), Design directions, New components (unresolved risks), Power-rail model (SC-5)"
  - "Two new open questions with resolution procedures: storage container material, RD520PA current rating"
affects: [06.1-05]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "New components' risk caveats documented as a dedicated '### New components — unresolved risks' subsection rather than folded into the price-table Source column, since the plan required per-risk confidence tags distinct from the price confidence"
    - "Power-rail model kept as its own H2 section (mirroring the existing Power/PSU model's shape) rather than merged into Power/PSU model, since it answers a genuinely different question (secondary-rail sizing vs primary PSU sizing)"

key-files:
  created: []
  modified:
    - tools/system-architecture-explorer/SPEC.md

key-decisions:
  - "Recounted the 'components not stocked by bitbyg' bullet list from Six to Seven and added the previously-omitted ESP32-2432S032R (espscreen32) entry — it is Low-confidence and NOT stocked at bitbyg per its own DEFAULTS source note, but the pre-phase SPEC text never listed it; this was a second, smaller drift beyond the '13 of 21 -> 17 of 26' fix the plan called out explicitly"
  - "Split the five new component price-table rows (Task 2's explicit scope) from the alignMot2 removal/align28byj relabel (Task 1's explicit scope) across the two task commits, even though both touch the same table, to keep each commit matching its task's stated file-diff intent"
  - "Fixed the pin-budget formula code block to include the +1 vibration-PWM term, which was entirely missing from SPEC's pseudocode even though the shipped pinsOf() has charged it since 06.1-01 — a drift the plan's Task 2 acceptance criteria implied but didn't call out by exact old/new text"
  - "Also updated the 'Live system diagram' section's Power-block description (previously 'dual-rail lines') to match the actual single-24V-rail-plus-two-bucks topology, since leaving it unchanged would have reintroduced a smaller version of the same drift Task 1 was fixing elsewhere"

requirements-completed: [D-17, D-18, SC-4, SC-5, SC-6]

# Metrics
duration: 10min
completed: 2026-07-16
---

# Phase 06.1 Plan 04: SPEC.md consistency pass + new-content documentation Summary

**Recounted every stale number in `tools/system-architecture-explorer/SPEC.md` against the shipped `index.html` (20 variants, 17-of-26 High-confidence components, real bitbyg PSU prices, €149.65 shared block) and added four new sections documenting the module schema, the four design directions, the five new BOM components with their unresolved risks, and the researched two-buck-converter power-rail topology.**

## Performance

- **Duration:** ~10 min
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments

- Fixed every stale count/price Task 1 targeted: "19 candidate architectures" → 20, "Variant BOMs (19 total)" → 20, "13 of 21 High-confidence" → **17 of 26** (verified against the shipped `DEFAULTS` object directly — matched the plan's expected split exactly, no discrepancy to flag), PSU power-model prices `(€12)`/`(€20)` → the real bitbyg-sourced `€18.26`/`€31.66`, and the Rail column's stale "12V rail to logic" claim replaced with the real single-24V-rail-plus-two-bucks description
- Removed the resolved `alignMot2`/"Alignment motor #2 (TBD)" placeholder row entirely; `align28byj`'s role relabeled from "alignment motor #1" to "alignment motor ×2" matching the shipped `DEFAULTS`
- Rewrote the SHARED_BOM cost-model description from the stale "€72 total" figure to the real **€149.65**, with accurate membership (NEMA17, ILI9341, alignment Nano, 2× 28BYJ-48+ULN2003, vibration motor, IRF520 driver, MPR121, 12V buck, 5V buck)
- Dropped the superseded blanket "±20%, not quotes" cost-model claim, replacing it with the real 17/2/7 confidence breakdown
- Bumped the documented `PRICES_VERSION` stamp to `2026-07-16-06-1-whole-system`, matching the shipped value
- Added a new **"Whole-device module schema (Part 00)"** section: all six modules by name with their hardware payload and purpose, the liquid-flow (Storage→Pump→Nozzle→Alignment) and data-flow (Software & Electronics ↔ everything) connectivity, the D-04 hardware-requirements-baseline framing, the D-05 spatial-layout-carries-no-meaning statement, and confirmation that the old tube-holding module no longer exists (its rack lives in Alignment)
- Added a new **"Design directions"** section with the exact `directionOf(v)` derivation logic and the full 2/2/8/8 variant-ID mapping (`modular`/`allinone`/`console`/`panelnode`), cross-checked variant-for-variant against `06.1-03-SUMMARY.md`
- Added `vibMotor`, `vibDriver`, `capTouch`, `buck12`, `buck5` rows to the component price table (all High price-confidence, matching `DEFAULTS`) plus a new **"New components — unresolved risks"** subsection recording: RD520PA's unsourced current rating (Low), IRF520's non-logic-level gate drive + mandatory flyback diode (Medium), and MPR121's High-confidence chip facts vs. Medium-confidence/precondition-unverified level-crossing application (container material — RESEARCH Assumption A4, risk-if-wrong High)
- Fixed the pin-budget model's pseudocode block, which had never been updated to show the `+1` fixed vibration-PWM term the shipped `pinsOf()` has charged since plan 06.1-01, and added prose for both new pin-budget terms (vibration +1, MPR121 +0 via I²C share)
- Added a new **"Power-rail model (SC-5)"** section: the 28BYJ-48 12V-winding rationale (60 mA vs 83 mA, 34.3 vs 29.4 mN·m, no trade-off), the single-24V-rail-plus-two-bucks topology, the full arithmetic (0.14 A / 0.25 A / 0.39 A / 9.4 W, no PSU wattage change required), the Medium-confidence caveat with its specific reasoning, and the rejected-alternatives rationale (linear regulator 9.5 W dissipation, DRV2605L mismatch)
- Added the D-17 temperature note (viscosity-first physics, ≈2–2.5%/°C vs ≈0.02–0.04%/°C, future software hook only) directly under the module schema table
- Added two new open questions with resolution procedures: storage container material (blocks MPR121 validation) and RD520PA's unsourced current rating (measure once ordered)

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix SPEC.md's stale counts and prices against the shipped tool** - `2871a92` (docs)
2. **Task 2: Document the schema, directions, new components, and the power-rail answer** - `14e399a` (docs)

## Files Created/Modified
- `tools/system-architecture-explorer/SPEC.md` - Consistency-pass fixes (variant count, component confidence split, PSU prices, shared-block total, PRICES_VERSION) plus four new sections (module schema, design directions, new components/risks, power-rail model) and two new open questions

## Decisions Made
- Recounted "components not stocked by bitbyg" as **Seven**, not the plan-suggested "the six unstocked parts plus carrier" phrasing — the real Low-confidence set is `rp2040`, `espscreen32`, `tmc2209`, `tmc5160`, `tmc5072`, `skr`, `carrier` (7 items), and `espscreen32` had never actually been listed in the old bullet text despite being Low-confidence and unstocked pre-phase. Recounted directly from the shipped `DEFAULTS` rather than trusting the plan's phrasing, per the plan's own explicit instruction ("if it differs, the file wins")
- Kept the five new component price-table rows in Task 2's commit (matching the plan's task split) rather than adding them in Task 1, even though Task 1 already touched the same table to remove `alignMot2`
- Extended the fix beyond the plan's literal line references where the same drift pattern recurred nearby: the "Live system diagram" section's Power-block description ("dual-rail lines") and the post-table PSU-derivation sentence both still claimed a stale dual-rail topology after the power-model table itself was fixed — updated both for internal consistency (Rule 1 — the file would otherwise still contradict itself two paragraphs apart)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Pin-budget pseudocode block missing the +1 vibration-PWM term**
- **Found during:** Task 2 (pin-budget model update)
- **Issue:** The `pinsOf(v)` pseudocode block in SPEC.md's "Pin-budget model" section never included the `+1` vibration-motor PWM pin, even though the shipped `pinsOf()` in `index.html` has charged it unconditionally since plan 06.1-01 (`used += 1;`). The plan's Task 2 acceptance criteria required the pin-budget section to state the `+1`/`0` deltas but didn't call out the stale pseudocode block by exact text.
- **Fix:** Added the `+ 1` line to the formula block with its comment, plus a new prose paragraph explaining both new pin-budget terms.
- **Files modified:** tools/system-architecture-explorer/SPEC.md
- **Verification:** Formula block now matches the shipped `pinsOf()` function line-for-line; `grep` confirms `+1` and the MPR121 zero-cost note are both present.
- **Committed in:** `14e399a` (Task 2 commit)

**2. [Rule 1 - Bug] "Live system diagram" and post-power-table prose still described the old dual-rail topology**
- **Found during:** Task 1 (power-model table fix)
- **Issue:** After fixing the Power/PSU model table's Rail cells, two other sentences elsewhere in the file (the "Live system diagram" section's Power-block description, and the sentence immediately after the power-model table) still said "dual-rail" / "12V/24V dual rail," which would have left the file self-contradictory two sections apart.
- **Fix:** Updated both to describe the single-24V-rail-plus-two-bucks topology, matching the corrected table.
- **Files modified:** tools/system-architecture-explorer/SPEC.md
- **Verification:** `grep -n "dual.rail"` in the file after the edit returns no remaining matches describing the power topology.
- **Committed in:** `2871a92` (Task 1 commit)

---

**Total deviations:** 2 auto-fixed (both Rule 1 — documentation-drift bugs directly in scope of this plan's D-18 consistency-pass mandate)
**Impact on plan:** Both fixes are the same class of problem this plan exists to solve, found by reading the live code rather than trusting the plan's line references. No scope creep — both stayed inside `tools/system-architecture-explorer/SPEC.md`.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required. This plan touched only a markdown documentation file.

## Confidence-count verification (as requested in the plan's `<output>` instruction)

Recounted directly from the shipped `tools/system-architecture-explorer/index.html` `DEFAULTS` object (26 keys total):

- **High: 17** — esp32, stm32, promini, nano, espscreen, drv8825, max485, mcp2515, ramps, screen, stepperNema, align28byj, vibMotor, vibDriver, capTouch, buck12, buck5
- **Medium: 2** — psu60, psu150
- **Low: 7** — rp2040, espscreen32, tmc2209, tmc5160, tmc5072, skr, carrier

**This matches the plan's stated expectation of 17-of-26 exactly — no discrepancy to flag.**

## Browser verification still pending (carried over from plans 01–03)

No browser automation was available in this session either — this plan only edited a markdown file with no runtime behavior, so there is nothing new to browser-verify from this plan specifically. The `.\serve.bat` + browser pass recommended in plans 01, 02, and 03's summaries (visual schema/gallery rendering, no-horizontal-scroll at various breakpoints, focus-ring/keyboard behavior, `.matrix-wide` breakout feel, WR-04 Firefox-reload seeding) remains outstanding and should be performed once before treating the whole phase as closed. This SPEC.md itself needs no browser check — it is human/GitHub-markdown-rendered only, never parsed by the tool at runtime.

## Next Phase Readiness

Plan 05 (decision records, landing page, README, CLAUDE.md) can now:
- Reference this SPEC.md as the accurate, canonical description of everything plans 01–04 shipped — every count, price, and confidence tag in it matches the live `index.html`
- Reuse the "17 of 26 / 2 Medium / 7 Low" confidence split and the "€149.65 shared block" figure if the landing page or README need a summary number
- Reuse the direction → variant-ID mapping table and the module-schema module list if either surface needs to describe the tool's new capabilities in brief

---
*Phase: 06.1-architecture-refinement-full-system-module-schema-design-dir*
*Completed: 2026-07-16*

## Self-Check: PASSED

- FOUND: tools/system-architecture-explorer/SPEC.md
- FOUND: .planning/phases/06.1-architecture-refinement-full-system-module-schema-design-dir/06.1-04-SUMMARY.md
- FOUND commit: 2871a92
- FOUND commit: 14e399a
