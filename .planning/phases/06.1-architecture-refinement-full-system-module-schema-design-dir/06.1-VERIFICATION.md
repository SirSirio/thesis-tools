---
phase: 06.1-architecture-refinement-full-system-module-schema-design-dir
verified: 2026-07-16T16:43:37Z
status: passed
score: 6/6 roadmap success criteria verified (28/28 plan-level truths verified)
overrides_applied: 0
gaps_resolved:
  - truth: "ARCHITECTURE.md describes the two-buck-converter rail topology instead of a 12 V + 24 V dual rail (Plan 06.1-05 truth 3; SC-6)"
    status: resolved
    resolved_at: 2026-07-16
    resolved_by: "69c3be9 — docs(06.1): resolve ARCHITECTURE.md self-contradictions (WR-05, closes SC-6)"
    reason: "The three WR-05 items this gap named were fixed after the verification pass, at the user's direction: :118 (was :115) now states the researched single-24V-PSU-plus-two-local-bucks topology and cites D-15; the inverted '~€2/driver premium for TMC2209' claim at :86 (was :84) is replaced with the sourced direction (€3.50 vs €6.87 — no premium); and :57-58 now names the tool as cost authority with SOLUTION-MATRIX.md demoted to the historical snapshot it declares itself to be (D-08)."
    reverified:
      - "grep for 'separate motor rail' across the three decision records → no matches"
      - "grep for '€2/driver premium' / '€2 more per driver' across SPEC.md, index.html and the three records → no matches"
      - "grep for unresolved 'TBD' across the three records → no matches"
      - "variant count reads 20 in SPEC.md (:59, :572); no 17/19 survivors anywhere"
      - "remaining SPI mentions are 'Resolved 2026-07-15 / confirmed SPI' statements, not stale confirm-this warnings"
deferred:
  - "POWER-AND-MOTORS.md (:64, :135, :291) calls ARCHITECTURE.md's rail spec 'dual-rail' — now loose terminology rather than a contradiction, since that file's own §6 diagram already draws the 24V→buck→12V / buck→5V topology ARCHITECTURE.md now describes. Out of SC-6 scope (not one of the three decision records) and already carried in deferred-items.md."
human_verification:
  - test: "Load tools/system-architecture-explorer/index.html via serve.bat (never Live Preview) and confirm no horizontal scroll appears at common viewport widths (desktop and the CLAUDE.md-mandated no-scroll constraint), including with the .matrix-wide break-out container and its own overflow-x:auto wrapper."
    expected: "Page body never scrolls horizontally; only the matrix's own bordered container may internally scroll if content exceeds calc(100vw - 64px)."
    why_human: "No browser automation available in this environment; CSS analysis (calc(100vw - 64px) sizing + inner overflow-x:auto) is consistent with the constraint but rendering/reflow can only be confirmed visually."
  - test: "Toggle 'Include shared block' to whole-system cost and move the #fPrice slider across its new €20-€400 range in both controller-only and whole-system views."
    expected: "Matrix never renders '0 of 20 variants shown' at the slider's default/max position; all 20 rows are reachable at max in both views."
    why_human: "Confirmed correct by Node-sandbox evaluation of the shipped constants (whole-system costs €207.61-€341.48, all ≤ €400 ceiling) but the actual DOM/event-listener behavior (label sync, re-render on toggle) needs a browser to fully confirm."
  - test: "Hover, click, and Tab/Enter/Space through the six module-schema boxes and the four direction-gallery cards."
    expected: "Hover previews the module's side note without becoming sticky; click/keyboard makes it sticky; re-clicking a direction card clears the matrix filter; focus rings are visible and follow a sensible tab order."
    why_human: "Interaction wiring exists in code (selectModule/showSchemaPrompt/wireSchemaInteractivity, setDirection/wireDirectionsInteractivity) but keyboard focus-visibility, hover-timing feel, and the 'liquid-glass' visual treatment (D-05 Claude's Discretion) are only assessable in a browser."
  - test: "Visually inspect the module-schema SVG's data-flow lines from Software & Electronics to Pump and to Nozzle."
    expected: "No data-flow line visually crosses through the Alignment module box (the design constraint stated in SPEC.md and the buildSchema() comment: spatial position must encode connectivity only)."
    why_human: "Geometrically confirmed as a real defect by computing the shipped LAYOUT/anchor coordinates (WR-06, still open/deferred) — both the Electronics->Pump and Electronics->Nozzle dashed lines cross the Alignment box's x-range (330-550) and y-range (250-370) — but final visual judgment on whether this reads as a misleading routing is a human call."
---

# Phase 06.1: Architecture Refinement — Full-System Module Schema & Design Directions Verification Report

**Phase Goal:** Refine the System Architecture Explorer from a pump-control cost matrix into a
whole-device architecture page: module schema (six modules, hardware payload, liquid/data flow),
a design-direction gallery, a Design-type matrix column, new bitbyg-sourced components, and a
consistency pass across the tool and its three decision records.

**Verified:** 2026-07-16T16:43:37Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Method

All numeric/logical claims were verified by extracting the shipped `DEFAULTS`, `COMP`,
`SHARED_BOM`, `VARIANTS`, `costOf()`, `pinsOf()`, `directionOf()`, `topoClassOf()`, and
`intBrainKey()` from `tools/system-architecture-explorer/index.html` and executing them in a
Node.js `vm` sandbox against the real shipped constants — not by trusting SUMMARY.md, PLAN.md, or
the prior REVIEW.md/REVIEW-FIX.md's stated values. Prose/documentation claims (SPEC.md,
ARCHITECTURE.md, PUMP-CONTROL-CONCEPTS.md, SOLUTION-MATRIX.md) were read directly and cross-checked
against the sandboxed numbers. No browser automation was available; visual/interaction claims are
routed to Human Verification below rather than assumed.

## Goal Achievement

### Observable Truths (Roadmap Success Criteria — the contract)

| # | Truth (SC) | Status | Evidence |
|---|---|---|---|
| SC-1 | Tool opens with a general architecture schema: all six modules, their electronics payload, and data vs liquid flow visually distinguished | ✓ VERIFIED (with a known open quality defect, see WR-06 below) | `MODULES` object (index.html:941-976) defines all six modules with payload chip arrays matching CONTEXT-SEED's inventory exactly (6× NEMA17, 2× 28BYJ-48+ULN2003+rack, vibration motor+IRF520+mass, 6×containers+MPR121, ILI9341+SD+LM75, ESP32-class brain+bus+drivers). `buildSchema()` draws liquid flow as solid red+arrowhead and data flow as dashed blue+arrowhead with a legend (index.html:1057-1075). Geometric check confirms two data-flow lines cross the Alignment box (WR-06, still open) — a genuine but non-blocking visual-clarity defect, not a failure of "distinguishable by flow type." |
| SC-2 | A design-direction section shows 2-4 visually distinct build directions as graphics (SVG), each mapping to rows in the matrix | ✓ VERIFIED | `DIRECTIONS` object defines exactly 4 directions (modular/allinone/console/panelnode) each with a `directionSketch()` SVG + name + copy (index.html:823-905). Node-sandbox evaluation of `directionOf()` against all 20 `VARIANTS` confirms a clean 2/2/8/8 partition with zero unclassified rows. |
| SC-3 | The variant matrix has a Design type column; sorting/filtering still works; price + functionality remain the primary comparison drivers | ✓ VERIFIED | `<th class="sortable" data-k="direction">Design type</th>` present (index.html:499); `directionPill(v)` renders a colour-coded chip per row (index.html:1323-1327); `sortKey==='direction'` branch added to the existing sort chain alongside cost/cx/at (index.html:1414-1419); `selectedDirection` filter composes with the pre-existing price/complexity/concurrency filters in `renderMatrix()` (index.html:1409-1412) without removing any of them. |
| SC-4 | New components are priced from bitbyg with confidence tags, following the existing COMP/DEFAULTS model | ✓ VERIFIED | `DEFAULTS.vibMotor/vibDriver/capTouch/buck12/buck5` (index.html:605-614) each carry `eur`, `confidence:'High'`, and a `source.url`+note pointing at a bitbyg listing, in the same shape as every pre-existing `DEFAULTS` entry. `renderComps()` iterates `for(const k in COMP)` generically, so the new rows render in the live price table with no special-casing needed. |
| SC-5 | The voltage/power questions are answered (sourced) and reflected in the power-rail model | ✓ VERIFIED | `align28byj` source note documents the 12V-vs-5V research answer (60 mA/34.3 mN·m at 12V vs 83 mA/29.4 mN·m at 5V, same price — index.html:603-604). The live diagram's Power block (index.html:1242-1261) draws a single 24V PSU rail stepped down by `buck12`/`buck5` — matches SPEC.md's "Power-rail model (SC-5)" section (SPEC.md:403-449) and the shipped `SHARED_BOM`/`costOf()` numbers exactly. |
| SC-6 | SPEC.md and the three decision records are internally consistent (variant count, resolved open questions, current prices) | ✓ **VERIFIED** (after `69c3be9`) | SPEC.md is internally consistent (verified: "20 candidate architectures" / "20 total" BOM header / 17H-2M-7L-of-26 confidence census all match the sandboxed constants exactly; the CR-02/CR-03 fixes for `ESPINT32-brain-i2c`'s pin count and cost comparison are confirmed correct against the live code). This criterion **initially failed** on `prototypes/System-Architecture/ARCHITECTURE.md`, which stated the researched two-buck topology at :50-55 while still carrying the retired "separate motor rail (12/24 V) and logic rail (5/3.3 V)" framing at :115, an inverted "~€2/driver premium for TMC2209" claim at :84 (the tool's own table shows TMC2209 is €3.37 *cheaper*), and a cost-authority pointer at :57-58 contradicting D-08. All three were fixed in `69c3be9` after the verification pass and re-checked: no "separate motor rail" or driver-premium survivors anywhere across SPEC.md, index.html, or the three records; no unresolved TBDs; variant count 20 throughout; remaining SPI mentions are "Resolved 2026-07-15 / confirmed SPI" statements rather than stale warnings. |

**Score:** 5/6 roadmap success criteria verified.

### Decision Compliance (D-01…D-18, from 06.1-CONTEXT.md)

| Decision | Status | Evidence |
|---|---|---|
| D-01 (schema at top, short intro) | ✓ VERIFIED | index.html:280-308 — `<header>` intro rewritten (3 sentences), `Part 00` label immediately follows. |
| D-02 (old intro replaced, not patched) | ✓ VERIFIED | Same block reads as a fresh funnel framing ("schema → directions → matrix"), no leftover pump-matrix-only language. |
| D-03 (interactive hover/click/keyboard reveal) | ✓ VERIFIED | `selectModule()`/`showSchemaPrompt()`/`wireSchemaInteractivity()` wire `mouseenter`/`mouseleave`/`click`/`keydown` (index.html:1107-1140). |
| D-04 (payload chips) | ✓ VERIFIED | `MODULES[key].payload` arrays rendered as SVG pill chips in `moduleBox()` (index.html:1019-1024). |
| D-05 (liquid+data flow, liquid barrier, tube-module removed) | ✓ VERIFIED (WR-06 open) | Barrier drawn with `liquidGlass`/`liquidGlow` filter (index.html:1043-1046); no "tube-holding" module in `MODULES`; two data-flow lines geometrically cross the Alignment box (confirmed by computing shipped LAYOUT coordinates) — open quality defect, not a missing feature. |
| D-06 (3-4 curated directions) | ✓ VERIFIED | Exactly 4: modular/allinone/console/panelnode. |
| D-07 (SVG sketch + name + 1-2 lines, sketch carries description) | ✓ VERIFIED | `directionSketch()` + `DIRECTIONS[key].copy` (1-2 sentences each). |
| D-08 (gallery between schema and matrix, click filters/highlights) | ✓ VERIFIED | DOM order confirms placement; `wireDirectionsInteractivity()` toggles `setDirection()`. |
| D-09 (vendor-independent ideation, bitbyg prices only) | ✓ VERIFIED (documentation-level) | Code comment at DIRECTIONS declares the ordering was followed; not independently re-derivable from code alone, but no bitbyg-catalogue-shaped constraint is visible in the four curated personalities. |
| D-10 (Design type column = direction names as chips, shared vocabulary) | ✓ VERIFIED | `directionPill(v)` reads `DIRECTIONS[directionOf(v)]` — single source of truth shared with the gallery. |
| D-11 (matrix may break out wider; page-level no-h-scroll still applies) | ⚠ code-consistent, needs human confirmation | `.matrix-wide{width:calc(100vw - 64px);max-width:1500px}` plus an inner `overflow-x:auto` wrapper (index.html:177, 495) — routed to Human Verification. |
| D-12 (direction filter sortable/filterable, shares state with gallery) | ✓ VERIFIED | `setDirection()` is the sole writer for both `#fDir` and gallery cards (index.html:679-694); `sortKey==='direction'` branch present. |
| D-13 (vibration motor + 2×28BYJ-48 fixed shared parts; TBD resolved) | ✓ VERIFIED | `SHARED_BOM` includes `vibMotor:1, align28byj:2`; no `alignMot2`/TBD string anywhere in index.html, SPEC.md, or the three decision records (grep confirmed zero matches). |
| D-14 (screen never double-billed for integrated variants) | ✓ VERIFIED | `costOf()`'s `if(k==='screen' && intBrainKey(v)) continue;` guard fires for both `espscreen` and `espscreen32` integrated boards (confirmed via `intBrainKey()` sandbox test on all three `ESPINT*` variants). |
| D-15 (28BYJ-48 voltage + step-down topology researched) | ✓ VERIFIED | 12V winding justified with sourced current/torque numbers; two-buck topology reflected in `SHARED_BOM`, the diagram Power block, SPEC.md, and — after `69c3be9` — consistently in ARCHITECTURE.md as well. |
| D-16 (vibration-motor drive scheme researched) | ✓ VERIFIED | IRF520 MOSFET + flyback diode + PWM chosen and documented, with the gate-drive risk flagged (SPEC.md:166-169) rather than presented as settled. |
| D-17 (temp-compensation note on UI module only, no modelling) | ✓ VERIFIED | `MODULES.ui.purpose` states the viscosity-vs-expansion note in one sentence; no cost/pin impact anywhere in `costOf()`/`pinsOf()`. |
| D-18 (consistency pass: variant count, stale SPI warning, stale PSU prices, PUMP-CONTROL-CONCEPTS preface, motor-#2 TBD) | ✓ VERIFIED (after `69c3be9`) | Variant count (20), stale SPI warning, stale PSU prices, and the PUMP-CONTROL-CONCEPTS.md preface were all confirmed fixed during the pass. The ARCHITECTURE.md power-rail self-contradiction — the one item of this consistency pass still outstanding at verification time — was fixed in `69c3be9` and re-checked. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `tools/system-architecture-explorer/index.html` | DEFAULTS/SHARED_BOM/pinsOf extensions, Part 00 schema, direction gallery, Design type column | ✓ VERIFIED | All present and functioning per sandbox evaluation; 1533 lines. |
| `tools/system-architecture-explorer/SPEC.md` | Corrected counts/prices + new sections for schema, directions, new components, power-rail | ✓ VERIFIED | 585 lines; all sandbox-checked figures match. |
| `prototypes/System-Architecture/ARCHITECTURE.md` | Resolved SPI question, resolved motor #2, corrected rail topology | ✓ VERIFIED (after `69c3be9`) | SPI and motor-#2 resolved during the phase; rail topology fully corrected in `69c3be9`, which also removed the inverted driver-premium claim and the stale cost-authority pointer. |
| `prototypes/System-Architecture/PUMP-CONTROL-CONCEPTS.md` | Dated prices-superseded preface | ✓ VERIFIED | Preface present at file top, historical numbers left intact. |
| `prototypes/System-Architecture/SOLUTION-MATRIX.md` | Reference-snapshot banner, D-08 pointer | ✓ VERIFIED | Present; IN-08's minor "(resolved D-13)" should read "(resolved D-15)" — Info-level, not blocking. |
| `index.html` (landing page), `README.md`, `CLAUDE.md` | Whole-architecture-page description | ✓ VERIFIED | All three updated consistently (checked directly). |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `DEFAULTS.vibMotor/vibDriver/capTouch/buck12/buck5` | `SHARED_BOM` | shared-block cost aggregation in `costOf()` | ✓ WIRED | `SHARED_BOM` includes all 5 keys; sandbox-computed sum = €149.65, matches every documented figure. |
| `DEFAULTS` price changes | `localStorage 'sae-prices-v'` | `PRICES_VERSION` cache-bust | ✓ WIRED | `PRICES_VERSION = '2026-07-16-06-1-whole-system'`; `loadPersisted()` compares against it and discards stale sets. |
| `MODULES` data | `buildSchema()` SVG output | template-string assembly | ✓ WIRED | Confirmed by direct code read; six `moduleBox()` calls, one per key. |
| schema module `<g>` groups | side-note panel | click/keydown/mouseenter → `selectModule()` | ✓ WIRED | Single-writer function confirmed. |
| `directionOf(v)` | `renderMatrix()` filter chain | `selectedDirection` predicate | ✓ WIRED | Confirmed appended to the pre-existing `.filter()` chain without removing price/cx/at filters. |
| gallery card click + `#fDir` select | `selectedDirection` | `setDirection()`, one writer, two callers | ✓ WIRED | Both `wireDirectionsInteractivity()` and the `#fDir` change listener call `setDirection()` exclusively. |
| `#fPrice` slider | `renderMatrix()` cost filter | `input` listener + `maxPrice` | ✓ WIRED | Ceiling raised to €400; sandbox-confirmed all 20 whole-system costs (€207.61-€341.48) now fit under it (CR-01 fix verified functionally correct, not just claimed). |
| `ARCHITECTURE.md` | `tools/system-architecture-explorer/index.html#system` and SPEC.md | cross-links | ✓ VERIFIED (after `69c3be9`) | Links present and correct, and after `69c3be9` the linked-to content and ARCHITECTURE.md's own prose now agree — the file no longer contradicts the page it points at. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| Matrix rows (`renderMatrix()`) | `VARIANTS` (20 entries) | Static in-file array, no fetch/API | N/A — static by design (offline-only tool, CLAUDE.md constraint) | ✓ FLOWING (static data is the correct pattern here) |
| Component price table (`renderComps()`) | `COMP` (live-editable copy of `DEFAULTS`) | `JSON.parse(JSON.stringify(DEFAULTS))`, mutated by user input, persisted to localStorage | Real, sandbox-verified starting values; user edits flow through `costOf()`/`pinsOf()`/diagram on every keystroke | ✓ FLOWING |
| Direction gallery + Design-type column | `DIRECTIONS`, `directionOf(v)` | Static classifier over real `VARIANTS` fields (`topoClassOf`, `intBrainKey`) | Sandbox-confirmed 2/2/8/8 partition, no hardcoded per-variant direction field | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Whole-system cost never exceeds the `#fPrice` ceiling | Node vm: `Math.max(...VARIANTS.map(v=>costOf(v)))` with `includeShared=true` | `341.48` (≤ `max="400"`) | ✓ PASS |
| Every variant resolves to exactly one direction | Node vm: `VARIANTS.forEach(v=>dist[directionOf(v)]++)` | `{console:8, panelnode:8, modular:2, allinone:2}` = 20 | ✓ PASS |
| `ESPINT32-brain-i2c` pin budget matches SPEC's corrected claim | Node vm: `pinsOf(ESPINT32-brain-i2c)` | `{used:3, avail:3, free:0, overrun:false}` | ✓ PASS |
| No stale `TBD`/`alignMot2` string in this phase's file scope | `grep -rn "TBD" <the 5 in-scope files>` | zero matches | ✓ PASS |
| `POWER-AND-MOTORS.md` (out-of-scope file) still has a `TBD` | `grep TBD prototypes/System-Architecture/POWER-AND-MOTORS.md` | `alignMot2 is still TBD` at line 219 | ℹ️ Known, already logged in `deferred-items.md` — out of this phase's declared file scope, not a new finding. |

### Probe Execution

SKIPPED — no `scripts/*/tests/probe-*.sh` files exist in this repository and none are declared in any 06.1 PLAN/SUMMARY. This is a static-site content phase, not a migration/tooling phase.

### Requirements Coverage

No formal REQ-IDs exist for this phase in `.planning/REQUIREMENTS.md` (confirmed pre-existing, logged gap — see `deferred-items.md`, "Requirement ID registration gap"). The trackable set is D-01…D-18 and SC-1…SC-6 from `06.1-CONTEXT.md`, covered in the Decision Compliance and Observable Truths tables above. No orphaned requirements beyond the already-logged registry gap.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `prototypes/System-Architecture/ARCHITECTURE.md` | 115 | Self-contradicted the two-buck rail topology stated at :50-55 | ✓ Resolved in `69c3be9` | Was: reader reaching "How it all connects" got the superseded 12V+24V dual-rail model. Now states the D-15 topology. |
| `prototypes/System-Architecture/ARCHITECTURE.md` | 84 | "~€2/driver premium for TMC2209" — inverted vs. the tool's own sourced prices | ⚠ Warning | Same claim already fixed in the tool's own theory prose (CR-04) and PUMP-CONTROL-CONCEPTS.md's superseded-prices preface, but not here. |
| `prototypes/System-Architecture/ARCHITECTURE.md` | 57-58 | "Component unit prices ... used for all cost math" points at SOLUTION-MATRIX.md, contradicting D-08 | ⚠ Warning | Minor authority confusion; SOLUTION-MATRIX.md itself correctly demotes to "reference snapshot." |
| `tools/system-architecture-explorer/index.html` | 1059-1071 | Two schema data-flow lines geometrically cross the Alignment module box | ⚠ Warning | Violates the diagram's own stated design constraint (spatial position encodes connectivity only); does not break functionality. |
| `tools/system-architecture-explorer/index.html` | 341-345 | Theory prose still calls the screen interface "ambiguous" / "not a variable," contradicted elsewhere on the same page | ⚠ Warning (pre-existing, not introduced this phase) | Cosmetic inconsistency, not a functional defect. |
| `tools/system-architecture-explorer/index.html` | 504-508 | Hand-copied €149.65 shared-block breakdown, not computed from `SHARED_BOM`/`COMP` | ⚠ Warning | Will silently go stale the next time a price is edited live; every other cost readout on the page updates, this one does not. |
| `prototypes/System-Architecture/SOLUTION-MATRIX.md` | 58 | "(resolved D-13)" should read "(resolved D-15)" | ℹ️ Info | Minor audit-trail ID error, not user-facing. |

No `TBD`/`FIXME`/`XXX` unresolved debt markers were found in any of this phase's declared file scope (index.html, SPEC.md, ARCHITECTURE.md, PUMP-CONTROL-CONCEPTS.md, SOLUTION-MATRIX.md). The one `TBD` found (`POWER-AND-MOTORS.md`) is in a file outside this phase's file scope and is already logged in `deferred-items.md`.

### Human Verification Required

See frontmatter `human_verification` for the structured list. Summary: (1) confirm no page-level horizontal scroll with the wider matrix container at real viewport widths, (2) confirm the `#fPrice` slider/whole-system toggle behaves correctly in the browser (sandbox-confirmed at the data level only), (3) confirm keyboard focus/hover feel and the liquid-glass visual treatment of the schema and gallery, (4) make a visual judgment call on whether the WR-06 data-flow-line crossing through the Alignment box actually misleads a reader.

## Gaps Summary

**Resolved — the phase now passes 6/6.** The verification pass found one concrete, code-verified gap: **`prototypes/System-Architecture/ARCHITECTURE.md` was internally self-contradictory on the power-rail topology** (stated the new two-buck answer at :50-55, then repeated the old 12V+24V dual-rail framing at :115), and separately carried an inverted TMC2209-cost claim (:84) and a stale cost-math authority pointer (:57-58) — failing SC-6's "internally consistent" requirement and Plan 06.1-05's own truth #3 on the same evidence.

All three were fixed in `69c3be9` (WR-05 from `06.1-REVIEW.md`, which the user's earlier critical-only fix scope had deliberately left open; the user elected to close it once verification showed it was the sole barrier to a clean pass). Re-checked afterwards: no "separate motor rail" framing, no driver-premium claim, and no unresolved TBDs survive anywhere across SPEC.md, `index.html`, or the three decision records; variant count reads 20 throughout; remaining SPI mentions are resolution statements, not stale warnings.

The 6 remaining Warnings and 8 Info findings from `06.1-REVIEW.md` stay open by design and do not bear on any success criterion — see `06.1-REVIEW-FIX.md` for the deferred list.

This is not a functional break — the tool itself (`index.html`, `SPEC.md`) is fully consistent and correctly reflects the researched answer everywhere it matters for SC-1 through SC-5. The gap is confined to one decision-record file that the user's own explicit scope decision (06.1-REVIEW-FIX.md: "the 4 Critical findings only... 7 Warnings... remain open") consciously left unfixed. Because that scope decision was made by the human (not silently skipped by an agent), this looks like a strong candidate for an explicit override rather than a plan-and-reopen cycle — see the suggested override block below. Absent that override, the fix is small and mechanical (three sentence-level edits already spelled out in `06.1-REVIEW.md`'s WR-05 finding).

**This looks intentional.** To accept this deviation, add to this file's frontmatter:

```yaml
overrides:
  - must_have: "ARCHITECTURE.md describes the two-buck-converter rail topology instead of a 12 V + 24 V dual rail"
    reason: "User explicitly scoped the code-review fix to Critical findings only (06.1-REVIEW-FIX.md); WR-05's ARCHITECTURE.md self-contradiction was consciously deferred, not missed."
    accepted_by: "Sirio"
    accepted_at: "2026-07-16T00:00:00Z"
```

---

_Verified: 2026-07-16T16:43:37Z_
_Verifier: Claude (gsd-verifier)_
