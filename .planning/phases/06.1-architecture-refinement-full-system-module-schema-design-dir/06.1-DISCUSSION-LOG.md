# Phase 6.1: Architecture Refinement - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-16
**Phase:** 06.1-architecture-refinement-full-system-module-schema-design-dir
**Areas discussed:** Module schema section, Design directions, Design-type column, New components & power

---

## Area selection

All four proposed gray areas were selected for discussion. Three matched pending todos were
reviewed and none folded (landing-page redesign = stale/already done 2026-07-13; new-tab
links = prototypes page; .agent-vs-.agents = unrelated).

---

## Module schema section

| Option | Description | Selected |
|--------|-------------|----------|
| New Part 00 at top | Schema + short paragraph before the comms-layer theory | ✓ (via free text) |
| Inside Part 01 theory | First theory card, no new section | |
| Next to the live diagram | General-vs-specific pairing at the bottom | |

**User's choice:** Free text — at the top, with a brief intro (short paragraph or even less)
about what the page contains. **Crucially: the current introduction becomes obsolete and
must be rewritten entirely** to reflect the refined page.

| Option | Description | Selected |
|--------|-------------|----------|
| Hover/click highlights | Highlight module + reveal payload in a side note | ✓ |
| Static SVG + legend | No interaction, prints well | |
| Full detail-on-click cards | Rich cards; risks duplicating matrix/BOM | |

| Option | Description | Selected |
|--------|-------------|----------|
| Modules + payload chips | Chips naming each module's electronics; hover adds purpose | ✓ |
| Clean boxes, payload on hover only | Tidier, baseline not at-a-glance | |
| Boxes + companion table | Most quotable, second artifact to sync | |

| Option | Description | Selected |
|--------|-------------|----------|
| Keep both flows + barrier | Liquid + data + barrier + enclosure, dark-theme adapted | ✓ (via free text) |
| Flows only | Drop barrier/enclosure | |
| Data-flow-centric | Liquid path faint | |

**Notes:** Keep both flows and the barrier, but representation is creative freedom —
"elegant and modern… I like liquid glass."

---

## Design directions

| Option | Description | Selected |
|--------|-------------|----------|
| Screen × topology | Curated combinations of both axes into device personalities | ✓ |
| Screen integration only | Exactly 2 directions | |
| Topology only | 4 directions from existing classes | |

| Option | Description | Selected |
|--------|-------------|----------|
| 3–4 curated | Opinionated set; leftovers tagged nearest | ✓ |
| Exactly 2 | Standalone vs integrated screen | |
| Let the matrix decide | One per occurring combination (5–6) | |

| Option | Description | Selected |
|--------|-------------|----------|
| SVG device sketch + few words | Physical impression, name, 1–2 lines | ✓ |
| Architecture mini-diagram | Abstract topology thumbnails | |
| Both sketch + mini-diagram | Richest, most work | |

| Option | Description | Selected |
|--------|-------------|----------|
| Between schema and matrix, click-to-filter | Funnel: system → shapes → costs; cards filter matrix | ✓ |
| Between schema and matrix, static | Cards visual only | |
| After the matrix | Interpretive endnote | |

---

## Design-type column

| Option | Description | Selected |
|--------|-------------|----------|
| Direction names | Colour-coded chips, one vocabulary with gallery | ✓ |
| Topology classes | Reuse internal fused/satellite/distributed/printer | |
| Two-part tag | "screen · topology" per row | |

| Option | Description | Selected |
|--------|-------------|----------|
| Chip + demote a column | Keep standard width, demote e.g. C link | |
| Merge into 'In plain words' | Prefix chips, no new column | |
| Colour-code the row | Border tint + legend | |

**User's choice:** Free text — the matrix table may be **a bit wider than the rest of the
page**; a breakout container is fine.

| Option | Description | Selected |
|--------|-------------|----------|
| Sortable + filter | Header sort + direction filter, shared state with gallery | ✓ |
| Filter only via gallery | Cards as sole filter entry | |
| Passive label only | Informative chip only | |

---

## New components & power

| Option | Description | Selected |
|--------|-------------|----------|
| Extend shared block | All new parts constant across variants | (partially, via free text) |
| New matrix axis | Sensing/vibration varies per row | |
| Info-only, not priced | Schema chips only | |

**User's choice:** Free text — vibration motor and 28BYJ-48 are fixed; **the screen is
fixed only for solutions that actually use it** (integrated-screen variants must not be
charged for the standalone screen). Additionally: **design ideas must be generated
independent of bitbyg** — ideate first from search/knowledge, then check bitbyg for
available/compatible components; the vendor must not limit idea generation.

| Option | Description | Selected |
|--------|-------------|----------|
| Research decides (28BYJ-48 voltage) | Compare windings, stock, rail trade-off | ✓ (via free text) |
| Lock 12 V now | Keep ARCHITECTURE.md assumption | |
| Lock 5 V now | One fewer rail | |

**Notes:** "I will need to downsize from 24 V anyways, so let research decide what is
better, also in terms of components" — the step-down/rail component choice is part of the
research question.

| Option | Description | Selected |
|--------|-------------|----------|
| Lock transistor+PWM, research verifies | Treat no-12V as settled, BOM gets the parts now | |
| Research decides fully | Also consider dedicated rail, resistor, driver module, motor swap | ✓ |

| Option | Description | Selected |
|--------|-------------|----------|
| Note as future hook (temp comp) | One-line note on UI module | ✓ (via free text) |
| Research feasibility now | Real research load | |
| Leave it out entirely | Records only | |

**Notes:** Future hook, wording should mention **volume compensation** (Sirio's bigger
worry: thermal volume expansion) and viscosity; final phrasing delegated to Claude.

---

## Claude's Discretion

- Schema + direction-sketch visual treatment (liquid-glass rendering, chips, flow lines).
- Direction names and final count (3 vs 4).
- Column layout details within the wider matrix container.
- Temp-compensation note wording.
- Grouping of new fixed parts in the component table; PRICES_VERSION bump mechanics.

## Deferred Ideas

- USB port / USB-stick protocol loading for the UI module (SD covers it for now).
- Temperature-compensated dispensing implementation (future software phase).
- Stale todo "Redesign landing page" flagged for closure (done 2026-07-13).
