---
phase: 06.1-architecture-refinement-full-system-module-schema-design-dir
plan: 05
subsystem: docs
tags: [consistency-pass, decision-records, landing-page, i18n, spec-sync]

# Dependency graph
requires:
  - phase: 06.1-01
    provides: Resolved align28byj (2x, EUR5.86 each), two-buck-converter power-rail topology, PRICES_VERSION bump
  - phase: 06.1-02
    provides: Part 00 #system module schema anchor in tools/system-architecture-explorer/index.html
  - phase: 06.1-03
    provides: Design-direction gallery, DIRECTIONS object, direction-to-variant mapping
  - phase: 06.1-04
    provides: SPEC.md resynced (20 variants, 17/26 confidence split, Power-rail model (SC-5) section, Open questions #1 SPI resolution)
provides:
  - "ARCHITECTURE.md's three stale claims resolved: SPI confirmed (2026-07-15), 2x 28BYJ-48+ULN2003 alignment motors (no TBD), single-24V-rail-plus-two-bucks topology (0.39A/9.4W)"
  - "PUMP-CONTROL-CONCEPTS.md dated prices-superseded preface (DRV8825 EUR9->EUR41 magnitude example), historical numbers left intact"
  - "SOLUTION-MATRIX.md's stale tool-authority claim removed and its own Alignment motor #2 (TBD) row resolved"
  - "Landing card (EN+IT), README row, and CLAUDE.md folder entry describe the whole-device architecture page instead of a cost/complexity matrix"
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Historical-record preservation pattern extended from PUMP-CONTROL-CONCEPTS.md to SOLUTION-MATRIX.md: fix stale claims/TBD wording, leave historical EUR estimates untouched"

key-files:
  created: []
  modified:
    - prototypes/System-Architecture/ARCHITECTURE.md
    - prototypes/System-Architecture/PUMP-CONTROL-CONCEPTS.md
    - prototypes/System-Architecture/SOLUTION-MATRIX.md
    - index.html
    - README.md
    - CLAUDE.md

key-decisions:
  - "SOLUTION-MATRIX.md's 'Component unit prices' intro no longer claims to drive the tool's cost math (it now maintains its own, richer 26-component model) — fixed as a Rule 1 bug beyond the plan's literal task text, because the plan's own acceptance criterion (a) explicitly requires the file not claim authority over the tool, and the pre-existing sentence violated it"
  - "SOLUTION-MATRIX.md's own 'Alignment motor #2 (TBD)' row (in its Component unit prices table, distinct from ARCHITECTURE.md's TBD) was resolved to '28BYJ-48 12V + ULN2003 (resolved D-13)' with its historical EUR6.0 estimate left untouched — required because the plan's own top-level <verification> step 1 greps the whole prototypes/System-Architecture/ directory for TBD and expects zero matches"
  - "Landing-card/README copy deliberately avoids stating a variant count anywhere (index.html included, even though only README's acceptance criteria explicitly banned it) — matches the D-18 lesson that hardcoded variant counts are exactly the kind of number that drifts"

requirements-completed: [D-13, D-15, D-18, SC-6]

# Metrics
duration: 25min
completed: 2026-07-16
---

# Phase 06.1 Plan 05: Consistency-Pass Decision Records + Landing Copy Refresh Summary

**The three System-Architecture decision records (ARCHITECTURE.md, PUMP-CONTROL-CONCEPTS.md, SOLUTION-MATRIX.md) no longer present resolved questions as open or TBD, and the landing card / README / CLAUDE.md now describe the tool as a whole-device architecture page instead of a cost/complexity matrix.**

## Performance

- **Duration:** ~25 min
- **Tasks:** 3
- **Files modified:** 6

## Accomplishments

- **ARCHITECTURE.md** — three surgical fixes inside `## Fixed components (decided)`:
  - Touchscreen bullet: removed the "⚠ confirm SPI vs parallel before wiring" warning, replaced with the resolved 2026-07-15 SPI confirmation (physically inspected, High confidence), pointing to SPEC.md's Open questions #1
  - Alignment motors bullet: "Motor #2 — TBD (placeholder: NEMA17 + DRV8825)" replaced with 2x 28BYJ-48 (12V) + ULN2003 at the shipped `align28byj` price (EUR5.86 each, bitbyg bundle 43.75 DKK), plus the D-15 torque/current rationale (60mA/34.3mN·m @ 12V vs 83mA/29.4mN·m @ 5V, no trade-off)
  - Rail assumption: "likely a 12V + 24V dual rail" replaced with the researched single-24V-PSU-plus-two-bucks topology (LM2596->12V, fixed 5V/5A buck), 0.39A/9.4W new draw, pointing to SPEC.md's Power-rail model (SC-5) section
  - The file's `#matrix` cross-link (line 14), Scope-note blockquote, open-questions list, and different-Arduino-per-pump verdict are all unchanged
- **PUMP-CONTROL-CONCEPTS.md** — added a dated blockquote preface (copying ARCHITECTURE.md's `> **Scope note.**` idiom) stating the ~EUR prices are pre-bitbyg-sourcing estimates (2026-07-02) retained as a historical record, linking the tool's `#matrix` Component unit prices table, and naming the DRV8825 magnitude shift (~EUR9 -> ~EUR41.22, `6 x EUR6.87`) as a concrete anchor. All seven concepts' price figures are byte-for-byte unchanged; the existing `#theory` link is untouched
- **SOLUTION-MATRIX.md** — verified against the plan's two specific checks and found two issues, both fixed:
  1. The "Component unit prices" intro claimed "all cost math below **and in the System Architecture Explorer** is built from these" — false since the tool now has its own independent 26-component model; reworded to a historical-snapshot framing pointing to the tool's live table as authoritative
  2. Its own price table (separate from ARCHITECTURE.md's TBD) had an "Alignment motor #2 (TBD)" row — resolved to "28BYJ-48 12V + ULN2003 (resolved D-13)", historical EUR6.0 estimate left as-is
  - The main matrix table (17 historical rows) was left completely unchanged, per the plan's explicit "do not expand the snapshot to 20 rows" instruction
- **Landing card, README, CLAUDE.md** — refreshed to describe a whole-device architecture page:
  - `index.html` tool card (EN markup + EN/IT `data-i18n` dictionaries, all three kept in sync): now leads with the six-module hardware schema and design-direction personas before the cost/complexity matrix; `href`/`class`/`--rd` on the `<a>` element untouched — diff confined to text content
  - `README.md` row: rewritten to cover the module schema, the design-direction gallery, and the Design type column, alongside the pre-existing matrix/BOM/converter/diagram features; no hardcoded variant count anywhere
  - `CLAUDE.md`'s `tools/system-architecture-explorer/` folder entry: no longer describes the tool as "control-electronics cost/complexity matrix" alone

## Task Commits

Each task was committed atomically:

1. **Task 1: ARCHITECTURE.md — resolve the SPI warning, motor #2, and the rail topology** - `87d0fd7` (docs)
2. **Task 2: PUMP-CONTROL-CONCEPTS.md preface and SOLUTION-MATRIX.md wording check** - `ce75c8a` (docs)
3. **Task 3: Landing card, README row, and CLAUDE.md description refresh** - `20f4282` (docs)

## Files Created/Modified
- `prototypes/System-Architecture/ARCHITECTURE.md` - SPI resolution, resolved alignment motors, two-buck-converter rail topology
- `prototypes/System-Architecture/PUMP-CONTROL-CONCEPTS.md` - Dated prices-superseded blockquote preface (historical numbers untouched)
- `prototypes/System-Architecture/SOLUTION-MATRIX.md` - Removed stale tool-authority claim in the price-table intro; resolved its own Alignment motor #2 (TBD) row
- `index.html` - Landing card copy refresh (EN markup + EN/IT `data-i18n` dictionary entries)
- `README.md` - System Architecture Explorer row rewritten to cover the schema and directions
- `CLAUDE.md` - `tools/system-architecture-explorer/` folder-structure entry description updated

## Decisions Made
- Fixed SOLUTION-MATRIX.md's stale tool-authority claim beyond the plan's literal task text (Rule 1 bug), because the plan's own acceptance criterion (a) required it not claim currency/authority over the tool, and the pre-existing sentence directly violated that
- Resolved SOLUTION-MATRIX.md's own "Alignment motor #2 (TBD)" price-table row (distinct from the one already fixed in ARCHITECTURE.md), because the plan's own top-level `<verification>` step 1 greps the whole `prototypes/System-Architecture/` directory for the literal string `TBD` and expects zero matches — leaving it would have failed the plan's own verification
- Left SOLUTION-MATRIX.md's main 17-row matrix table and its EUR estimates completely untouched, per the plan's explicit "do not expand the snapshot to 20 rows... it is a dated reference view" instruction
- Avoided stating a variant count in the `index.html` landing card too (not just README, which explicitly required it) — same drift lesson D-18 exists to fix, applied preemptively to a second surface

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] SOLUTION-MATRIX.md claimed authority over the tool's cost math**
- **Found during:** Task 2 (SOLUTION-MATRIX.md wording check)
- **Issue:** The "Component unit prices" section opened with "all cost math below **and in the System Architecture Explorer** is built from these" — no longer true; the tool's `DEFAULTS` object has grown to 26 independently-priced components (5 new since 06.1-01) that have no corresponding row in this file's table.
- **Fix:** Reworded the intro to a historical-snapshot framing that points to the tool's live Component unit prices table as the current, authoritative source, without touching the file's own historical price figures.
- **Files modified:** prototypes/System-Architecture/SOLUTION-MATRIX.md
- **Verification:** `grep -n "is built from these"` shows the sentence no longer extends past "the cost math below."
- **Committed in:** `ce75c8a` (Task 2 commit)

**2. [Rule 1 - Bug] SOLUTION-MATRIX.md's own "Alignment motor #2 (TBD)" row**
- **Found during:** Task 2, running the plan's own top-level verification grep across the whole `prototypes/System-Architecture/` directory
- **Issue:** SOLUTION-MATRIX.md's "Component unit prices" table (separate from the bullet already fixed in ARCHITECTURE.md) still had a literal `TBD` in an "Alignment motor #2 (TBD)" row — the same D-13-resolved placeholder, in a second location the plan's task-level action text didn't call out by exact line reference.
- **Fix:** Relabeled the row "28BYJ-48 12V + ULN2003 (resolved D-13)", leaving its historical EUR6.0 estimate unchanged (consistent with the file's historical-snapshot treatment elsewhere).
- **Files modified:** prototypes/System-Architecture/SOLUTION-MATRIX.md
- **Verification:** `grep -c "TBD" prototypes/System-Architecture/SOLUTION-MATRIX.md` returns 0.
- **Committed in:** `ce75c8a` (Task 2 commit)

---

**Total deviations:** 2 auto-fixed (both Rule 1 — the same class of documentation-drift bug this plan exists to fix, found by running the plan's own verification greps rather than trusting the task-level line references)
**Impact on plan:** Both fixes stayed inside `prototypes/System-Architecture/SOLUTION-MATRIX.md`, the file already in this plan's file scope. No scope creep beyond the plan's own stated files.

## Out-of-Scope Discovery (logged, not fixed)

While running the plan's directory-wide `TBD` grep, a third match was found: `prototypes/System-Architecture/POWER-AND-MOTORS.md:219` ("`alignMot2` is still TBD"). This file is **not** in this plan's `files_modified` list or file scope (`ARCHITECTURE.md`/`PUMP-CONTROL-CONCEPTS.md`/`SOLUTION-MATRIX.md`/`index.html`/`README.md`/`CLAUDE.md` only) and was pre-existing before this phase (committed in `009818b`). Per the executor's scope-boundary rule, this was **not** fixed — it is logged in `.planning/phases/06.1-architecture-refinement-full-system-module-schema-design-dir/deferred-items.md` for a future consistency pass.

## Issues Encountered
None blocking. See "Out-of-Scope Discovery" above for the one item deliberately left unfixed.

## User Setup Required
None - no external service configuration required. This plan touched only markdown documentation and static landing-page text content.

## Browser verification still pending (carried over from plans 01–03)

No browser automation was available in this session. This plan's only browser-relevant change is the `index.html` landing card's text content (no markup/CSS change). Structural verification confirmed: the diff is confined to the `<p class="card-desc">` text and its two `data-i18n` dictionary values; `href`, `class`, and `--rd` on the `<a class="tool-card">` element are byte-for-byte unchanged; no remaining stale Italian string describing the tool as only a matrix.

**A human `.\serve.bat` + browser pass is still recommended** before closing the phase, covering:
1. This plan's card: renders correctly, hovers, navigates to the tool, in both EN and IT
2. The full backlog of visual checks carried over from plans 01–03 (module schema/gallery visual layout, no-horizontal-scroll at multiple breakpoints, focus-ring/keyboard behavior, `.matrix-wide` breakout feel, WR-04 Firefox-reload seeding) — none of this session's changes affect those, but they remain unverified

## Next Phase Readiness

This is the last plan in Phase 06.1. All `files_modified` scoped to this plan are complete:
- The three canonical decision records (ARCHITECTURE.md, PUMP-CONTROL-CONCEPTS.md, SOLUTION-MATRIX.md) are internally consistent with each other, with SPEC.md, and with the shipped tool — no resolved question presented as open, no TBD for a decided part (within this plan's file scope), no superseded rail topology
- Landing card, README, and CLAUDE.md all describe the whole-device architecture page shipped by plans 01–04
- One deferred item remains for a future pass: `POWER-AND-MOTORS.md`'s own stale `TBD` reference (out of this plan's file scope)
- The phase's `<human-check>` still needs Sirio's confirmation that the landing-card/README one-liners describe the tool the way he'd want it cited, plus the accumulated browser-verification backlog from plans 01–03

---
*Phase: 06.1-architecture-refinement-full-system-module-schema-design-dir*
*Completed: 2026-07-16*

## Self-Check: PASSED

- FOUND: prototypes/System-Architecture/ARCHITECTURE.md
- FOUND: prototypes/System-Architecture/PUMP-CONTROL-CONCEPTS.md
- FOUND: prototypes/System-Architecture/SOLUTION-MATRIX.md
- FOUND: index.html
- FOUND: README.md
- FOUND: CLAUDE.md
- FOUND: .planning/phases/06.1-architecture-refinement-full-system-module-schema-design-dir/06.1-05-SUMMARY.md
- FOUND: .planning/phases/06.1-architecture-refinement-full-system-module-schema-design-dir/deferred-items.md
- FOUND commit: 87d0fd7
- FOUND commit: ce75c8a
- FOUND commit: 20f4282
