---
phase: 06.1-architecture-refinement-full-system-module-schema-design-dir
plan: 01
subsystem: ui
tags: [bom, pricing, svg-diagram, bitbyg, esp32, power-rail]

# Dependency graph
requires:
  - phase: 06-system-architecture-explorer
    provides: DEFAULTS/COMP/SHARED_BOM cost model, pinsOf() pin-budget model, buildDiagram() SVG renderer, sourceCellContent() XSS-safe rendering
provides:
  - Five new bitbyg-sourced DEFAULTS components (vibMotor, vibDriver, capTouch, buck12, buck5) with confidence tags and risk-bearing source notes
  - Resolved alignment-motor-#2 placeholder (deleted alignMot2, align28byj qty bumped to 2 in SHARED_BOM)
  - pinsOf() charges the fixed vibration-motor PWM pin; documents MPR121's zero-pin-cost I²C share with LM75
  - Diagram Power block redrawn as a single 24V rail stepped down by two labelled buck converters (12V alignment, 5V logic), replacing the stale "12V+24V dual rail" framing
  - Recomputed shared-block total (≈€149.65) and stale-string fixes (motor #2, €72) in the tool's own UI copy
affects: [06.1-02, 06.1-03, 06.1-04, 06.1-05]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "New DEFAULTS entries follow the exact align28byj key shape (label/role/eur/shared/confidence/source) and render automatically through the existing for(const k in COMP) loop — no new rendering code"
    - "Risk caveats (unsourced current, non-logic-level MOSFET, non-conductive-container requirement) are embedded directly in source.note strings rather than a separate risk field"
    - "PRICES_VERSION bump is the mandatory companion to any DEFAULTS price-set change (Phase-6 D-06 mechanism)"

key-files:
  created: []
  modified:
    - tools/system-architecture-explorer/index.html

key-decisions:
  - "align28byj role changed from 'alignment motor #1' to 'alignment motor ×2' and its source note now records the 12V winding as the researched choice (D-15), rather than adding a second differently-priced key"
  - "The two new buck converters (buck12, buck5) were grouped into SHARED_BOM alongside the vibration motor and 2x 28BYJ-48, per RESEARCH's recommendation (Claude's Discretion, not a locked CONTEXT.md decision)"
  - "Power block's three dashed 'feeds into Layer C' connector lines are decorative routing indicators (all terminate at the same driverY-14 row), not literal point-to-point wiring diagrams — consistent with the existing diagram's abstraction level"

requirements-completed: [D-13, D-14, D-15, D-16, D-18, SC-4, SC-5]

# Metrics
duration: 20min
completed: 2026-07-16
---

# Phase 06.1 Plan 01: New whole-device BOM components + power-rail topology Summary

**Five bitbyg-sourced components (vibration motor, MOSFET driver, MPR121 capacitive controller, two buck converters) added to the System Architecture Explorer's cost model; the alignment-motor-#2 placeholder is resolved to 2× 28BYJ-48; the diagram's power block now shows the researched single-24V-rail-plus-two-bucks topology instead of a stale dual-rail claim.**

## Performance

- **Duration:** ~20 min
- **Tasks:** 3
- **Files modified:** 1

## Accomplishments
- `DEFAULTS` grew from 22 to 26 keys: added `vibMotor`, `vibDriver`, `capTouch`, `buck12`, `buck5` (all High confidence on price, bitbyg-sourced, with the RESEARCH-mandated electrical-risk caveats embedded in their source notes); deleted the `alignMot2` TBD placeholder entirely
- `SHARED_BOM` rebuilt: `align28byj` qty 1→2 (D-13's resolved second alignment motor), `drv8825:1` removed (was `alignMot2`'s assumed driver, now redundant since 28BYJ-48 bundles its own ULN2003), five new fixed components added
- `pinsOf()` now charges a fixed +1 PWM pin for the vibration-motor MOSFET drive (constant across every variant) and documents that the MPR121 rides the LM75's existing I²C attachment at zero additional pin cost
- Verified (via direct Node evaluation of the live `pinsOf()`/`VARIANTS` code, not just visual inspection) that no variant flips from fitting to OVERRUN as a result of the new pin charge — `T9-fused-i2c` and `ESPINT32-brain-i2c` both land at exactly `0 free`, matching the plan's stated expectation
- Both D-14 screen double-count guard sites (`costOf()`, `bomHtml()`) confirmed intact and unaffected by the `SHARED_BOM` rewrite (2 matches for the guard condition, as required)
- Diagram Power block reworked to draw three labelled rails (24V, 12V via LM2596 buck, 5V via a synchronous buck) off a single PSU output, with a `+0.39 A / 9.4 W` new-draw estimate caption — replacing the stale "12V+24V dual rail" framing everywhere it appeared (diagram code comment, dashed drop-line label, part-summary prose)
- Alignment node box and note-card copy renamed from `28BYJ-48 + motor #2` to `2× 28BYJ-48 + ULN2003`
- Shared-block footnote recomputed from the live `DEFAULTS` values to **≈€149.65** — matches RESEARCH's independently-computed figure exactly, no discrepancy to flag

## Task Commits

Each task was committed atomically:

1. **Task 1: New DEFAULTS components, rebuilt SHARED_BOM, PRICES_VERSION bump** - `3743e98` (feat)
2. **Task 2: pinsOf() extension and D-14 double-count guard verification** - `6b17f33` (feat)
3. **Task 3: Power-rail topology in the diagram and the stale in-tool prose** - `f224fcf` (docs)

## Files Created/Modified
- `tools/system-architecture-explorer/index.html` - Five new DEFAULTS components, rebuilt SHARED_BOM, bumped PRICES_VERSION, extended pinsOf(), rewritten diagram Power block, alignment-node/footnote/part-summary prose fixes

## Decisions Made
- Recomputed shared-block total (€149.65) confirmed to match RESEARCH's figure with no discrepancy — no adjustment needed to the plan's stated number
- Power block's three dashed connector lines all terminate at the same `driverY-14` anchor row (matching the existing decorative-indicator style of the pre-existing two lines) rather than routing literally to each destination box, since the diagram's established abstraction level (Layer A/B/C boxes + a schematic Power block below) does not draw literal point-to-point wiring elsewhere either
- Used `acc2` (the existing red accent colour) as the "third distinguishable colour" for the 5V rail, per the plan's instruction to reuse the existing 5-colour palette rather than introduce a new hex literal

## Deviations from Plan

None - plan executed exactly as written. All acceptance criteria verified via grep and a direct Node.js evaluation of the live script block (extracting `DEFAULTS`, `SHARED_BOM`, `pinsOf()`, `buildDiagram()`, `VARIANTS` from the file and executing them in a sandboxed VM context) rather than relying solely on static inspection, since no browser automation tool was available in this environment. This substitutes for, but does not fully replace, the plan's "Browser check" acceptance criteria — no console/DOM-level verification (e.g. visual rendering, click interactions, localStorage round-trip in an actual browser) was performed. A human `.\serve.bat` + browser pass is recommended before treating the browser-specific acceptance criteria (localStorage stale-price discard, no-horizontal-scroll at 1440/929px, click-to-redraw) as fully confirmed, though the underlying logic was verified to be correct.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness

Plan 04 (SPEC.md sync) needs exactly the three figures this plan was asked to record:
- **Final DEFAULTS key count:** 26
- **Confidence split:** High 17, Medium 2, Low 7
- **Recomputed shared-block total:** €149.65 (no discrepancy from RESEARCH's figure)

Plans 02, 03, and 05 (module schema, direction gallery, remaining consistency-pass docs) can now reference real hardware in the tool's data model — `vibMotor`, `vibDriver`, `capTouch`, `buck12`, `buck5`, and the resolved 2× `align28byj` all exist with sourced prices and confidence tags.

**Recommended before treating this plan as fully closed:** run `.\serve.bat` and open `http://localhost:7331/tools/system-architecture-explorer/index.html` to confirm the browser-specific acceptance criteria (visual rendering of the three rails, no horizontal scroll at 1440px/929px, localStorage stale-price discard behavior, click-through row redraw) — the underlying JS logic has been verified correct via direct code execution, but true browser/DOM rendering was not observed in this session.

---
*Phase: 06.1-architecture-refinement-full-system-module-schema-design-dir*
*Completed: 2026-07-16*

## Self-Check: PASSED

- FOUND: tools/system-architecture-explorer/index.html
- FOUND: .planning/phases/06.1-architecture-refinement-full-system-module-schema-design-dir/06.1-01-SUMMARY.md
- FOUND commit: 3743e98
- FOUND commit: 6b17f33
- FOUND commit: f224fcf
