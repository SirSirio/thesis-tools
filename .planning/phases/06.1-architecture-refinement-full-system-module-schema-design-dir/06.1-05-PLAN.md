---
phase: 06.1-architecture-refinement-full-system-module-schema-design-dir
plan: 05
type: execute
wave: 4
depends_on: ["06.1-03"]
files_modified:
  - prototypes/System-Architecture/ARCHITECTURE.md
  - prototypes/System-Architecture/PUMP-CONTROL-CONCEPTS.md
  - prototypes/System-Architecture/SOLUTION-MATRIX.md
  - index.html
  - README.md
  - CLAUDE.md
autonomous: true
requirements: [D-13, D-15, D-18, SC-6]

must_haves:
  truths:
    - "ARCHITECTURE.md no longer tells the reader to confirm SPI vs parallel before wiring — that question was resolved 2026-07-15 (D-18, SC-6)"
    - "ARCHITECTURE.md names the alignment module's second motor as a 28BYJ-48 + ULN2003, with no TBD placeholder anywhere (D-13, D-18)"
    - "ARCHITECTURE.md describes the two-buck-converter rail topology instead of a 12 V + 24 V dual rail (D-15, D-18)"
    - "PUMP-CONTROL-CONCEPTS.md opens with a dated preface saying its prices are superseded by the tool, with its historical numbers left intact (D-18)"
    - "The landing-page card and README row describe a whole-architecture page, not just a matrix"
  artifacts:
    - path: "prototypes/System-Architecture/ARCHITECTURE.md"
      provides: "Resolved SPI question, resolved motor #2, corrected rail topology"
      contains: "28BYJ-48"
    - path: "prototypes/System-Architecture/PUMP-CONTROL-CONCEPTS.md"
      provides: "Dated prices-superseded blockquote preface"
      contains: "superseded"
  key_links:
    - from: "prototypes/System-Architecture/ARCHITECTURE.md"
      to: "tools/system-architecture-explorer/index.html#system and SPEC.md"
      via: "cross-links to the tool's schema anchor and SPEC's power-rail section"
      pattern: "system-architecture-explorer"
---

<objective>
Finish the D-18 consistency pass across the three decision records, and refresh the tool's
one-line description now that it is a whole-architecture page rather than a cost matrix.

Purpose: `prototypes/System-Architecture/` holds the design documentation the thesis cites. It
currently tells a reader to confirm a question that was answered on 2026-07-15, to treat the
alignment module's second motor as unchosen when it is now specified, and to expect a dual-rail
power design that research replaced. Meanwhile the landing card and README still sell a matrix.

Output: three corrected decision records, plus a refreshed landing card, README row, and
CLAUDE.md folder-structure entry.
</objective>

<execution_context>
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/workflows/execute-plan.md
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06.1-architecture-refinement-full-system-module-schema-design-dir/06.1-CONTEXT.md
@.planning/phases/06.1-architecture-refinement-full-system-module-schema-design-dir/06.1-RESEARCH.md
@.planning/phases/06.1-architecture-refinement-full-system-module-schema-design-dir/06.1-PATTERNS.md
@.planning/phases/06.1-architecture-refinement-full-system-module-schema-design-dir/06.1-01-SUMMARY.md
@.planning/phases/06.1-architecture-refinement-full-system-module-schema-design-dir/06.1-03-SUMMARY.md
@prototypes/System-Architecture/ARCHITECTURE.md
@prototypes/System-Architecture/PUMP-CONTROL-CONCEPTS.md
@prototypes/System-Architecture/SOLUTION-MATRIX.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ARCHITECTURE.md — resolve the SPI warning, motor #2, and the rail topology</name>
  <files>prototypes/System-Architecture/ARCHITECTURE.md</files>
  <read_first>
    - `prototypes/System-Architecture/ARCHITECTURE.md` — the whole file; specifically the `## Fixed components (decided)` section, lines 24–40.
    - `.planning/phases/06.1-architecture-refinement-full-system-module-schema-design-dir/06.1-RESEARCH.md` — Ground-Truth Findings rows 6, 7, 8 (the exact stale strings) and Q1/Q2 (the sourced replacements).
    - `tools/system-architecture-explorer/SPEC.md` — for the anchor/section names to point at (Open Question 1's SPI resolution; the new power-rail section).
    - `tools/system-architecture-explorer/index.html` — to confirm the `#system` anchor plan 02 landed exists before linking to it.
  </read_first>
  <action>
Three fixes in `## Fixed components (decided)`. Keep the file's existing voice and structure — these
are surgical corrections, not a rewrite. The document's fixed-components list, open questions, and
the different-Arduino-per-pump verdict all stay.

1. **Touchscreen bullet (lines ~26–30).** Remove the stale warning:
   `⚠ Listed as *SPI*, but the quoted Arduino pin map (A0–A3, D4–D13) looks like the **8-bit
   parallel Uno-shield** variant — **confirm SPI vs parallel before wiring** (changes brain pin
   budget).` This was **resolved on 2026-07-15**: the owned board was physically inspected
   (silkscreen/headers) and confirmed **SPI**, High confidence. Replace with a statement of the
   resolution — SPI confirmed on the owned board, 8 pins — and point to SPEC.md's Open Question 1
   resolution for the detail. Also fix the trailing `**Layer A = SPI (or 8-bit parallel) to the
   brain.**` — Layer A is SPI; 8-bit parallel survives in the tool only as a selectable
   counterfactual, and should be described that way if mentioned at all.
2. **Alignment motors bullet (line ~36).** Replace `**Motor #2 — TBD** (placeholder: NEMA17 +
   DRV8825 assumed for costing).` with the resolved answer (D-13): the alignment module uses
   **2× 28BYJ-48 + ULN2003**, both motors identical, no NEMA17 and no DRV8825 involved. Update the
   surrounding bullet so the `(≥2)` framing and the `~€2.5 incl. driver` figure are consistent with
   the tool's shipped `align28byj` price (**€5.86 each**, bitbyg bundle: 25.00 DKK motor + 18.75 DKK
   ULN2003 board = 43.75 DKK) — the `~€2.5` figure is a stale pre-bitbyg estimate. Also record
   here the **12 V variant choice** (D-15) as a researched decision rather than an inherited
   assumption: ~60 mA/phase vs ~83 mA/phase and ≥34.3 mN·m vs ≥29.4 mN·m for the 5 V winding — less
   current and more torque at the same price.
3. **Rail assumption (lines ~39–40).** Replace `Note: 28BYJ-48 is 12 V vs 24 V pump steppers →
   likely a **12 V + 24 V dual rail** on common ground.` The word "likely" marked it as an
   assumption, and research resolved it: there is **one 24 V PSU output**, with everything below
   24 V generated locally by **two buck converters** — an LM2596 trimmed to 12 V for the alignment
   motors, and a fixed 5 V/5 A synchronous buck for logic. Total new draw ≈0.39 A / 9.4 W on the
   24 V rail; no PSU wattage change needed. Point to SPEC.md's new power-rail section for the
   arithmetic rather than restating it in full here.

**Cross-links.** The file's existing tool links point at `#matrix` (line 14) — those stay valid and
must not be repointed. Where you now reference the whole-system view (the module schema), link the
new `#system` anchor plan 02 added; where you reference the power-rail detail, link SPEC.md.
Verify the `#system` anchor actually exists in the shipped `index.html` before linking to it.
  </action>
  <verify>
    <automated>grep -n "confirm SPI vs parallel\|Motor #2 — TBD\|12 V + 24 V dual rail" prototypes/System-Architecture/ARCHITECTURE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "confirm SPI vs parallel\|Motor #2 — TBD\|12 V + 24 V dual rail\|TBD" prototypes/System-Architecture/ARCHITECTURE.md` returns `0`.
    - The touchscreen bullet contains `SPI` stated as confirmed, plus the resolution date `2026-07-15`, and no `⚠` warning telling the reader to confirm it.
    - The alignment bullet contains `2× 28BYJ-48` (or `28BYJ-48 ×2`) and `ULN2003`, and contains neither `NEMA17` nor `DRV8825` in reference to the alignment motors.
    - The alignment bullet no longer contains `~€2.5`; it quotes a figure consistent with the shipped `align28byj` price in `index.html`.
    - The file contains `LM2596` (or `buck`) and describes a single 24 V supply with local step-down; it contains `0.39 A` or a pointer to SPEC.md's power-rail section.
    - The file still contains its `## Fixed components (decided)` heading, its `> **Scope note.**` blockquote, its open-questions content, and its existing `#matrix` tool link (line ~14 unchanged).
    - Any `#system` link added resolves: `grep -c 'id="system"' tools/system-architecture-explorer/index.html` returns ≥1.
  </acceptance_criteria>
  <done>ARCHITECTURE.md's three stale claims are replaced with the resolved, sourced answers, its existing structure and #matrix cross-link intact.</done>
</task>

<task type="auto">
  <name>Task 2: PUMP-CONTROL-CONCEPTS.md preface and SOLUTION-MATRIX.md wording check</name>
  <files>prototypes/System-Architecture/PUMP-CONTROL-CONCEPTS.md, prototypes/System-Architecture/SOLUTION-MATRIX.md</files>
  <read_first>
    - `prototypes/System-Architecture/PUMP-CONTROL-CONCEPTS.md` — the whole file, especially its top-of-file area and its per-concept price figures.
    - `prototypes/System-Architecture/ARCHITECTURE.md` line 19 — the `> **Scope note.** …` blockquote idiom to copy.
    - `prototypes/System-Architecture/SOLUTION-MATRIX.md` — its variant-count wording and its preface.
    - `.planning/phases/06.1-architecture-refinement-full-system-module-schema-design-dir/06.1-RESEARCH.md` — Ground-Truth Findings row 10, and the variant-count section confirming SOLUTION-MATRIX's 17-row snapshot is correct as-is.
  </read_first>
  <action>
**PUMP-CONTROL-CONCEPTS.md — add a preface, do not rewrite the numbers.** D-18 is explicit on this:
the file is a **historical record** of the 7-concept menu, and its pre-bitbyg price estimates (e.g.
6× DRV8825 = €9, versus the sourced €41.22) are part of that record. Rewriting them would destroy
the audit trail of how the cost picture changed once real vendor prices landed.

Add a dated blockquote preface at the top of the file, copying the `> **Scope note.**` idiom from
`ARCHITECTURE.md` line 19. It must say: prices below are **pre-bitbyg-sourcing estimates**, retained
as a historical record; current figures live in the tool's Component unit prices table; and give the
date. Link the tool's `#matrix` anchor (the file's existing `#theory` link at line ~25 stays as-is —
it is a reasoning pointer, not a price pointer). Make the magnitude concrete so a reader cannot skim
past it: the 6× DRV8825 line alone moved from ~€9 to ~€41 once sourced, so the concept costs in this
file understate reality substantially.

**SOLUTION-MATRIX.md — verify, expect no change.** RESEARCH's ground-truth read checked this file
and found **no fix needed**: its table is a 17-row historical snapshot taken before the
integrated-screen variants existed, and `SPEC.md` line ~391 already correctly describes it as "the
original static matrix (17 rows)". Confirm that (a) the file does not claim to be current or
authoritative over the tool, and (b) it does not state a variant count that contradicts the tool's
20. If both hold, change nothing and record "verified, no change" in the SUMMARY. **If** it does
claim currency or a wrong count, fix only that — do not expand the snapshot to 20 rows; it is a
dated reference view, and the tool is authoritative.
  </action>
  <verify>
    <automated>grep -n "superseded\|pre-bitbyg" prototypes/System-Architecture/PUMP-CONTROL-CONCEPTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `PUMP-CONTROL-CONCEPTS.md` begins (within its first ~30 lines, before the first concept) with a `>` blockquote containing `superseded` (or `pre-bitbyg-sourcing`), a date, and a link to the tool.
    - The preface names the DRV8825 magnitude shift (`€9` → `€41` or equivalent).
    - The file's existing per-concept price numbers are **unchanged**: `git diff prototypes/System-Architecture/PUMP-CONTROL-CONCEPTS.md` shows only added preface lines, with no modified or deleted price figures.
    - The file's existing `#theory` link is unchanged.
    - `SOLUTION-MATRIX.md` states no variant count that contradicts the tool's 20, and does not claim to be authoritative over the tool. If unchanged, `git diff prototypes/System-Architecture/SOLUTION-MATRIX.md` is empty and the SUMMARY records "verified, no change".
  </acceptance_criteria>
  <done>PUMP-CONTROL-CONCEPTS.md carries a dated prices-superseded preface with its historical numbers intact, and SOLUTION-MATRIX.md is verified consistent.</done>
</task>

<task type="auto">
  <name>Task 3: Landing card, README row, and CLAUDE.md description refresh</name>
  <files>index.html, README.md, CLAUDE.md</files>
  <read_first>
    - `index.html` (repo root) lines 330–340 — the System Architecture Explorer `<a class="tool-card">` block and the copy shape used by the neighbouring cards.
    - `README.md` line ~19 — the System Architecture Explorer row and the description style of the surrounding rows.
    - `CLAUDE.md` — the folder-structure block's `tools/system-architecture-explorer/` entry.
    - `06.1-03-SUMMARY.md` — the final direction names, for accurate copy.
    - `tools/system-architecture-explorer/index.html` — to describe only what actually shipped.
  </read_first>
  <action>
The tool changed character this phase: it is no longer a control-electronics cost matrix, it is a
whole-device architecture page (schema → directions → matrix → diagram). Refresh its one-line
description in the three places that carry one. **This is a copy refresh only** — landing-page
redesign and any new tool are explicitly out of scope for this phase.

1. **`index.html` (repo root), the tool card (~line 330).** Update the card's description to lead
   with the whole-system framing: a six-module architecture schema, design directions, then the
   variant comparison. Keep the existing card markup, classes, `--rd` stagger value, and
   `href="tools/system-architecture-explorer/index.html"` exactly as they are — only the copy
   changes. Note this page has EN/IT `data-i18n` coverage: if the card's copy is translated, update
   **both** language strings; a stale Italian string is the same drift this phase exists to fix.
   (The tool page itself stays English-only — that is its established precedent.)
2. **`README.md`, the tool table row (~line 19).** Same treatment: the current description
   ("Control-electronics cost / complexity selector…") undersells it. Rewrite to cover the schema,
   the directions gallery, the Design type column, and the existing matrix/BOM/diagram features.
   Keep the row's link, the `✅ Live` status cell, and the table's column structure. Do **not**
   state a variant count — that number has already drifted once (D-18); describe the capability, not
   the tally.
3. **`CLAUDE.md`, the folder-structure block.** Its `tools/system-architecture-explorer/` entry
   currently reads `System Architecture Explorer — control-electronics cost/complexity matrix
   (promoted from prototypes/System-Architecture/)`. Update to reflect the whole-device page. No new
   files landed this phase, so the folder shape itself does not change — only the description.

Describe only what shipped. Read the tool before writing the copy; do not promise a feature from
this plan's text that plans 01–03 did not actually land.
  </action>
  <verify>
    <automated>grep -n "system-architecture-explorer" index.html README.md CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - The landing card's description in `index.html` mentions the module schema and the design directions; `git diff index.html` shows changes confined to the card's text content — the `<a>` element's `href`, `class`, and `--rd` value are untouched.
    - If the card carries `data-i18n` translated strings, both the EN and IT strings are updated; `grep` shows no remaining Italian string describing it as only a matrix.
    - `README.md`'s System Architecture Explorer row mentions the schema and the design directions, retains its link and `✅ Live` cell, and contains no hardcoded variant count (`grep -n "20-variant\|19-variant\|17-variant" README.md` returns `0`).
    - `CLAUDE.md`'s `tools/system-architecture-explorer/` entry no longer describes the tool as a `cost/complexity matrix` alone.
    - No copy in any of the three files describes a feature not present in the shipped `tools/system-architecture-explorer/index.html`.
    - Browser check: http://localhost:7331/index.html renders the refreshed card with its layout and hover behaviour unchanged, and the card still navigates to the tool. Toggling EN/IT shows the refreshed copy in both languages.
  </acceptance_criteria>
  <done>The landing card, README row, and CLAUDE.md entry describe a whole-device architecture page, matching what actually shipped, with markup and structure untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None introduced. This plan edits markdown documentation and the text content of one existing
landing-page anchor element. It adds no code, no inputs, no data paths, no dependencies, and no
new network destinations.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-12 | Tampering | Decision records asserting resolved-as-open (or open-as-resolved) hardware facts, driving a wrong build/order decision | mitigate | The real (non-security) risk this plan addresses. Each fix is pinned to a sourced RESEARCH finding with its confidence level, and the acceptance criteria assert the exact stale strings are gone. Task 2 explicitly forbids rewriting PUMP-CONTROL-CONCEPTS.md's historical prices, preserving the audit trail rather than silently revising history. |
| T-06.1-13 | Tampering | Landing-card copy edit accidentally altering the card's `href` or markup | mitigate | Task 3 restricts the diff to text content and asserts via acceptance criteria that `href`, `class`, and `--rd` are untouched. CLAUDE.md's standing rule against VS Code Live Preview (which rewrites relative paths and strips inline scripts) applies — use `.\serve.bat`. |
| T-06.1-14 | Information Disclosure | Markdown/HTML links to public bitbyg vendor pages | accept | Public URLs in a public thesis repo. No credentials, no PII, no private endpoints. |
| T-06.1-SC | Tampering | Supply chain (npm/pip/cargo installs) | not applicable | No package manager in this project (CLAUDE.md: no build tools, no npm). This plan installs nothing. |

**Not applicable, and why:** Spoofing / Repudiation / Denial of Service / Elevation of Privilege —
no auth, no sessions, no accounts, no server, no roles. XSS is not reachable: the edited landing
card content is static author-written HTML text, not data flowing through `innerHTML`. SQLi, CSRF,
SSRF — no backend, no database, no requests.
</threat_model>

<verification>
1. `grep -rn "confirm SPI vs parallel\|Motor #2 — TBD\|12 V + 24 V dual rail\|TBD" prototypes/System-Architecture/` returns nothing.
2. `PUMP-CONTROL-CONCEPTS.md` opens with the dated superseded-prices blockquote; `git diff` on it shows additions only, no altered price figures.
3. `SOLUTION-MATRIX.md` is verified consistent (expected: zero diff).
4. Serve with `.\serve.bat` → http://localhost:7331/index.html: the refreshed card renders, hovers, and navigates correctly in both EN and IT (never VS Code Live Preview).
5. Every cross-link added resolves: `#system` and `#matrix` exist in the shipped tool; SPEC.md's power-rail section exists.
6. `assets/style.css` unchanged.

<human-check>
Sirio to confirm at end of phase: the landing-card and README one-liners describe the tool the way
he'd want it cited — it is now an architecture page, not a BOM calculator, and these two strings are
what a reader meets first.
</human-check>
</verification>

<success_criteria>
- SC-6: the three decision records are internally consistent and consistent with the tool — no resolved question still presented as open, no TBD for a decided part, no superseded rail topology.
- D-13: the alignment-motor-#2 placeholder is resolved to a second 28BYJ-48 + ULN2003 in ARCHITECTURE.md.
- D-15: ARCHITECTURE.md's dual-rail assumption is replaced by the researched two-buck-converter topology and the 12 V winding choice.
- D-18: the ARCHITECTURE.md fixes, the PUMP-CONTROL-CONCEPTS.md dated preface (preface, not rewrite), and the SOLUTION-MATRIX.md verification are all complete.
</success_criteria>

<output>
Create `.planning/phases/06.1-architecture-refinement-full-system-module-schema-design-dir/06.1-05-SUMMARY.md` when done.
Record in it: whether SOLUTION-MATRIX.md needed any change, and the final landing-card/README copy.
</output>
