---
phase: 05-html-presentation-decks
plan: 05
type: execute
wave: 4
gap_closure: true
depends_on: []
files_modified:
  - assets/deck.js
  - decks/lab-meeting-2026-06/index.html
autonomous: true
requirements: [SC-1, SC-2, SC-3]
must_haves:
  truths:
    - "deck.js handles Esc and O hotkeys properly and accurately scales the deck on window resize"
    - "The lab-meeting deck uses premium glassmorphic dark styling matching the main site"
    - "The lab-meeting deck focuses on concepts and images, explicitly avoiding raw numbers and dense text"
  artifacts: []
  key_links: []
---

<objective>
Gap Closure Plan: Resolve issues found during Human UAT. Fix the `deck.js` hotkeys and resize scaling, and completely overhaul the aesthetics and content of the seed deck (`decks/lab-meeting-2026-06/index.html`) to match the premium glassmorphic style and utilize the existing assets from the prototypes page.
</objective>

<execution_context>
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/workflows/execute-plan.md
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-html-presentation-decks-build-a-reveal-style-slide-system-on/05-HUMAN-UAT.md
@assets/style.css
@assets/deck.css
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix deck.js hotkeys and resize logic</name>
  <files>assets/deck.js</files>
  <read_first>
    - assets/deck.js
    - .planning/phases/05-html-presentation-decks-build-a-reveal-style-slide-system-on/05-HUMAN-UAT.md
  </read_first>
  <action>
    Fix the `fitStage` logic in `assets/deck.js` to ensure the resize event listener is correctly bound and triggers a recalculation of the transform scale. Fix the keydown listener to ensure `Esc` and `O` (or `o`) successfully toggle the `data-overview` attribute on `document.body` and `.hidden` on the overview element when not in demo mode. Ensure `Esc` exits demo mode without triggering the overview toggle.
  </action>
  <verify>
    <automated>grep -q 'toggleOverview' assets/deck.js && node --check assets/deck.js && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `deck.js` correctly recalculates dimensions and applies `transform: translate(...) scale(...)` inside the `resize` listener.
    - `deck.js` handles `Esc` properly to exit demo mode if active, OR close the overview if open.
    - `deck.js` handles `O` and `o` keys to toggle the overview grid.
  </acceptance_criteria>
</task>


<task type="auto">
  <name>Task 3: Overhaul aesthetics and content of the seed deck</name>
  <files>decks/lab-meeting-2026-06/index.html</files>
  <read_first>
    - decks/lab-meeting-2026-06/index.html
    - prototypes/index.html
    - assets/style.css
    - assets/deck.css
    - .planning/phases/05-html-presentation-decks-build-a-reveal-style-slide-system-on/05-HUMAN-UAT.md
  </read_first>
  <action>
    Completely overhaul the aesthetics of `decks/lab-meeting-2026-06/index.html` to follow high-quality presentation standards. Apply the premium dark glassmorphic design from the main site.
    Use classes like `.glass`, `.tool-card`, and the CSS variables from `assets/style.css`.
    Presentation Standards: Minimal text per slide, heavily visual, concept-driven. AVOID raw numbers/data dumps.
    Extract the beautiful images (e.g., `assets/test-campaign.png`) from `prototypes/index.html` to use as the hero focal point of the slides.
    For the "Automated Test-Campaign App" slide, fix the broken image link (`../../assets/test-campaign.png`) and expand it to dominate the slide visually, pushing text to a short concept summary.
    For the Gap-Sweep Redesign slide, strip out the bullet points of numbers and replace them with a high-level conceptual summary of the design approach.
    Ensure headings use the `.slide-title` or appropriate typography classes, and colors match the site's sleek aesthetic.
  </action>
  <verify>
    <automated>grep -q 'class=".*glass.*"' decks/lab-meeting-2026-06/index.html && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `decks/lab-meeting-2026-06/index.html` uses glassmorphic classes (e.g., `.glass` or inline styles utilizing `--glass-bg`).
    - The broken image on the Test-Campaign slide is fixed and points to a valid image path.
    - The slides use minimal text and focus on high-level concepts rather than data/numbers.
    - Images are utilized as the focal point of the slides (hero images, large layouts).
    - The overall layout looks premium and utilizes CSS flexbox/grid for proper alignment.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | UAT Updates | accept | Gap closure only modifies visual aesthetics and fixes minor state machine bugs in JS; no new threat vectors introduced. |
</threat_model>

<verification>
- Resize the browser window on `decks/lab-meeting-2026-06/index.html` and verify the stage scales correctly.
- Press `O` to toggle overview. Press `Esc` to close it.
- Enter iframe demo mode, press `Esc`, verify it exits demo mode without toggling overview.
- Check the seed deck to ensure it looks premium and includes the actual images and metrics.
</verification>

<success_criteria>
Human UAT gaps are fully closed: hotkeys work, resize works, and the seed deck is visually stunning.
</success_criteria>

<output>
Create `.planning/phases/05-html-presentation-decks-build-a-reveal-style-slide-system-on/05-05-SUMMARY.md` when done.
</output>
