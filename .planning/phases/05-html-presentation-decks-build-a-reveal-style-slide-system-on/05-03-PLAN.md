---
phase: 05-html-presentation-decks
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - decks/lab-meeting-2026-06/index.html
  - decks/lab-meeting-2026-06/assets/.gitkeep
autonomous: true
requirements: [SC-2, SC-3]
must_haves:
  truths:
    - "The seed deck at decks/lab-meeting-2026-06/index.html loads assets/style.css + assets/deck.css + assets/deck.js and authors a flat sequence of <section class=\"slide\"> elements"
    - "The deck covers all six segments in order: Coding-with-AI (GSD), Designing-with-AI (rotor solver), Designing-with-AI (displaced-volume model), proto-01, proto-02, automated test-campaign app"
    - "The rotor-solver and displaced-volume-model segments embed the real tool pages as live iframes inside a framed titled panel"
    - "Each tool-embed slide uses the iframe-overlay click-to-activate pattern so the deck keeps keyboard control until the presenter activates the embed"
    - "The test-campaign segment uses screenshots/static media (not a live iframe) and still carries a slide title"
    - "The deck runs offline from file:// and on GitHub Pages — all paths are relative and no external/CDN resource is loaded"
    - "Fragments build then advance via arrow/space, and slides deep-link via #/<n>"
  artifacts:
    - path: "decks/lab-meeting-2026-06/index.html"
      provides: "Seed lab-meeting deck covering all six segments using the runtime"
      contains: "class=\"slide"
      min_lines: 120
  key_links:
    - from: "decks/lab-meeting-2026-06/index.html"
      to: "assets/deck.js"
      via: "script src ../../assets/deck.js"
      pattern: "\\.\\./\\.\\./assets/deck\\.js"
    - from: "decks/lab-meeting-2026-06/index.html"
      to: "tools/rotor-solver/index.html"
      via: "live iframe inside .iframe-wrapper"
      pattern: "\\.\\./\\.\\./tools/rotor-solver/index\\.html"
    - from: "decks/lab-meeting-2026-06/index.html"
      to: "tools/peristaltic-roller-displaced-volume-model/index.html#calculator"
      via: "live iframe deep-linked to the calculator anchor"
      pattern: "displaced-volume-model/index\\.html#calculator"
---

<objective>
Author the seed ~15-minute lab-meeting deck at `decks/lab-meeting-2026-06/index.html` (D-02 slug mirrors the index card href). It is a thin HTML shell that loads the runtime (D-01) and contains a flat sequence of `<section class="slide">` elements covering all six required segments in a sensible order. The two site tools are embedded as live iframes in framed titled panels with the click-to-activate overlay (D-07/D-08/D-09); the external test-campaign app is shown via static media (D-10).

Purpose: Satisfies Success Criterion SC-3 (first deck covers all six segments, embeds the most relevant interactive elements/images, runs offline AND on GitHub Pages) and exercises SC-2 (the runtime in real use).
Output: `decks/lab-meeting-2026-06/index.html` plus a `decks/lab-meeting-2026-06/assets/` folder for deck media.
</objective>

<execution_context>
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/workflows/execute-plan.md
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-html-presentation-decks-build-a-reveal-style-slide-system-on/05-CONTEXT.md
@.planning/phases/05-html-presentation-decks-build-a-reveal-style-slide-system-on/05-RESEARCH.md
@.planning/phases/05-html-presentation-decks-build-a-reveal-style-slide-system-on/05-PATTERNS.md
@prototypes/Prototype-1-Pump-Module/proto-01-5ul-4roller/PROTOTYPE.md
@prototypes/Prototype-1-Pump-Module/proto-02-5ul-4roller-v2/PROTOTYPE.md
@prototypes/PROTOTYPES.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the deck shell and media folder; author the title + GSD + proto-01 + proto-02 + test-campaign slides</name>
  <files>decks/lab-meeting-2026-06/index.html, decks/lab-meeting-2026-06/assets/.gitkeep</files>
  <read_first>
    - .planning/phases/05-html-presentation-decks-build-a-reveal-style-slide-system-on/05-PATTERNS.md (decks/<slug>/index.html section: document shell loading style.css + deck.css + deck.js, .deck-stage tabindex="0", flat <section class="slide"> sequence, .deck-back-link, no-CDN-without-local-fallback)
    - assets/deck.css (the slide / .slide-title / .fragment / HUD / overview / iframe-wrapper class names this HTML must use — produced by 05-01)
    - assets/deck.js (confirms the markup hooks it expects: .deck-stage, .slide, .slide--active, .fragment, .iframe-wrapper, .iframe-overlay — produced by 05-01)
    - prototypes/Prototype-1-Pump-Module/proto-01-5ul-4roller/PROTOTYPE.md (proto-01 design params + measured results for that slide's content)
    - prototypes/Prototype-1-Pump-Module/proto-02-5ul-4roller-v2/PROTOTYPE.md (proto-02 gap-sweep redesign content)
    - prototypes/PROTOTYPES.md (shallow registry — one-line framing per prototype)
    - tools/gsd-workflow-guide/index.html (GSD segment may reference/link this guide)
  </read_first>
  <action>
    Create the folder decks/lab-meeting-2026-06/ with an assets/ subfolder; add decks/lab-meeting-2026-06/assets/.gitkeep so the media folder is tracked (D-10 screenshots/recording will be dropped here later).
    Create decks/lab-meeting-2026-06/index.html as the thin shell per PATTERNS.md: doctype, `<html lang="en">`, charset+viewport meta, title "Lab Meeting Jun 2026 — Thesis Tools". In `<head>` load, IN ORDER and with relative paths: `../../assets/style.css`, then `../../assets/deck.css`. At the end of `<body>` load `<script src="../../assets/deck.js"></script>`. No `data-i18n` anywhere (single-language). Deck-specific overrides (e.g. a slide background) go in a small `<style>` block here, NOT in deck.css.
    Body structure: `<div class="deck-viewport"><div class="deck-stage" tabindex="0"> ... slides ... </div></div>`, the HUD elements deck.js expects (or that it injects — match whichever 05-01 chose; read deck.js), and a persistent `<a class="deck-back-link" href="../../decks/index.html">← Presentations</a>`.
    Author these slides as a flat `<section class="slide">` sequence (first slide also gets `slide--active`). Use `.slide-title` for every slide's title (D-08 — even embed slides keep a title) and `.fragment` spans/list-items for stepped reveals (D-13). Keep content concise (lab-meeting pace, ~15 min):
      1. TITLE slide — talk title, author "Sirio Vittorio Feltrin", date June 2026, one-line agenda.
      2. CODING-WITH-AI (GSD) — what GSD is and the discuss→plan→execute loop; bullet fragments; reference the GSD Workflow Guide (a link to ../../tools/gsd-workflow-guide/index.html is fine; embedding it as a live iframe is optional and at discretion).
      6. proto-01 — purpose + key design params + measured result, sourced from PROTOTYPE.md (real numbers, not placeholders).
      7. proto-02 — the gap-sweep redesign rationale + what it tests, from proto-02 PROTOTYPE.md.
      8. TEST-CAMPAIGN APP (D-10) — NOT a live iframe. Use an `<img>` referencing decks/lab-meeting-2026-06/assets/ (or a muted `<video>` if a recording is added later); if the actual screenshot files do not exist yet, reference the intended filename(s) and add an HTML comment noting the presenter must drop the captured screenshots into assets/. Slide still carries a `.slide-title`.
    Leave the two Designing-with-AI iframe slides (positions 3,4,5 in segment order — rotor solver and displaced-volume model) to Task 2; insert clear placeholder comments `<!-- TASK 2: rotor-solver iframe slide here -->` and `<!-- TASK 2: displaced-volume-model iframe slide here -->` in the correct order between the GSD slide and the proto-01 slide so segment order is preserved.
    All paths relative (`../../...`); no http/https/CDN references (D-15, offline + GitHub Pages).
  </action>
  <verify>
    <automated>test -f decks/lab-meeting-2026-06/assets/.gitkeep && grep -q '../../assets/deck.css' decks/lab-meeting-2026-06/index.html && grep -q '../../assets/deck.js' decks/lab-meeting-2026-06/index.html && grep -q 'deck-stage' decks/lab-meeting-2026-06/index.html && grep -q 'slide--active' decks/lab-meeting-2026-06/index.html && grep -q 'TASK 2' decks/lab-meeting-2026-06/index.html && ! grep -qi 'src="http\|href="http' decks/lab-meeting-2026-06/index.html && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - decks/lab-meeting-2026-06/assets/.gitkeep exists.
    - index.html loads `../../assets/style.css`, `../../assets/deck.css`, and `../../assets/deck.js` with relative paths.
    - The first `.slide` also carries `slide--active`; every authored slide has a `.slide-title`.
    - Slides exist for: title, GSD, proto-01, proto-02, test-campaign (5 authored) plus two `<!-- TASK 2 ... -->` placeholders positioned between GSD and proto-01.
    - The test-campaign slide uses an `<img>`/`<video>` from the local `assets/` folder, not an `<iframe>` of an external app.
    - proto-01 and proto-02 slides contain real values drawn from their PROTOTYPE.md files (not "TBD"/placeholder text).
    - No external/CDN `src`/`href` in the file.
  </acceptance_criteria>
  <done>The deck shell loads the runtime and contains five authored segment slides plus correctly-ordered placeholders for the two tool-embed slides, all offline-relative and single-language.</done>
</task>

<task type="auto">
  <name>Task 2: Author the two Designing-with-AI live-iframe slides (rotor solver + displaced-volume model)</name>
  <files>decks/lab-meeting-2026-06/index.html</files>
  <read_first>
    - decks/lab-meeting-2026-06/index.html (the shell from Task 1 with the two TASK 2 placeholder comments)
    - .planning/phases/05-html-presentation-decks-build-a-reveal-style-slide-system-on/05-RESEARCH.md (mechanic 4: iframe-wrapper + iframe tabindex="-1" + iframe-overlay structure; same-origin note)
    - .planning/phases/05-html-presentation-decks-build-a-reveal-style-slide-system-on/05-PATTERNS.md (decks/<slug>/index.html iframe embed slide structure)
    - assets/deck.css (.iframe-wrapper / .iframe-overlay styling produced by 05-01)
    - assets/deck.js (overlay-click activation + data-demo-active gating produced by 05-01 — confirms the exact class hooks)
    - tools/peristaltic-roller-displaced-volume-model/index.html (confirm the #calculator anchor id exists for deep-linking)
  </read_first>
  <action>
    Replace the two `<!-- TASK 2 ... -->` placeholder comments in decks/lab-meeting-2026-06/index.html with two `<section class="slide">` tool-embed slides, in segment order (rotor solver first, then displaced-volume model), per RESEARCH.md mechanic 4 / PATTERNS.md embed structure.
    Each slide: a `.slide-title` (D-08, the slide always keeps a title even though the embed is live) plus a `.iframe-wrapper` (~70% of the slide, D-08) containing:
      - an `<iframe>` with `tabindex="-1"` (inert until activated, D-09) whose `src` is a RELATIVE path:
        - Slide A — "Designing with AI: Rotor Geometry Solver" → `src="../../tools/rotor-solver/index.html"`.
        - Slide B — "Designing with AI: Occlusion & Displaced-Volume Model" → `src="../../tools/peristaltic-roller-displaced-volume-model/index.html#calculator"` (deep-link straight into the interactive calculator via the existing #calculator anchor).
      - a sibling `<div class="iframe-overlay" aria-hidden="true"></div>` that catches clicks until the presenter activates demo mode (deck.js from 05-01 handles overlay-click → data-demo-active, and Esc/click-outside → return; this slide only supplies the markup hooks).
    Add a small caption/fragment beside or above the panel explaining the live-embed point ("this is the real tool, always in sync"). Do NOT add a `sandbox` attribute — these are same-origin own pages and demo mode needs `iframe.focus()` (RESEARCH same-origin note). Keep all paths relative; no CDN (D-15).
  </action>
  <verify>
    <automated>grep -q '../../tools/rotor-solver/index.html' decks/lab-meeting-2026-06/index.html && grep -q 'displaced-volume-model/index.html#calculator' decks/lab-meeting-2026-06/index.html && grep -c 'iframe-overlay' decks/lab-meeting-2026-06/index.html | grep -qv '^0$' && grep -q 'tabindex="-1"' decks/lab-meeting-2026-06/index.html && ! grep -q 'TASK 2' decks/lab-meeting-2026-06/index.html && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Both `<!-- TASK 2 ... -->` placeholder comments are gone (replaced by real slides).
    - A slide embeds `../../tools/rotor-solver/index.html` and another embeds `../../tools/peristaltic-roller-displaced-volume-model/index.html#calculator`, both via relative `src`.
    - Each embed `<iframe>` has `tabindex="-1"` and a sibling `.iframe-overlay` inside a `.iframe-wrapper`.
    - Each embed slide has a `.slide-title` (the title persists even though the embed is live, D-08).
    - No `sandbox` attribute on the iframes (same-origin own content; demo mode needs focus).
    - The rotor-solver embed precedes the displaced-volume-model embed and both sit between the GSD slide and the proto-01 slide (segment order preserved).
  </acceptance_criteria>
  <done>The deck embeds both site tools as live, click-to-activate iframes in titled framed panels, with the displaced-volume model deep-linked to its calculator — completing the six-segment coverage for SC-3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deck → live `<iframe>` embeds | The deck frames the project's OWN tool pages (rotor solver, displaced-volume model) — same-origin (same file tree on USB / same domain on GitHub Pages). No third-party or untrusted page is ever framed. |
| deck → static media | The test-campaign segment uses screenshots/recording checked into the deck folder — static local files, no external app loaded (D-10). |
| user → deck | Navigation only; no form input, no persisted data, no network calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | live iframe embeds | mitigate | Only same-origin OWN tool pages are framed; the click-to-activate overlay + `tabindex="-1"` keep the embed inert until the presenter activates it. No `sandbox` is used deliberately (same-origin, focus needed) and is safe because the content is the project's own trusted pages. |
| T-05-02 | Information Disclosure | static deck + media | accept | No PII, no secrets, no backend, no user input. Screenshots are author-chosen non-sensitive captures. Nothing to disclose. |
| T-05-03 | Tampering (supply chain) | external resources | mitigate | D-15 forbids CDN/external resources. All `src`/`href` are relative local paths; the test-campaign app is shown as local static media, not a remote embed. Verify asserts no `http` references. |
</threat_model>

<verification>
- Open decks/lab-meeting-2026-06/index.html (via serve.bat / file://): the title slide shows; arrow/space steps fragments then advances through all six segments in order.
- The rotor-solver and displaced-volume slides show the real tools inside framed panels; clicking the overlay activates demo mode (arrow keys stop advancing the deck); Esc returns deck control.
- The displaced-volume embed lands on the calculator (#calculator anchor).
- The test-campaign slide shows local media with a title and no live external embed.
- #/4 on refresh lands on the 4th slide; no horizontal scroll; no external/CDN references (grep).
</verification>

<success_criteria>
SC-3 met: the first deck covers all six segments in a sensible order, embeds the most relevant interactive tools (live iframes) and images (test-campaign media), and runs offline AND on GitHub Pages. SC-2 is exercised end-to-end by a real deck.
</success_criteria>

<output>
Create `.planning/phases/05-html-presentation-decks-build-a-reveal-style-slide-system-on/05-03-SUMMARY.md` when done.
</output>
