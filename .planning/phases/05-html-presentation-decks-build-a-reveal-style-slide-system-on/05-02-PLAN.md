---
phase: 05-html-presentation-decks
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - decks/index.html
  - index.html
autonomous: true
requirements: [SC-1]
must_haves:
  truths:
    - "A presentations index page exists at decks/index.html, themed in the site dark-glass style with blob chrome and a '← Resources' back nav"
    - "The index lists each deck as a hand-authored card showing the deck title and its date"
    - "On load the cards perform a card-deck fan / deal-out motion, distinct from the Phase-4 winding path"
    - "Under prefers-reduced-motion: reduce the cards are fully visible with no deal-out motion"
    - "The root landing page index.html has a Presentations section linking to decks/index.html"
    - "Each deck card links to decks/<slug>/index.html via a full page load (no in-page swap)"
  artifacts:
    - path: "decks/index.html"
      provides: "Presentations index: fanned deck cards with deal-out, nav, blob chrome"
      contains: "deck-card"
      min_lines: 80
    - path: "index.html"
      provides: "Presentations section linking to the index"
      contains: "decks/index.html"
  key_links:
    - from: "index.html"
      to: "decks/index.html"
      via: "anchor in a new Presentations <section>"
      pattern: "decks/index\\.html"
    - from: "decks/index.html"
      to: "decks/lab-meeting-2026-06/index.html"
      via: "deck-card anchor (full page load)"
      pattern: "lab-meeting-2026-06/index\\.html"
---

<objective>
Build the presentations index page (`decks/index.html`) with its signature card-deck fan / deal-out motion (D-06), hand-authored deck cards (D-05), site theme + blob chrome + back nav (D-16), and a `prefers-reduced-motion` fallback (D-17). Link it from the root landing page `index.html` (Success Criterion SC-1). Each deck card links via a full page load to the deck (D-04).

Purpose: The browse surface for decks with the distinctive interaction SC-1 requires. The seed deck it links to is authored in plan 05-03 (parallel; this plan only needs the href/slug to exist).
Output: `decks/index.html`, edited `index.html`.
</objective>

<execution_context>
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/workflows/execute-plan.md
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-html-presentation-decks-build-a-reveal-style-slide-system-on/05-CONTEXT.md
@.planning/phases/05-html-presentation-decks-build-a-reveal-style-slide-system-on/05-PATTERNS.md
@assets/style.css
@index.html
@prototypes/index.html
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: decks/index.html — fanned deck cards with deal-out, theme chrome, back nav</name>
  <files>decks/index.html</files>
  <read_first>
    - .planning/phases/05-html-presentation-decks-build-a-reveal-style-slide-system-on/05-PATTERNS.md (decks/index.html section: document shell, blob chrome, nav bar, animate-in stagger, deal-out within reduced-motion inversion, deck-card structure; Shared Patterns: blob chrome, animate-in, glass nav-bar, reduced-motion guard structure)
    - prototypes/index.html (lines 1-28 shell + blobs; lines 10-26 .tool-nav glass bar; lines 84-103 reduced-motion inversion; lines 440-450 blob + nav HTML; lines 1096-1109 reducedMotion JS guard + IntersectionObserver)
    - assets/style.css (lines 1-13 tokens; lines 29-73 blob styles already shared; lines 143-193 .tool-card; lines 207-216 fade-up/animate-in)
    - index.html (the root landing page — to match card title/desc tone and the section pattern)
  </read_first>
  <action>
    Create decks/index.html as a standalone themed page. It loads ONLY `../assets/style.css` (NOT deck.css/deck.js — those are deck-runtime files per PATTERNS.md). Put index-specific styles in a single `<style>` block. Single-language English only — do NOT add any `data-i18n` attributes (precedent: displaced-volume model opted out; consistent with D-05 hand-authored simplicity).
    Shell: standard doctype, `<html lang="en">`, charset + viewport meta, title "Presentations — Thesis Tools".
    Blob chrome: copy the `.bg-blobs` / three `.blob blob-1/2/3` markup verbatim (styles already in style.css, no redefinition).
    Nav bar: `.tool-nav animate-in` with `<a class="nav-back" href="../index.html">← Resources</a>` and `<span class="nav-title">Presentations</span>`. Define `.tool-nav`/`.nav-back`/`.nav-title` in the page `<style>` with the same glass values as prototypes/index.html (sticky, `background: rgba(10,10,12,0.85)`, `backdrop-filter: blur(16px)` + `-webkit-`, `border-bottom: 1px solid var(--glass-border)`).
    Deck cards (D-05, hand-authored): one `<a class="deck-card ...">` per deck. Author exactly ONE card now for the seed deck — title "Lab Meeting — June 2026", a one-line desc listing the six segments (GSD · rotor solver · displaced-volume model · proto-01 · proto-02 · test-campaign app), a visible date `2026-06-24`, and `href="lab-meeting-2026-06/index.html"` (relative — full page load, D-04). Leave an HTML comment "Future decks — duplicate the card block above" for future hand-authoring. Build deck-card base styling off the `.tool-card` glass aesthetic in style.css.
    Deal-out motion (D-06) — the signature interaction, distinct from Phase-4's winding path. Use the reduced-motion INVERSION pattern (PATTERNS.md Shared Patterns / prototypes lines 84-103): DEFAULT state of `.deck-card` is fully visible (`opacity:1; transform:none`). Inside `@media (prefers-reduced-motion: no-preference)` give each card an initial fanned/stacked transform (per-card `--card-angle` from about -8deg to 8deg, slight translateY, overlapping/stacked) and a transition to the spread/grid resting position when an `.is-visible` class is added. Stagger via per-card `--delay`. This guarantees reduced-motion users see the spread cards with no animation. Idle/entrance timing is Claude's discretion within the bespoke, guarded constraint.
    Deal-out trigger JS: a small inline `<script>` mirroring prototypes/index.html — `const reducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;` then, only if `!reducedMotion`, add `.is-visible` to the cards (immediately on load or via IntersectionObserver) to fire the deal-out. Native only, no libraries, no CDN (D-15). Optionally reuse `.animate-in` + `--delay` from style.css for the nav/section entrance.
    No horizontal scroll at any width (CLAUDE.md constraint) — cards wrap.
  </action>
  <verify>
    <automated>grep -q 'deck-card' decks/index.html && grep -q 'lab-meeting-2026-06/index.html' decks/index.html && grep -q 'prefers-reduced-motion' decks/index.html && grep -q 'nav-back' decks/index.html && grep -q '2026-06-24' decks/index.html && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - decks/index.html loads `../assets/style.css` and does NOT load deck.css or deck.js.
    - Exactly one `.deck-card` anchor exists, with title text "Lab Meeting — June 2026", a visible date `2026-06-24`, and `href="lab-meeting-2026-06/index.html"`.
    - `.deck-card` default (outside any media query) is `opacity: 1` with no offsetting transform; the fanned/animated state lives inside `@media (prefers-reduced-motion: no-preference)`.
    - Page contains a `.tool-nav` with `.nav-back` linking `../index.html` and the title "Presentations".
    - Blob chrome markup (`.bg-blobs` + three `.blob`) is present.
    - No `data-i18n` attributes anywhere; no external/CDN `src`/`href`/`@import url(http`.
  </acceptance_criteria>
  <done>The presentations index renders fanned deck cards that deal out on load (and sit fully visible under reduced-motion), themed in the site dark-glass style, with one card linking to the seed deck via a full page load.</done>
</task>

<task type="auto">
  <name>Task 2: index.html — add Presentations section linking to the index</name>
  <files>index.html</files>
  <read_first>
    - index.html (lines 34-79: the .resources-sections wrapper and the existing Calculators/Guides/Prototypes `<section>` blocks; lines 67-77 the Prototypes section to clone; note --delay stagger goes 0.75/0.80/0.88)
    - .planning/phases/05-html-presentation-decks-build-a-reveal-style-slide-system-on/05-PATTERNS.md (index.html edit section: pattern to copy, increment --delay, no data-i18n)
    - assets/style.css (lines 143-193 .tool-card; lines 207-216 animate-in)
  </read_first>
  <action>
    Edit index.html. Add a new `<section aria-label="Presentations">` block INSIDE the `.resources-sections` wrapper, immediately AFTER the existing "Hardware prototypes" section (after its closing `</section>` near line 77, before the wrapper's closing `</div>`). Clone the Prototypes section structure: a `.section-label animate-in` ("Presentations"), a `.section-desc animate-in` (one line, e.g. "Reveal-style HTML slide decks covering the AI-assisted design process — runnable in the browser or offline."), and a `.tools-grid` containing one `.tool-card animate-in` anchor `href="decks/index.html"` with an icon (e.g. the playing-card emoji to echo the card-deck pun), title "Presentations", a short desc, and `<span class="card-link">Open →</span>`.
    Increment the `--delay` stagger past the last existing value (Prototypes uses 0.75/0.80/0.88) — use about 1.0s / 1.05s / 1.12s so the new section animates in after the prototypes section.
    Do NOT add `data-i18n` attributes to the new section (single-language decision, consistent with the no-i18n precedent for this feature). Leave the rest of index.html (the LANG dictionary, lang-toggle) untouched — the new section simply has no translations and renders in English in both modes, which is acceptable here.
    No horizontal scroll; reuse existing grid classes so layout matches the other sections exactly.
  </action>
  <verify>
    <automated>grep -q 'aria-label="Presentations"' index.html && grep -q 'href="decks/index.html"' index.html && node -e "const s=require('fs').readFileSync('index.html','utf8'); const op=(s.match(/<section/g)||[]).length, cl=(s.match(/<\/section>/g)||[]).length; if(op!==cl){console.error('mismatch',op,cl);process.exit(1)} console.log('PASS',op)" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains a `<section aria-label="Presentations">` placed after the Hardware prototypes section and inside `.resources-sections`.
    - That section contains a `.tool-card` anchor with `href="decks/index.html"` and title "Presentations".
    - `<section>` open and close tags are balanced (no broken nesting introduced by the edit).
    - The new section's `animate-in` `--delay` values are larger than the prototypes section's 0.88s.
    - The new section adds no `data-i18n` attributes; existing LANG dictionary and lang-toggle are unchanged.
  </acceptance_criteria>
  <done>The landing page shows a Presentations section that links to decks/index.html, styled identically to the other resource sections, satisfying the "linked from the landing page" half of SC-1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → index pages | Navigation only. Static HTML, no form input, no network calls, no persisted data beyond the existing site lang localStorage (untouched). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Information Disclosure | static index pages | accept | No PII, no secrets, no backend, no user input processed. Static HTML served from USB or GitHub Pages — nothing to disclose. |
| T-05-03 | Tampering (supply chain) | external resources | mitigate | D-15 forbids any CDN/external resource (offline + GitHub Pages constraint). The index loads only the local `assets/style.css`; deal-out JS is inline and native. Verify confirms no external `src`/`href`/`@import url(http`. |
</threat_model>

<verification>
- Open index.html and confirm a Presentations card appears after Prototypes and links to decks/index.html.
- Open decks/index.html: cards fan and deal out on load; with OS reduced-motion enabled the cards are fully visible and static; the seed-deck card links to lab-meeting-2026-06/index.html via a full page load; no horizontal scroll at narrow widths.
- grep confirms no data-i18n on the new index page and no external/CDN references.
</verification>

<success_criteria>
SC-1 met: a presentations index page is linked from the landing page, lists each deck by title + date, and uses the distinctive card-deck fan / deal-out browse interaction (reduced-motion-guarded, offline-safe).
</success_criteria>

<output>
Create `.planning/phases/05-html-presentation-decks-build-a-reveal-style-slide-system-on/05-02-SUMMARY.md` when done.
</output>
