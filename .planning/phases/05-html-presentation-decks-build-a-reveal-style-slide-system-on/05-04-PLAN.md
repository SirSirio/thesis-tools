---
phase: 05-html-presentation-decks
plan: 04
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - decks/lab-meeting-2026-06/SPEC.md
  - README.md
  - ROADMAP.md
  - CLAUDE.md
autonomous: true
requirements: [SC-1, SC-2, SC-3]
must_haves:
  truths:
    - "A co-located SPEC.md documents the deck system: the shared-runtime D-01 exception, segments, embedded tools, and offline/single-language assumptions"
    - "README.md tool table has a row for the presentations / deck system linking to decks/index.html"
    - "Repo-root ROADMAP.md reflects the shipped deck system"
    - "CLAUDE.md folder structure documents decks/ and assets/deck.css + assets/deck.js, and notes the sanctioned D-01 shared-runtime exception"
  artifacts:
    - path: "decks/lab-meeting-2026-06/SPEC.md"
      provides: "Deck-system + seed-deck spec per the tool-spec standard"
      contains: "deck.js"
      min_lines: 30
    - path: "CLAUDE.md"
      provides: "Updated folder structure incl. decks/ and shared deck runtime"
      contains: "decks/"
  key_links:
    - from: "README.md"
      to: "decks/index.html"
      via: "tool-table row link"
      pattern: "decks/index\\.html"
    - from: "CLAUDE.md"
      to: "assets/deck.js"
      via: "folder-structure entry + D-01 exception note"
      pattern: "deck\\.js"
---

<objective>
Integrate and document the new deck system per the project's tool-spec standard and CLAUDE.md maintenance rules: a co-located `SPEC.md` for the deck system (including the sanctioned D-01 shared-runtime exception to the inline-only norm), a README tool-table row, a repo-root ROADMAP update, and a CLAUDE.md folder-structure update adding `decks/` plus `assets/deck.css`/`assets/deck.js`.

Purpose: Closes the project's documentation contract (CLAUDE.md development-workflow steps 4–6) so the deck system is discoverable and the D-01 exception is recorded. Touches docs only — runs after the system exists.
Output: `decks/lab-meeting-2026-06/SPEC.md`, edited `README.md`, `ROADMAP.md`, `CLAUDE.md`.
</objective>

<execution_context>
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/workflows/execute-plan.md
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-html-presentation-decks-build-a-reveal-style-slide-system-on/05-CONTEXT.md
@CLAUDE.md
@README.md
@ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author decks/lab-meeting-2026-06/SPEC.md (deck-system + seed-deck spec)</name>
  <files>decks/lab-meeting-2026-06/SPEC.md</files>
  <read_first>
    - CLAUDE.md (Tool spec standard section: what goes in SPEC.md — purpose, inputs/outputs, constants, assumptions; the only-shared-resource-is-style.css rule that D-01 now extends)
    - tools/rotor-solver/SPEC.md (the SPEC.md format precedent to mirror)
    - .planning/phases/05-html-presentation-decks-build-a-reveal-style-slide-system-on/05-CONTEXT.md (D-01..D-17 to summarize accurately)
    - assets/deck.css, assets/deck.js (the actual runtime to describe)
    - decks/lab-meeting-2026-06/index.html (the seed deck's six segments + embedded tools)
  </read_first>
  <action>
    Create decks/lab-meeting-2026-06/SPEC.md following the tool-spec standard (mirror tools/rotor-solver/SPEC.md structure). Sections:
      - Purpose & scope: a reveal-style HTML deck system on the tools site + the seed lab-meeting deck.
      - The shared runtime (D-01 exception): explicitly state that `assets/deck.css` + `assets/deck.js` are a DELIBERATE, sanctioned exception to the project's inline-only norm (the runtime is reused by every future deck); the only other shared resource is `assets/style.css`. Describe what each file owns (deck.css = stage/transitions/HUD/overview/iframe panel + reduced-motion; deck.js = state machine: scale-to-fit, fragment stepping, slide transitions, hash routing #/<n>, HUD, overview grid, iframe focus capture).
      - Authoring model (D-02/D-03): decks at `decks/<slug>/index.html`, flat `<section class="slide">` sequence, fixed 1280×720 scale-to-fit stage, `.fragment` for stepped reveals, `.iframe-wrapper`+`.iframe-overlay` for live tool embeds.
      - Seed deck contents: list the six segments in order and which tools are live iframes (rotor solver; displaced-volume model #calculator) vs static media (test-campaign app, D-10).
      - Navigation: arrow/space stepping (D-13), #/<n> deep links (D-11), progress/counter/arrows (D-12), O/Esc overview + ← Presentations link (D-14).
      - Assumptions: single-language English (no data-i18n), offline-first (no CDN, D-15), reduced-motion guarded (D-17), same-origin live embeds only.
    No fenced code blocks needed beyond short token/identifier mentions; keep it a reference doc.
  </action>
  <verify>
    <automated>test -f decks/lab-meeting-2026-06/SPEC.md && grep -q 'deck.js' decks/lab-meeting-2026-06/SPEC.md && grep -qi 'exception' decks/lab-meeting-2026-06/SPEC.md && grep -qi 'offline' decks/lab-meeting-2026-06/SPEC.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - decks/lab-meeting-2026-06/SPEC.md exists and mentions both `assets/deck.css` and `assets/deck.js`.
    - It explicitly states the shared runtime is a sanctioned exception (D-01) to the inline-only norm.
    - It lists all six seed-deck segments and identifies which are live iframes vs static media.
    - It records the single-language, offline-first, and reduced-motion assumptions.
  </acceptance_criteria>
  <done>The deck system has a co-located SPEC.md documenting the runtime, the D-01 exception, the authoring model, the seed deck's segments, and its assumptions.</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md, repo-root ROADMAP.md, and CLAUDE.md folder structure</name>
  <files>README.md, ROADMAP.md, CLAUDE.md</files>
  <read_first>
    - README.md (lines 13-18: the tool table header + existing rows to match format exactly)
    - ROADMAP.md (repo root — the shipped/planned structure to add the deck system to)
    - CLAUDE.md (the folder-structure code block under "Folder structure"; the "Tool spec standard" + "Key constraints" sections that the D-01 exception touches)
  </read_first>
  <action>
    README.md: add ONE new row to the tool table (after the Prototype Design Space row, matching the existing `| [Name](path) | Description | ✅ Live |` format) — name "Presentations (HTML Decks)" linking `decks/index.html`, description summarizing the reveal-style runtime + the seed lab-meeting deck (six segments, live tool embeds, offline + GitHub Pages), status ✅ Live.
    ROADMAP.md (repo root): reflect the deck system as shipped — add it under the shipped/live section in the same style as existing entries (mirror how prior phases were recorded there). Do not duplicate the .planning/ROADMAP.md phase tracking; this is the repo-facing roadmap.
    CLAUDE.md: in the "Folder structure" code block, add the `decks/` tree (`decks/index.html`, `decks/<slug>/index.html`, `decks/<slug>/SPEC.md`, `decks/<slug>/assets/`) and add `assets/deck.css` + `assets/deck.js` to the assets listing. Update the prose: in "Tool spec standard" / "Key constraints", note that `assets/deck.css` and `assets/deck.js` are the SANCTIONED D-01 exception to the inline-only / single-shared-resource rule (the only shared resources are now `assets/style.css` + the deck runtime). Keep edits minimal and consistent with the existing document tone.
    Do not touch unrelated sections.
  </action>
  <verify>
    <automated>grep -q 'decks/index.html' README.md && grep -q 'decks/' CLAUDE.md && grep -q 'deck.js' CLAUDE.md && grep -qi 'deck' ROADMAP.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - README.md tool table has a new row linking `decks/index.html` in the established `| [Name](path) | Description | Status |` format.
    - Repo-root ROADMAP.md records the deck system as shipped, in the existing entry style.
    - CLAUDE.md folder-structure block lists `decks/` (index + slug/index.html + slug/SPEC.md + slug/assets/) and `assets/deck.css` + `assets/deck.js`.
    - CLAUDE.md prose notes the D-01 shared-runtime exception to the inline-only / single-shared-resource rule.
    - No unrelated sections of the three docs are modified.
  </acceptance_criteria>
  <done>README, repo-root ROADMAP, and CLAUDE.md all reflect the shipped deck system, and CLAUDE.md records the D-01 shared-runtime exception — completing the project's documentation contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → documentation files | Documentation-only edits (Markdown). No executable code, no runtime surface, no user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Information Disclosure | docs | accept | SPEC/README/ROADMAP/CLAUDE are public-facing project docs by design; no secrets are introduced. Nothing sensitive to disclose. |
| T-05-04 | Tampering | CLAUDE.md guardrails | mitigate | The D-01 exception is recorded explicitly so the inline-only norm is not silently eroded; future agents see the single sanctioned exception and its rationale. No new external dependency is documented (offline constraint preserved). |
</threat_model>

<verification>
- README tool table shows a Presentations row linking decks/index.html.
- CLAUDE.md folder structure includes decks/ and the deck runtime, and prose notes the D-01 exception.
- Repo-root ROADMAP.md lists the deck system as shipped.
- decks/lab-meeting-2026-06/SPEC.md documents the runtime, exception, segments, and assumptions.
</verification>

<success_criteria>
Documentation contract complete: the deck system (SC-1/SC-2/SC-3 deliverables) is discoverable via README, recorded in the repo-root ROADMAP, specified in a co-located SPEC.md, and reflected in CLAUDE.md's folder structure with the D-01 exception noted.
</success_criteria>

<output>
Create `.planning/phases/05-html-presentation-decks-build-a-reveal-style-slide-system-on/05-04-SUMMARY.md` when done.
</output>
