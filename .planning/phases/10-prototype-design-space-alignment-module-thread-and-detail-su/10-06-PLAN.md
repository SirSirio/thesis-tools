# Plan 10-06 — Registry and documentation sync

**Wave:** 3 (last — describes what the earlier plans actually built)
**Modifies:** `prototypes/SPEC.md`, `prototypes/PROTOTYPES.md`, `README.md`, `CLAUDE.md`

## Goal

Make the written record match the built one. Several of these files are currently wrong, not
merely incomplete.

## Tasks

### `prototypes/SPEC.md`
- Rewrite **Content Structure** for the new shape: the page is now a two-thread journey view
  plus separate per-prototype subpages, no longer a single-file two-view app.
- Document the **thread model**: what a thread is, the `--t-*` per-thread accent contract,
  and the two colour families.
- Document the **subpage contract**: co-located with the prototype's `PROTOTYPE.md`, links
  `assets/style.css` + `assets/prototype-page.css`, root class `.proto-page`, optional accent
  modifier.
- Update **Animation Technique** — it is now per-thread, with one observer per stage.
- **Prototype Registry**: currently says the alignment module has *"(none yet) — Not yet
  designed"*. That is wrong. Add the alignment rows. Also fix the stale proto-02 row, which
  still reads "In design" though it is v2.3 tested.

### `prototypes/PROTOTYPES.md`
- Replace the alignment module's placeholder row. The module's table uses its own column set
  (`ID | Slug | Status | Notes`) — that is fine, per-module schemas are already allowed to
  differ, but it now needs a **key result** column to carry the V2.1 numbers.
- Rows: **V2** (built 2026-06-25, rack indexing stage) and **V2.1** (built 2026-07-30,
  bench-validated 2026-07-31 — repeatable zero ≤0.03 mm, 132 mm home in 22 s, stroke ~14 mm
  short of the 8-position pattern).
- Note that the alignment module's detail lives at module root rather than in a
  `proto-NN-<slug>/` subfolder, so a reader is not left hunting for one.

### `README.md`
- Update the Prototype Design Space row in the tool table to say it now carries two module
  threads with per-prototype subpages.

### `CLAUDE.md`
- Update the folder-structure tree: the three new subpage `index.html` files, the new
  `assets/prototype-page.css`, and `AlignmentModuelHomingV2.1.png`.
- Add `assets/prototype-page.css` to the **shared-resources list** in the Tool spec standard
  section, next to `assets/deck.css` / `assets/deck.js` / `assets/fonts/` / `assets/gsap/`,
  with its one-line justification (three prototype subpages sharing one visual system, same
  precedent as the deck runtime).
- Update the Design system section to record that the prototype journey page carries
  **per-thread accent colours** — the pump thread orange, the alignment thread violet — and
  that this is scoped to that page via `--t-*` variables and does not change the global
  accent.

## Verification

- No file still claims the alignment module has no prototypes.
- No file still claims proto-02 is "in design".
- Every path written into `CLAUDE.md`'s tree actually exists on disk — check each one.
- `README.md` renders correctly on GitHub (table syntax intact).
