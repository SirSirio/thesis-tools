# Plan 10-02 — proto-01 subpage

**Wave:** 1 (parallel with 10-03, 10-04)
**Creates:** `prototypes/Prototype-1-Pump-Module/proto-01-5ul-4roller/index.html`
**Modifies:** nothing

## Goal

Move the proto-01 detail view out of `prototypes/index.html` into a standalone page, with
its content **materially unchanged**. This is a move, not a rewrite.

## Source

`prototypes/index.html`, `<div id="detail-proto-01" class="detail-section">` — approximately
lines 744–1015, eleven `.detail-block` sections. Read the exact range; do not trust the line
numbers blindly.

## Tasks

1. Build the page shell:
   - `<link rel="stylesheet" href="../../../assets/style.css">` then
     `<link rel="stylesheet" href="../../../assets/prototype-page.css">` — count the `../`
     carefully, this file is three levels below the repo root.
   - Background blobs and the `.tool-nav` back-link, matching the pattern used by other
     pages. Back-link goes to `../../index.html` and reads `← Prototype Design Space`.
   - `<title>Proto-01 · 5 µL 4-roller peristaltic — Prototype Design Space</title>`.
2. Wrap the content in `.proto-page` (pump accent is the default — no modifier class).
3. Add a `.proto-page-header`: eyebrow "Prototype 1 · Pump Module", the title, status chips
   carried over from the journey card, and a one-line summary.
4. Paste the eleven `.detail-block`s verbatim, then:
   - Fix every relative path that changed depth (images, video, links to `tools/…`, links to
     sibling `PROTOTYPE.md` files). **Every single one** — this is the highest-risk part.
   - Add `target="_blank" rel="noopener"` to links that point at tools (folds in the
     standing pending todo).
   - Wrap any table too wide to wrap in `.table-scroll`.
5. Carry the lightbox markup and its JS if proto-01's blocks use it; drop it if they do not.
6. Inline `<style>` only for rules genuinely unique to this page.

## Verification

- Open the file and confirm: no `vscode-cdn` paths, no absolute `file://` or `/`-rooted
  paths, every `src`/`href` resolves relative to the new location.
- Content diff against the original block: headings, tables, numbers and figures all present.
- No horizontal scroll at 320px.
- Renders readably with JavaScript disabled.
