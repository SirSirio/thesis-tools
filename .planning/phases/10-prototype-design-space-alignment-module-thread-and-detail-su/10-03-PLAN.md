# Plan 10-03 — proto-02 subpage

**Wave:** 1 (parallel with 10-02, 10-04)
**Creates:** `prototypes/Prototype-1-Pump-Module/proto-02-5ul-4roller-v2/index.html`
**Modifies:** nothing

## Goal

Move the proto-02 detail view — the largest block in the repo, ~1070 lines — out of
`prototypes/index.html` into a standalone page, content **materially unchanged**.

## Source

`prototypes/index.html`, `<div id="detail-proto-02" class="detail-section">` — approximately
lines 1016–2083. Read the exact range. It contains, all of which must survive the move:

- a generated table of contents
- `<details class="ver-panel">` version panels with version chips (`.ver-21/.ver-22/.ver-23`)
- inline SVG schematics
- KaTeX math
- gravimetric/flow bar graphics, equation chips, good/warn callouts
- the media block for `pump-head-web.mp4` + `pump-head-poster.jpg`

## Tasks

1. Page shell as in plan 10-02, three levels below root:
   `../../../assets/style.css`, `../../../assets/prototype-page.css`.
   Title: `Proto-02 · 5 µL 4-roller v2 — Prototype Design Space`.
2. **KaTeX**: the local fallback lives at `prototypes/katex/` (confirmed present:
   `katex.min.css`, `katex.min.js`, `auto-render.min.js`, `fonts/`). From this page that is
   `../../katex/…`. Carry the stylesheet link, both deferred scripts, and the `auto-render`
   invocation. Verify the delimiters config matches the original exactly, or the math breaks
   silently.
3. **Media**: `pump-head-web.mp4` and `pump-head-poster.jpg` are **already in this folder** —
   their references collapse to bare filenames. This is the payoff of co-locating.
4. **TOC**: carry the TOC-building script. Its selectors must still match after the move —
   if it scoped to `#detail-proto-02`, rescope it to the new root. Test that every TOC entry
   scrolls to a real heading.
5. **Version panels**: `<details>` elements and their chips must keep working, including the
   open/closed default state.
6. Fix every relative path that changed depth. Add `target="_blank" rel="noopener"` to tool
   links (folds in the standing pending todo).
7. Wrap wide tables in `.table-scroll`. This page has the most tables; check each one.
8. Inline `<style>` only for genuinely page-unique rules — the inline SVG schematics' own
   styling likely qualifies.

## Verification

- **KaTeX renders with the network off.** This is the single most important check: the site
  must work from a USB drive. Confirm the fonts directory resolves too.
- Every `<details>` opens and closes; every TOC link lands on its heading.
- The video plays and the poster shows before playback.
- No horizontal scroll at 320px, 640px, 1024px.
- Content diff against the original block — nothing dropped. This block is large enough that
  silent truncation is a real risk; count the `.detail-block` elements before and after.
