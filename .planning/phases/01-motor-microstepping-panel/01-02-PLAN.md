---
phase: 01-motor-microstepping-panel
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - tools/rotor-solver/index.html
autonomous: true
requirements: [MOTOR-01, MOTOR-02]
must_haves:
  truths:
    - "Each feasible roller-count row shows six motor columns: rollers in contact, steps/stroke, µL/step, torque rim (g), FoS, max step rate"
    - "FoS cell is green ≥ 2.0, amber 1.0–2.0, red < 1.0, with the numeric value always visible"
    - "Infeasible rows show — for all six motor columns"
    - "The Time column recalculates from the selected microstepping mode, not a hardcoded 6400"
    - "An RPM summary card appears alongside the existing summary cards and updates with step rate and microstepping"
  artifacts:
    - path: "tools/rotor-solver/index.html"
      provides: "motor reads in upd(), six motor columns in ROWS.map(), six new <th> headers, RPM card, dynamic Time column"
      contains: "maxStepRate"
  key_links:
    - from: "upd() motor reads (VOLT, MS, Mf, Tf)"
      to: "MS_FACTOR / TORQUE_FRAC constants (plan 01)"
      via: "object-key lookup by msSel value"
      pattern: "MS_FACTOR\\[MS\\]"
    - from: "motor column <td> cells"
      to: "ROWS.map() feasible flag"
      via: "ternary gating computed values vs — placeholder"
      pattern: "feasible\\s*\\?"
    - from: "FoS <span> class"
      to: ".ok / .warn / .no CSS"
      via: "threshold-selected class string"
      pattern: "FoS\\s*>=\\s*2"
---

<objective>
Wire the motor calculations into the existing `upd()` function and surface them in the UI: read the two new selects, compute the six per-row motor values inside the existing `ROWS.map()`, append six `<th>` headers (with EN/IT i18n keys), append `${motorCols}` to each table row, replace the hardcoded `6400` in the Time column with the dynamic steps-per-rev, and add the RPM summary card. This is the calculation-and-display core of the phase.

Purpose: MOTOR-01 (step rate already shared per D-03; columns surfaced), MOTOR-02 (six of the seven derived outputs displayed per roller count; the seventh — cumulative compression load range — is encoded in the FoS denominator and documented in plan 03's notes).
Output: A geometry table with six new motor columns, a dynamic Time column, and an RPM card, all driven by the shared `upd()` function.
</objective>

<execution_context>
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/workflows/execute-plan.md
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/01-motor-microstepping-panel/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add motor reads to upd(), make Time column dynamic, compute motor columns</name>
  <read_first>
    - tools/rotor-solver/index.html (upd() input reads lines 450-458; per-row calcs lines 474-498; totalSteps line 487; the constants/selects added by plan 01)
    - tools/rotor-solver/SPEC.md (Per-row motor calculations block; FoS colour thresholds; max step rate note; torque_rim formula with the ×10 factor)
    - .planning/phases/01-motor-microstepping-panel/01-PATTERNS.md (Section 3 — motor reads; Section 4 — Time column; Section 5 — motor column computations; Infeasible Row Guard Pattern)
    - .planning/phases/01-motor-microstepping-panel/01-RESEARCH.md (Max Step Rate Formula — Resolved; Pitfall 1 torque units; Pitfall 3 hardcoded 6400; Pitfall 4 infeasible rows)
  </read_first>
  <action>
    In `upd()`, immediately after the `const SP = +document.getElementById('spS').value;` line (line 458), add motor reads:
    `VOLT` = `+document.getElementById('voltSel').value`; `MS` = `document.getElementById('msSel').value` (string, no `+`); `Mf` = `MS_FACTOR[MS]`; `Tf` = `TORQUE_FRAC[MS]`; `stepsRev` = `MOTOR.stepsFullRev * Mf`; `maxStepRate` = `Math.round(VOLT / (2 * MOTOR.inductance * MOTOR.ratedCurrent))`.
    The max step rate formula is `f_max = V / (2 × L × I_rated)` — the inductive ceiling, microstepping-independent (12 V → 1333, 24 V → 2667 steps/s per RESEARCH.md).
    Replace the Time-column line `const totalSteps = strokes * (6400 / N);` (line 487) with `const totalSteps = strokes * (stepsRev / N);`. After this edit, no literal `6400` may remain anywhere in index.html (Pitfall 3).
    Inside the `ROWS.map(N => { ... })` callback, after the `why` block is defined (after line 498) and before the `return` template literal, compute the motor values:
    `rollersContact` = `Math.floor(N / 2)` (D-04); `stepsPerStroke` = `stepsRev / N` (D-05); `uLperStep` = `vol * N / stepsRev` (D-06); `torqueRim` = `feasible ? (MOTOR.holdingTorque * Tf / R * 10) : null` (D-07 — the `×10` converts g·cm/mm to g-force at rim, keep an inline comment, Pitfall 1); `FoS` = `feasible ? (torqueRim / (200 * rollersContact)) : null` (D-08); `FoSClass` = `FoS === null ? '' : FoS >= 2.0 ? 'ok' : FoS >= 1.0 ? 'warn' : 'no'`.
    Build a `motorCols` string: when `feasible`, six `<td>` cells in order — `rollersContact`, `stepsPerStroke.toFixed(1)`, `uLperStep.toFixed(3)`, `torqueRim.toFixed(0)`, `<td><span class="${FoSClass}">${FoS.toFixed(2)}</span></td>`, `maxStepRate`. When NOT feasible, six `<td>—</td>` cells (D-12).
    All motor `<td>` values are dynamic numbers — they get NO `data-i18n` attribute.
  </action>
  <verify>
    <automated>powershell -Command "$c = Get-Content 'tools/rotor-solver/index.html' -Raw; if ($c -notmatch '6400' -and $c -match 'MS_FACTOR\[MS\]' -and $c -match 'stepsRev\s*=\s*MOTOR\.stepsFullRev\s*\*\s*Mf' -and $c -match 'maxStepRate\s*=\s*Math\.round\(VOLT' -and $c -match 'Math\.floor\(N\s*/\s*2\)' -and $c -match 'MOTOR\.holdingTorque\s*\*\s*Tf\s*/\s*R\s*\*\s*10' -and $c -match 'FoS\s*>=\s*2\.0\s*\?\s*.ok' -and $c -match 'class=\"\$\{FoSClass\}\"') { 'PASS' } else { 'FAIL' }"</automated>
  </verify>
  <acceptance_criteria>
    - No literal `6400` remains anywhere in `tools/rotor-solver/index.html`
    - `upd()` reads `VOLT`, `MS`, `Mf`, `Tf`, computes `stepsRev = MOTOR.stepsFullRev * Mf` and `maxStepRate = Math.round(VOLT / (2 * MOTOR.inductance * MOTOR.ratedCurrent))`
    - `totalSteps` uses `stepsRev / N` (Time column now reflects selected microstepping)
    - The map callback computes `rollersContact`, `stepsPerStroke`, `uLperStep`, `torqueRim` (with `* 10`), `FoS`, and `FoSClass`
    - `FoSClass` selects `ok` for FoS ≥ 2.0, `warn` for 1.0 ≤ FoS < 2.0, `no` for FoS < 1.0
    - `motorCols` emits six `<td>—</td>` cells for infeasible rows and six computed cells for feasible rows
    - The FoS cell wraps its value in `<span class="${FoSClass}">`
  </acceptance_criteria>
  <done>Motor values compute correctly for feasible rows (FoS ~1.23 at 10 rollers full step per SPEC.md known values), show — for infeasible rows, and the Time column changes when microstepping mode changes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append six table headers (with i18n), wire motorCols into rows, add RPM card</name>
  <behavior>
    - Switching microstepping to full step changes the Time column and steps/stroke values
    - Changing step rate slider changes the RPM card value
    - Switching language to IT translates all six new column headers and the RPM card label
  </behavior>
  <read_first>
    - tools/rotor-solver/index.html (thead lines 322-333; return template literal lines 500-511; summary.innerHTML lines 516-521; LANG.en lines 363-378; LANG.it lines 399-413)
    - tools/rotor-solver/SPEC.md (Summary card RPM formula)
    - .planning/phases/01-motor-microstepping-panel/01-PATTERNS.md (Section 6 — th headers; Section 8 — RPM card; Section 9 — th/sum-rpm LANG keys)
  </read_first>
  <action>
    In the `<thead>` row, after the `<th data-i18n="th-feasible">` cell (line 332), append six header cells in column order with `data-i18n` keys and `<br>`-wrapped fallback text: `th-contact` ("Rollers<br>contact"), `th-steps-stroke` ("Steps /<br>stroke"), `th-ul-step` ("&micro;L /<br>step"), `th-torque-rim` ("Torque rim<br>(g)"), `th-fos` ("FoS &sup3;"), `th-max-step` ("Max step rate &sup4;<br>(steps/s)").
    In the `ROWS.map()` return template literal, append `${motorCols}` immediately before the closing `</tr>` (after the `<td>${why}</td>` line 510).
    Compute `RPM = (SP / stepsRev) * 60;` before the `summary.innerHTML` assignment (before line 516). Append one more `.mc` card to the end of the summary template literal, following the existing `.mc` markup: label `${L['sum-rpm']}`, value `${RPM.toFixed(1)}` with a `<small>` "RPM" unit.
    Add the i18n keys to BOTH LANG objects. In LANG.en (after `'th-feasible'` and `'sum-minr'` entries): `th-contact`='Rollers<br>contact', `th-steps-stroke`='Steps /<br>stroke', `th-ul-step`='μL /<br>step', `th-torque-rim`='Torque rim<br>(g)', `th-fos`='FoS ³', `th-max-step`='Max step rate ⁴<br>(steps/s)', `sum-rpm`='Rotor speed'.
    In LANG.it: `th-contact`='Rulli<br>contatto', `th-steps-stroke`='Step /<br>ciclo', `th-ul-step`='μL /<br>step', `th-torque-rim`='Coppia rim<br>(g)', `th-fos`='FoS ³', `th-max-step`='Step rate max ⁴<br>(steps/s)', `sum-rpm`='Velocità rotore'.
    Every one of the seven new keys MUST appear in BOTH LANG.en and LANG.it (Pitfall 6 — missing IT entries render blank).
  </action>
  <verify>
    <automated>powershell -Command "$c = Get-Content 'tools/rotor-solver/index.html' -Raw; $keys = @('th-contact','th-steps-stroke','th-ul-step','th-torque-rim','th-fos','th-max-step','sum-rpm'); $ok = $true; foreach ($k in $keys) { $n = ($c.Split([char]10) | Where-Object { $_ -match \"'$k'\" }).Count; if ($n -lt 2) { $ok = $false } }; if ($ok -and $c -match '\$\{motorCols\}' -and $c -match 'RPM\s*=\s*\(SP\s*/\s*stepsRev\)\s*\*\s*60' -and $c -match \"sum-rpm\") { 'PASS' } else { 'FAIL' }"</automated>
  </verify>
  <acceptance_criteria>
    - The `<thead>` row has six new `<th>` cells after `th-feasible`, with `data-i18n` keys `th-contact`, `th-steps-stroke`, `th-ul-step`, `th-torque-rim`, `th-fos`, `th-max-step` in that order
    - `${motorCols}` appears in the `ROWS.map()` return template literal before `</tr>`
    - `RPM = (SP / stepsRev) * 60` computed before the summary innerHTML assignment
    - A sixth `.mc` summary card uses `${L['sum-rpm']}` as its label and `RPM.toFixed(1)` as its value
    - All seven new keys (`th-contact`, `th-steps-stroke`, `th-ul-step`, `th-torque-rim`, `th-fos`, `th-max-step`, `sum-rpm`) appear in BOTH `LANG.en` and `LANG.it` (each key on exactly two lines)
    - Header cell count equals the per-row `<td>` count (10 existing + 6 motor = 16)
  </acceptance_criteria>
  <done>The table renders 16 columns; motor columns populate for feasible rows and show — otherwise; the RPM card updates with step rate and microstepping; IT mode translates every new header.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| computed values → innerHTML | Numeric results inserted into table/card HTML via template literals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | innerHTML of #tb and #summary | accept | All inserted motor values are numbers via `toFixed()` / `Math.round()` / integer arithmetic; no user-supplied strings reach innerHTML (RESEARCH.md Security Domain verdict: low risk) |
| T-02-02 | Information Disclosure | FoS / torque values shown for infeasible rows | mitigate | Motor columns gated on `feasible` flag; infeasible rows render `—` (D-12), avoiding misleading data |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — pure inline edit |
</threat_model>

<verification>
- Open the tool; the table shows 16 columns. Set 10 rollers, full step: FoS ≈ 1.23 (amber) matching SPEC.md known values.
- Set 10 rollers, 1/8 step: FoS ≈ 0.43 (red), still readable (numeric value visible).
- Switch microstepping full ↔ 1/32: Time column and steps/stroke change; max step rate stays constant per voltage.
- Switch voltage 12 ↔ 24: max step rate changes 1333 ↔ 2667.
- Move step rate slider: RPM card updates.
- Switch to IT: all six new headers and the RPM card label translate.
- `grep` confirms `6400` no longer present in index.html.
</verification>

<success_criteria>
- Six motor columns render per feasible row in the documented order; infeasible rows show —
- FoS traffic light uses ok/warn/no thresholds with the numeric value always visible
- Time column is dynamic (no hardcoded 6400); RPM card present and live
- All new headers + RPM label translated in EN and IT
- Single file modified: tools/rotor-solver/index.html
</success_criteria>

<output>
Create `.planning/phases/01-motor-microstepping-panel/01-02-SUMMARY.md` when done
</output>
