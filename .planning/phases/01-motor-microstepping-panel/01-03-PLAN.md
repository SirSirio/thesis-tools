---
phase: 01-motor-microstepping-panel
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - tools/rotor-solver/index.html
autonomous: true
requirements: [MOTOR-02, MOTOR-03]
must_haves:
  truths:
    - "The notes section documents the motor model and its hard-coded constants"
    - "The notes section explains the FoS formula, its colour thresholds, and the torque-at-rim formula"
    - "The notes section documents the max step rate formula and its 12 V / 24 V values"
    - "The notes section documents the cumulative compression load range (50–200 g/roller) covering MOTOR-02's seventh output"
    - "The Time footnote no longer hardcodes 1⁄32 microstepping or 6400 steps/rev"
  artifacts:
    - path: "tools/rotor-solver/index.html"
      provides: "updated LANG.en.note and LANG.it.note with motor footnotes ³⁴, motor constants block, compression-load range; no hardcoded 1⁄32/6400"
      contains: "Wantai 42BYGHW811"
  key_links:
    - from: "FoS column footnote ³"
      to: "th-fos header marker ³"
      via: "matching superscript reference"
      pattern: "³"
    - from: "max step rate footnote ⁴"
      to: "th-max-step header marker ⁴"
      via: "matching superscript reference"
      pattern: "⁴"
---

<objective>
Complete the on-page documentation (D-13). Update both `LANG.en.note` and `LANG.it.note` to: remove the hardcoded "1⁄32 microstepping, 6400 steps/rev" phrasing from the Time footnote (it is now dynamic), add footnote ³ explaining FoS and the torque-at-rim formula, add footnote ⁴ explaining the max step rate formula, append a motor constants reference block (Wantai 42BYGHW811 / DRV8825 datasheet values and the torque-fraction table), and document the cumulative compression load range (50–200 g/roller, 200 g worst-case) that backs the FoS denominator.

Purpose: MOTOR-02 (the seventh derived output — estimated cumulative compression load range — is delivered as documented context tied to the FoS column, per the RESEARCH.md open-question recommendation); MOTOR-03 (constants shown on-page as reference, hard-coded, no editable fields). Documenting every formula shown to the user is a locked requirement (D-13).
Output: Fully documented notes block in both languages, consistent with the six motor columns and the dynamic Time column.
</objective>

<execution_context>
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/workflows/execute-plan.md
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/01-motor-microstepping-panel/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update EN + IT note strings with motor footnotes and compression-load range</name>
  <read_first>
    - tools/rotor-solver/index.html (LANG.en.note line 382; LANG.it.note line 418; the static note paragraph line 340)
    - tools/rotor-solver/SPEC.md (Worst-case compression load basis: 50–200 g/roller, 200 g worst-case, ~100 g typical; FoS thresholds; torque_rim formula; max step rate note)
    - .planning/phases/01-motor-microstepping-panel/01-PATTERNS.md (Section 10 — notes block extension; the exact append pattern)
    - .planning/phases/01-motor-microstepping-panel/01-RESEARCH.md (Max Step Rate — values and labeling; Pitfall 5 — remove 1⁄32/6400; Open Questions — compression load range coverage)
  </read_first>
  <action>
    In tools/rotor-solver/index.html, edit the `'note'` value inside LANG.en (line 382). In the `² Dispense time` footnote, replace the phrase "1⁄32 microstepping, 6400 steps/rev" with "at the selected microstepping mode" (the note is a static LANG string and cannot read the live msSel value, so use generic phrasing — Pitfall 5).
    Then append (concatenated to the same note string, using `<br>` separators and the existing `<b>...</b>` label style) the following footnotes and reference block:
    Footnote ³ — FoS: "Factor of Safety (FoS) = torque at rim ÷ (200 g × rollers in contact). Green ≥ 2.0 · amber 1.0–2.0 · red < 1.0. Torque at rim (g) = 4800 × torque_fraction ÷ R_mm × 10."
    Footnote ⁴ — Max step rate: "Max step rate (inductive ceiling, theoretical) = V_supply ÷ (2 × L × I_rated). At 12 V → 1333 steps/s; at 24 V → 2667 steps/s. DRV8825 chopper regulation enables higher practical rates."
    Compression load (covers MOTOR-02 seventh output): "Compression load (estimated, 0.51 mm soft PVC tube): 50–200 g per roller; worst-case per row = 200 g × rollers in contact (the FoS denominator); typical ≈ 100 g × rollers in contact."
    Motor reference block: "Motor: Wantai 42BYGHW811 — 200 steps/rev (1.8°), 4800 g·cm holding torque, 2.5 A/phase, 1.25 Ω, 1.8 mH. Driver: DRV8825. Torque fractions: full=1.00, 1/2=0.70, 1/4=0.50, 1/8=0.35, 1/16=0.20, 1/32=0.10."
    Use HTML entities consistent with the existing note string (`&lt;` for `<`, `&divide;` / `&times;` / `&minus;` where the existing string uses them). Keep `³` and `⁴` superscripts matching the `th-fos` (³) and `th-max-step` (⁴) header markers from plan 02.
    Apply the same set of changes to the LANG.it.note value (line 418): replace "microstepping 1/32, 6400 step/giro" with "al modo di microstepping selezionato", and append the Italian translations of footnotes ³, ⁴, the compression-load range, and the motor reference block. Keep formulas, numbers, units, and the Wantai 42BYGHW811 model name identical (numeric content is language-neutral).
    After the edit, no occurrence of "6400" or "1⁄32 microstepping" / "1/32, 6400" may remain in either note string.
  </action>
  <verify>
    <automated>powershell -Command "$c = Get-Content 'tools/rotor-solver/index.html' -Raw; $en = ($c.Split([char]10) | Where-Object { $_ -match \"Wantai 42BYGHW811\" }).Count; if ($c -notmatch '6400' -and $c -match 'Factor of Safety' -and $c -match 'inductive ceiling' -and $c -match '1333 steps/s' -and $c -match '50.{0,4}200 g' -and ($c -match 'Coppia' -or $c -match 'al modo di microstepping') -and $en -ge 2) { 'PASS' } else { 'FAIL' }"</automated>
  </verify>
  <acceptance_criteria>
    - Neither `LANG.en.note` nor `LANG.it.note` contains the string `6400`, and neither contains the hardcoded "1⁄32 microstepping" / "1/32, 6400 step" phrase
    - `LANG.en.note` contains the FoS footnote with the formula `torque at rim ÷ (200 g × rollers in contact)` and the thresholds green ≥ 2.0 / amber 1.0–2.0 / red < 1.0
    - `LANG.en.note` contains the max step rate footnote with `V_supply ÷ (2 × L × I_rated)` and the 1333 / 2667 steps/s values
    - Both note strings document the compression load range 50–200 g per roller and the 200 g worst-case (MOTOR-02 seventh output)
    - Both note strings contain the motor reference block naming "Wantai 42BYGHW811" with the torque-fraction table
    - Footnote markers ³ and ⁴ in the notes match the `th-fos` (³) and `th-max-step` (⁴) header markers
    - `assets/style.css` unchanged; no new files created
  </acceptance_criteria>
  <done>The notes block fully documents every motor formula and constant shown to the user, in both EN and IT, with the compression load range covering MOTOR-02's seventh output and no stale 6400/1⁄32 references.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LANG note string → innerHTML | Static documentation HTML set via applyLang innerHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | note innerHTML | accept | Note value is an author-controlled static string in the LANG object; no user input flows into it |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — documentation-only edit |
</threat_model>

<verification>
- Open the tool; the notes block lists the Wantai 42BYGHW811 constants, the torque-fraction table, the FoS formula + thresholds, the max step rate formula + values, and the 50–200 g/roller compression range.
- The Time footnote reads "at the selected microstepping mode" (no 1⁄32 / 6400).
- Switch to IT: the notes block is fully translated; formulas, numbers, and the motor model name are unchanged.
- `grep` confirms `6400` is absent from index.html.
</verification>

<success_criteria>
- Both note strings document motor model, constants, torque-fraction table, FoS formula + thresholds, max step rate formula + values, and compression load range
- Time footnote no longer hardcodes 1⁄32 / 6400
- Footnote markers ³⁴ match the header markers
- EN and IT both updated; numeric/formula content language-neutral
- Single file modified: tools/rotor-solver/index.html
</success_criteria>

<output>
Create `.planning/phases/01-motor-microstepping-panel/01-03-SUMMARY.md` when done
</output>
