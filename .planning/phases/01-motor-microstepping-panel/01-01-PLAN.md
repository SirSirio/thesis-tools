---
phase: 01-motor-microstepping-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/rotor-solver/index.html
autonomous: true
requirements: [MOTOR-01, MOTOR-03, MOTOR-04]
must_haves:
  truths:
    - "User sees a Supply voltage selector (12 V / 24 V) in the Parameters panel"
    - "User sees a Microstepping dropdown (full / 1/2 / 1/4 / 1/8 / 1/16 / 1/32) in the Parameters panel"
    - "Changing either new selector triggers a recalculation (upd) without page reload"
    - "Motor constants are hard-coded; no editable motor spec field exists"
  artifacts:
    - path: "tools/rotor-solver/index.html"
      provides: "voltSel + msSel select inputs, MOTOR/MS_FACTOR/TORQUE_FRAC constants, .warn CSS, event listeners"
      contains: "id=\"voltSel\""
  key_links:
    - from: "voltSel / msSel selects"
      to: "upd()"
      via: "addEventListener('change', upd)"
      pattern: "voltSel.*msSel"
    - from: "msSel option value strings"
      to: "MS_FACTOR / TORQUE_FRAC keys"
      via: "exact key match (full, 1/2, 1/4, 1/8, 1/16, 1/32)"
      pattern: "MS_FACTOR"
---

<objective>
Lay the foundation for the motor panel: add the two new motor inputs (supply voltage, microstepping) to the existing Parameters panel, define the hard-coded motor constants block at script scope, add the `.warn` amber CSS class for the FoS traffic light, and wire the new inputs to the existing `upd()` update function. This plan establishes the contracts (option value strings, constant keys, CSS class) that plan 02 consumes — it does NOT yet compute or display any motor outputs.

Purpose: MOTOR-01 (voltage + microstepping inputs), MOTOR-03 (hard-coded constants, no editable fields), MOTOR-04 (all logic inline in the single file).
Output: Two working selectors that trigger `upd()`, a `MOTOR`/`MS_FACTOR`/`TORQUE_FRAC` constants block, a `.warn` CSS rule, and their i18n labels in EN + IT.
</objective>

<execution_context>
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/workflows/execute-plan.md
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add voltage + microstepping selects and their i18n labels</name>
  <read_first>
    - tools/rotor-solver/index.html (current state of the .ctrl grid lines 288-312, select pattern lines 292-302, LANG.en lines 349-384, LANG.it lines 385-421)
    - tools/rotor-solver/SPEC.md (Inputs table — voltSel and msSel rows; microstepping factors table)
    - .planning/phases/01-motor-microstepping-panel/01-PATTERNS.md (Section 1 — select inputs; Section 9 — label-volt / label-ms LANG keys)
  </read_first>
  <action>
    In tools/rotor-solver/index.html, inside the `.ctrl` grid (the `<div class="ctrl">` block, after the Step rate `.cg` block that ends near line 311), add two new `.cg` blocks following the exact `idSel`/`bSel` select markup pattern.
    First block: `<label data-i18n="label-volt">` and `<select id="voltSel">` with two options: `<option value="12" selected="selected">12 V</option>` and `<option value="24">24 V</option>`.
    Second block: `<label data-i18n="label-ms">` and `<select id="msSel">` with six options whose `value` attributes are the EXACT strings `full`, `1/2`, `1/4`, `1/8`, `1/16`, `1/32`. Option display text: `Full step`, `1/2`, `1/4`, `1/8`, `1/16`, `1/32`. Set `selected="selected"` on the `1/8` option (the SPEC.md default for `msSel`).
    The option `value` strings MUST match the keys that will be defined in MS_FACTOR and TORQUE_FRAC in Task 2 — `full`, `1/2`, `1/4`, `1/8`, `1/16`, `1/32`. A mismatch causes silent undefined in motor calculations (Pitfall 2 in RESEARCH.md).
    Then add the i18n keys to BOTH language objects. In LANG.en (after `'label-sp'`): `'label-volt': 'Supply voltage'` and `'label-ms': 'Microstepping'`. In LANG.it (after `'label-sp'`): `'label-volt': 'Tensione alimentazione'` and `'label-ms': 'Microstepping'`.
    Do NOT add any other i18n keys in this task — table-header and summary keys belong to plan 02.
  </action>
  <verify>
    <automated>powershell -Command "$c = Get-Content 'tools/rotor-solver/index.html' -Raw; if ($c -match 'id=\"voltSel\"' -and $c -match 'id=\"msSel\"' -and ($c | Select-String -Pattern 'value=\"1/8\" selected' -Quiet) -and ($c.Split([char]10) | Where-Object { $_ -match \"'label-volt'\" }).Count -eq 2 -and ($c.Split([char]10) | Where-Object { $_ -match \"'label-ms'\" }).Count -eq 2) { 'PASS' } else { 'FAIL' }"</automated>
  </verify>
  <acceptance_criteria>
    - `tools/rotor-solver/index.html` contains a `<select id="voltSel">` with exactly two options whose values are `12` and `24`, with `12` marked `selected`
    - `tools/rotor-solver/index.html` contains a `<select id="msSel">` with six options whose values are exactly `full`, `1/2`, `1/4`, `1/8`, `1/16`, `1/32`, with `1/8` marked `selected`
    - The voltage label uses `data-i18n="label-volt"` and the microstepping label uses `data-i18n="label-ms"`
    - Both `'label-volt'` and `'label-ms'` keys exist in BOTH `LANG.en` and `LANG.it` (key appears on exactly two lines each)
    - No new files created; only `tools/rotor-solver/index.html` modified
  </acceptance_criteria>
  <done>Two new selects render in the Parameters panel with translated labels; option value strings exactly match the constant keys planned for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Add motor constants block, .warn CSS class, and event listeners</name>
  <read_first>
    - tools/rotor-solver/index.html (constants region lines 438-445; .ok/.no CSS lines 249-251; event listener block lines 524-528)
    - tools/rotor-solver/SPEC.md (Motor constants table; Microstepping factors table — confirm values)
    - .planning/phases/01-motor-microstepping-panel/01-PATTERNS.md (Section 2 — constants block; Section 7 — .warn CSS; Event Listener Pattern)
  </read_first>
  <action>
    In tools/rotor-solver/index.html, immediately after the `const TUBE_AVAIL = 300;` line (line 440), add three script-scope `const` declarations (NOT inside `upd()` — declaring them inside redeclares on every event, see Anti-Patterns in RESEARCH.md):
    `MOTOR` object with keys `stepsFullRev: 200`, `holdingTorque: 4800`, `ratedCurrent: 2.5`, `resistance: 1.25`, `inductance: 0.0018`.
    `MS_FACTOR` object: `{ 'full':1, '1/2':2, '1/4':4, '1/8':8, '1/16':16, '1/32':32 }`.
    `TORQUE_FRAC` object: `{ 'full':1.00, '1/2':0.70, '1/4':0.50, '1/8':0.35, '1/16':0.20, '1/32':0.10 }`.
    The object keys MUST be the exact strings `full`, `1/2`, `1/4`, `1/8`, `1/16`, `1/32` to match the msSel option values from Task 1.
    In the inline `<style>` block, immediately after the `.no { ... }` rule (line 251), add the rule `.warn { color: #e8a020; font-weight: 600; }`.
    Add the two new selects to the existing change-listener array. Change the line `['idSel', 'bSel'].forEach(id =>` (line 526) to `['idSel', 'bSel', 'voltSel', 'msSel'].forEach(id =>` so both new selects call `upd` on change. Do NOT alter the `['occS','bossS','spS']` input-listener array or the `volN` listener.
  </action>
  <verify>
    <automated>powershell -Command "$c = Get-Content 'tools/rotor-solver/index.html' -Raw; if ($c -match 'stepsFullRev:\s*200' -and $c -match 'holdingTorque:\s*4800' -and $c -match 'inductance:\s*0\.0018' -and $c -match \"'1/32':32\" -and $c -match \"'1/32':0\.10\" -and $c -match '\.warn\s*\{\s*color:\s*#e8a020' -and $c -match \"'idSel',\s*'bSel',\s*'voltSel',\s*'msSel'\") { 'PASS' } else { 'FAIL' }"</automated>
  </verify>
  <acceptance_criteria>
    - A `const MOTOR` object exists at script scope (sibling of `ROWS`/`TUBE_AVAIL`, not inside `upd()`) with `stepsFullRev:200`, `holdingTorque:4800`, `ratedCurrent:2.5`, `resistance:1.25`, `inductance:0.0018`
    - A `const MS_FACTOR` object maps `full→1, 1/2→2, 1/4→4, 1/8→8, 1/16→16, 1/32→32`
    - A `const TORQUE_FRAC` object maps `full→1.00, 1/2→0.70, 1/4→0.50, 1/8→0.35, 1/16→0.20, 1/32→0.10`
    - The inline `<style>` block contains `.warn { color: #e8a020; font-weight: 600; }`
    - The change-listener array is `['idSel', 'bSel', 'voltSel', 'msSel']`
    - `assets/style.css` is unchanged (verify no edit to that file)
  </acceptance_criteria>
  <done>Motor constants are defined once at script scope, the amber FoS class is available, and both selectors trigger `upd()` on change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → DOM select | User chooses one of fixed voltage / microstepping options |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | voltSel / msSel values fed to arithmetic | accept | Values are fixed dropdown options coerced via `+value` / object-key lookup; no free-text input, no innerHTML injection of user strings |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — pure inline HTML/CSS/JS edit (RESEARCH.md Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- Open tools/rotor-solver/index.html in a browser; Parameters panel shows Supply voltage and Microstepping selectors styled like the existing dropdowns.
- Switching language to IT shows "Tensione alimentazione" for the voltage label.
- `grep` confirms `.warn` exists only in the inline `<style>` of index.html and not in assets/style.css.
- No console errors on load (constants parse, listeners attach).
</verification>

<success_criteria>
- voltSel (12/24) and msSel (full–1/32, default 1/8) render in the Parameters panel with EN/IT labels
- MOTOR, MS_FACTOR, TORQUE_FRAC constants defined at script scope with exact values from SPEC.md
- `.warn` amber class added inline; assets/style.css untouched
- Both new selects call `upd()` on change
- Single file modified: tools/rotor-solver/index.html
</success_criteria>

<output>
Create `.planning/phases/01-motor-microstepping-panel/01-01-SUMMARY.md` when done
</output>
