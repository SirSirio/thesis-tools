---
phase: 08-dispense-choreography-throughput-simulator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/dispense-throughput-simulator/index.html
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08]
must_haves:
  truths:
    - "User can enter 1..6 liquids with per-liquid volumes; line length N = number of liquids entered (D-02)"
    - "User sees a total-dose warning when the sum of volumes reaches 2000 µL (D-02)"
    - "User can set global flow params (rollers, µL/stroke, RPM), a concurrency slider 1..N with A1/A2 endpoint markers, and a lockstep(A1)/independent(A2) mode toggle (D-03, D-04)"
    - "The engine reproduces the pinned benchmark exactly: serial 16.6667 s/sample, pipelined ceiling 10 s, full totals 583.33 s @ K=1 and 376.67 s @ K=4 (delta 206.67 s) (D-01, D-05, D-06, D-07)"
    - "Degenerate inputs (RPM/rollers/µL-per-stroke = 0 or blank) are clamped, never producing NaN/Infinity cards"
  artifacts:
    - path: "tools/dispense-throughput-simulator/index.html"
      provides: "Tool shell, input panel, and the simulateSchedule scheduling engine"
      contains: "function simulateSchedule"
      min_lines: 250
  key_links:
    - from: "recompute()"
      to: "simulateSchedule()"
      via: "single-source-of-truth call on every input event"
      pattern: "simulateSchedule\\("
    - from: "input/change listeners"
      to: "recompute()"
      via: "addEventListener wiring (rotor-solver precedent)"
      pattern: "addEventListener\\('(input|change)'"
---

<objective>
Create the new tool page `tools/dispense-throughput-simulator/index.html` with its page shell, the full input panel (up to 6 liquids, global flow params, concurrency slider 1..N with A1/A2 endpoint markers, control-mode toggle, fixed 1 s shift / 5 s rack-change constants), and the single-source-of-truth scheduling engine (`computeDoseTimes` → `groupDispenseTime` LPT → `simulateSchedule`) that reproduces the pinned benchmark EXACTLY.

Purpose: This is the computational heart of the U5 (concurrency) decider. Every downstream output (metric cards, Gantt, animation) reads from this one engine, so it must be correct and benchmark-faithful before any visualization is built. Implements D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08.

Output: A working tool page whose engine, when fed the benchmark preset, computes serial 16.6667 s/sample, pipelined ceiling 10 s, and full totals 583.33 s (K=1) / 376.67 s (K=4).
</objective>

<execution_context>
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/workflows/execute-plan.md
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dispense-choreography-throughput-simulator-tool-interactive-/08-CONTEXT.md
@.planning/phases/08-dispense-choreography-throughput-simulator-tool-interactive-/08-RESEARCH.md
@.planning/phases/08-dispense-choreography-throughput-simulator-tool-interactive-/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Page shell + input panel + control CSS</name>
  <files>tools/dispense-throughput-simulator/index.html</files>
  <read_first>
    - tools/rotor-solver/index.html (lines 77-180 .ctrl/.cg/.v controls grid + number/range input styling; lines 305-313 .tool-nav/.tool-main/.tool-header/.bg-blobs shell; lines 668-673 setRangeFill helper with the --fill custom property trick)
    - tools/thesis-timeline/index.html (lines 397-400 the simpler no-i18n `← All tools` nav bar block — use THIS nav pattern, not rotor-solver's i18n one)
    - .planning/phases/08-dispense-choreography-throughput-simulator-tool-interactive-/08-PATTERNS.md (Analog A section — controls grid, nav, range-fill excerpts)
    - .planning/phases/08-dispense-choreography-throughput-simulator-tool-interactive-/08-RESEARCH.md (Recommended Project Structure; Pattern 1)
  </read_first>
  <action>
    Create `tools/dispense-throughput-simulator/index.html` as a self-contained page: `<head>` links `../../assets/style.css` (import only, no edits), sets title "Dispense Choreography & Throughput Simulator", includes the `.bg-blobs` block, the no-i18n `.tool-nav` (`← All tools` → `../../index.html`, span "Dispense Choreography & Throughput Simulator"), and a `.tool-main` > `.tool-header` (site-label "Design Tool · Peristaltic Dispensing Line", h1, one-line description referencing the 6-nozzle line / 32-sample rack / U5). English only — do NOT add the `data-i18n` system.
    Build ONE input `.glass-panel` (`.panel-title` "Configuration") with a `.ctrl` grid containing: (a) a liquids sub-panel of up to 6 rows, each row a `.cg` with a liquid label and a volume `<input type="number" id="vol0".."vol5">` in µL, plus Add/Remove-liquid buttons enforcing 1..6 rows (start with the 4 benchmark rows 600/200/175/25); (b) global flow-param `.cg` number inputs `rollers` (default 4), `uLPerStroke` (default 5), `rpm` (default 180); (c) a concurrency `<input type="range" id="concK" min="1" step="1">` whose `max` = current liquid count N, default value 1, with visible endpoint markers labelling `1 = A1 (serial / shared bus)` and `N = A2 (full parallel)` (D-03); (d) a control-mode toggle `<select id="mode">` or two-button toggle with options `a1` (lockstep / shared-bus) and `a2` (independent-rate) (D-04); (e) read-only constant chips showing "Sample shift 1 s" and "Rack change 5 s" (D-08 — these are fixed constants `SAMPLE_SHIFT=1`, `RACK_CHANGE=5`, `RACKS=4`, `SAMPLES=32` declared at script scope, not editable). Re-declare all tool-specific CSS (`.ctrl`, `.cg`, `.v`, range `--fill` fill-track, `.glass-panel`, `.panel-title`) inline in a `<style>` block adapted from rotor-solver — do NOT add classes to the shared stylesheet. Add empty placeholder containers `<div id="metrics">`, `<div id="gantt">`, `<div id="rackAnim">` for Plan 08-02 to populate (leave empty here). Stagger `.animate-in`/`--delay` on major sections per rotor-solver's cadence.
  </action>
  <verify>
    <automated>bash -c 'f=tools/dispense-throughput-simulator/index.html; grep -q "../../assets/style.css" "$f" && grep -q "All tools" "$f" && grep -Eq "id=.?vol0" "$f" && grep -Eq "id=.?concK" "$f" && grep -Eq "id=.?mode" "$f" && grep -Eq "id=.?(metrics|gantt|rackAnim)" "$f" && echo SHELL_OK'</automated>
    <automated>bash -c 'f=tools/dispense-throughput-simulator/index.html; if grep -q "data-i18n" "$f"; then echo "FAIL: i18n must not be present"; exit 1; fi; echo NO_I18N_OK'</automated>
    <human-check>Serve with `.\serve.bat`, open the tool: benchmark defaults (4 liquids 600/200/175/25, rollers 4, µL/stroke 5, RPM 180) are prefilled; the slider max equals the liquid count; A1/A2 endpoint markers and the mode toggle are visible; no horizontal scroll at 1280px or 375px.</human-check>
  </verify>
  <acceptance_criteria>
    - File `tools/dispense-throughput-simulator/index.html` exists and imports `../../assets/style.css` unchanged.
    - Input panel exposes: `vol0..vol{N-1}` volume fields (1..6 rows, defaults 600/200/175/25), `rollers`/`uLPerStroke`/`rpm` (defaults 4/5/180), `concK` range (min 1, max = liquid count), and `mode` toggle (a1/a2).
    - Fixed constants `SAMPLE_SHIFT=1`, `RACK_CHANGE=5`, `RACKS=4`, `SAMPLES=32` are declared at script scope and shown as read-only chips (D-08).
    - Empty `#metrics`, `#gantt`, `#rackAnim` containers exist for Plan 08-02.
    - No `data-i18n` attributes anywhere; no edits to `assets/style.css`.
  </acceptance_criteria>
  <done>Tool page renders with a complete, benchmark-prefilled input panel in the site's dark glassmorphic theme; downstream render containers exist but are empty.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scheduling engine + input validation (benchmark-faithful)</name>
  <files>tools/dispense-throughput-simulator/index.html</files>
  <read_first>
    - tools/dispense-throughput-simulator/index.html (the file from Task 1 — add the `<script>` engine)
    - tools/rotor-solver/index.html (lines 668-673 setRangeFill; lines 728-737 feasibility "show, don't hide" precedent for the NaN/clamp guard; lines 805-810 event-listener wiring)
    - tools/system-architecture-explorer/index.html (lines 1261-1287 pinsOf() single-source-of-truth breakdown-function pattern to mirror)
    - .planning/phases/08-dispense-choreography-throughput-simulator-tool-interactive-/08-RESEARCH.md (The Scheduling / Timing Engine section — flow-rate derivation, LPT grouping, fill/steady/drain `M+(N-1)` decomposition, rack-change convention, the A1/A2 landmine, and the "Recommended implementation shape" JS skeleton)
    - .planning/phases/08-dispense-choreography-throughput-simulator-tool-interactive-/engine-harness.cjs (the verification scaffold; note the top-level-function / column-0-closing-brace requirement it documents)
  </read_first>
  <behavior>
    Given the benchmark inputs (volumes [600,200,175,25], rollers 4, µL/stroke 5, RPM 180, N=4, M=32, shift 1 s, rack 5 s, 3 rack changes):
    - `computeDoseTimes([600,200,175,25],4,5,180)` → [10, 3.3333, 2.9167, 0.4167] (within 0.01)
    - serial sum (K=1) of doseTimes = 16.6667 s/sample
    - pipelined ceiling max(doseTimes) = 10 s (the 600 µL station)
    - `groupDispenseTime([10,3.3333,2.9167,0.4167],2)` = 12.9167 (LPT: {10,3.33}+{2.92,0.42})
    - `simulateSchedule(dt,4,32,1,1,5,3).totalRunTime` = 583.33 s (K=1 / A1 endpoint)
    - `simulateSchedule(dt,4,32,4,1,5,3).totalRunTime` = 376.67 s (K=4 / A2 endpoint)
    - delta = 583.33 − 376.67 = 206.67 s
    - Edge: rpm=0 or rollers=0 or uLPerStroke=0 → recompute() clamps to minimums (rpm≥1, rollers≥1, uLPerStroke>0), engine returns finite numbers, no NaN/Infinity
    - Edge: total volume ≥ 2000 µL → a `feasible=false` / warning flag is set (D-02), outputs still computed and shown
  </behavior>
  <action>
    Add a `<script>` with THREE pure functions declared at the top level of the script (each closing brace at column 0 so they stay independently testable — do NOT nest them inside an IIFE or DOMContentLoaded handler): (1) `computeDoseTimes(liquids, rollers, uLPerStroke, rpm)` — `strokesPerSec = (rpm/60)*rollers`; return `liquids.map(v => Math.ceil(v/uLPerStroke)/strokesPerSec)` (D-06 stroke quantization). (2) `groupDispenseTime(activeDoseTimes, K)` — LPT: copy, `sort((a,b)=>b-a)` with an original-index stable tiebreaker (RESEARCH Pitfall 3), then walk `for(i=0;i<sorted.length;i+=K)` summing `Math.max(...sorted.slice(i,i+K))` (Claude's-Discretion grouping resolved to LPT). (3) `simulateSchedule(doseTimes, N, M, K, sampleShift, rackChangeSec, numRackChanges)` — the unified cycle loop from RESEARCH: `totalCycles = M + N - 1` (NOT `M + 2(N-1)` — the fill/steady/drain phases emerge from `i = c - j + 1`, active iff `1<=i<=M`); per cycle `dispensePhase = groupDispenseTime(activeTimes, K)`, `cycleTime = dispensePhase + (active? sampleShift : 0)`; then `total += numRackChanges * rackChangeSec` as flat additive overhead (D-05, D-07). Return `{ cycles, totalRunTime, bottleneck, samplesPerHour }` where `bottleneck` = index/label of max doseTime and its per-cycle seconds, so every downstream consumer reads this one object (system-architecture-explorer `pinsOf()` precedent). Use `numRackChanges = RACKS - 1 = 3` (rack change does NOT precede rack 1 — Discretion resolved; document in SPEC).
    Add `recompute()` that: reads all input fields; CLAMPS degenerate values (rpm≥1, rollers≥1, uLPerStroke>0, volume≥0) and sets a `feasible`/`warn` flag with an inline warning rather than rendering NaN (rotor-solver "show don't hide" precedent); computes N = liquid count and updates `concK.max`; calls `computeDoseTimes` then `simulateSchedule` at the user's K; also computes the D-02 total-dose-<2000 µL warning. For this plan, have `recompute()` stash the result on `window.__sim` and write `totalRunTime` into `#metrics` as plain text (Plan 08-02 replaces this with the four cards). Wire every volume field + `rollers`/`uLPerStroke`/`rpm` + `concK` via `addEventListener('input', recompute)` and `mode` via `addEventListener('change', recompute)`; call `setRangeFill(concK)` inside `recompute()`; call `recompute()` once on load. Keep the A1/A2 mode toggle numerically honest per RESEARCH: at fixed K under global params both modes compute identically — do NOT invent a lockstep penalty here (Plan 08-02 adds the teaching copy).
  </action>
  <verify>
    <automated>bash -c 'cd "D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page" && node .planning/phases/08-dispense-choreography-throughput-simulator-tool-interactive-/engine-harness.cjs core'</automated>
    <automated>bash -c 'f=tools/dispense-throughput-simulator/index.html; grep -Eq "M *[+] *N *- *1|M *[+] *\\(N *- *1\\)" "$f" && ! grep -Eq "2 *\\* *\\(N *- *1\\)|2\\*\\(N-1\\)" "$f" && echo CYCLE_FORMULA_OK'</automated>
    <automated>bash -c 'f=tools/dispense-throughput-simulator/index.html; grep -Eq "addEventListener\\(.(input|change)." "$f" && echo WIRING_OK'</automated>
    <human-check>Serve the page: at K=1 the stashed total reads ~583 s; drag the slider to N and it drops to ~377 s, updating live; clear the RPM field and the page shows an inline warning instead of NaN/blank.</human-check>
  </verify>
  <acceptance_criteria>
    - `node engine-harness.cjs core` prints `ENGINE CORE OK` with serial=16.6667, ceiling=10.00, K1=583.33, K4=376.67 (extracted from the executor's actual functions).
    - `computeDoseTimes`, `groupDispenseTime`, `simulateSchedule` are top-level functions with closing brace at column 0; `simulateSchedule` returns `{cycles, totalRunTime, bottleneck, samplesPerHour}`.
    - `totalCycles === M + N - 1` (no `2*(N-1)` total-cycle formula present).
    - `recompute()` clamps rpm/rollers/uLPerStroke and never writes `NaN`/`Infinity`; total-dose ≥ 2000 µL sets a warning flag (D-02).
    - `recompute()` is wired to every input via `addEventListener` and called once on load.
  </acceptance_criteria>
  <done>The engine reproduces both pinned benchmark numbers and the RESEARCH-verified full totals exactly, is a single source of truth for all downstream outputs, and is robust to degenerate input.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user number inputs → engine | Untrusted numeric values (volumes, rollers, µL/stroke, RPM, concurrency) cross into `computeDoseTimes`/`simulateSchedule` |

This tool is a pure client-side static calculator: no server, no network, no auth, no persistence. Classic web threats (XSS, CSRF, injection, SSRF) have NO attack surface here and are N/A — the page renders no untrusted HTML and makes no requests.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Denial of Service (of the tool's own UI) | `computeDoseTimes`/`recompute` with rpm=0, rollers=0, or µL/stroke=0 | mitigate | Clamp to minimums (rpm≥1, rollers≥1, µLPerStroke>0) before values reach the engine; show an inline "invalid input" state rather than rendering NaN/Infinity (rotor-solver "show don't hide" precedent) — ASVS V5 |
| T-08-02 | Denial of Service (of the tool's own UI) | total dose exceeding hardware envelope | mitigate | D-02 total-dose-<2000 µL guard raises a visible inline warning; outputs still computed, not silently truncated — ASVS V5 |
| T-08-SC | Tampering | npm/pip/cargo installs | accept (N/A) | Zero package installs this phase (RESEARCH Package Legitimacy Audit: not applicable); no supply-chain surface |
</threat_model>

<verification>
- `node engine-harness.cjs core` passes with exact benchmark numbers (16.6667 / 10 / 583.33 / 376.67).
- No `data-i18n`; `assets/style.css` untouched (`git diff --stat assets/style.css` empty).
- No horizontal scroll at 1280px / 375px (human-check).
</verification>

<success_criteria>
Engine + shell complete when the input panel is fully wired, the engine reproduces the pinned benchmark exactly via the node harness, degenerate inputs are clamped, and the D-02 total-dose warning fires — with all downstream render containers present but empty for Plan 08-02.
</success_criteria>

<output>
Create `.planning/phases/08-dispense-choreography-throughput-simulator-tool-interactive-/08-01-SUMMARY.md` when done.
</output>
