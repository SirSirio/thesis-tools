---
phase: 08-dispense-choreography-throughput-simulator
plan: 04
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - index.html
  - README.md
  - ROADMAP.md
  - CLAUDE.md
autonomous: true
requirements: [D-01, D-03, D-11]
must_haves:
  truths:
    - "The tool is reachable from the landing page via a new tool card"
    - "The tool is listed in the README tool table and the repo-root ROADMAP Shipped table"
    - "CLAUDE.md's folder-structure tree documents the new tool folder"
  artifacts:
    - path: "index.html"
      provides: "Landing-page card linking to the new tool"
      contains: "dispense-throughput-simulator"
    - path: "README.md"
      provides: "Tool-table row"
      contains: "dispense-throughput-simulator"
    - path: "ROADMAP.md"
      provides: "Shipped-table row"
      contains: "Dispense Choreography"
    - path: "CLAUDE.md"
      provides: "Folder-structure entry"
      contains: "dispense-throughput-simulator"
  key_links:
    - from: "index.html tool card"
      to: "tools/dispense-throughput-simulator/index.html"
      via: "href link in the Tools grid"
      pattern: "tools/dispense-throughput-simulator/index.html"
---

<objective>
Integrate the shipped tool into the site chrome: add a landing-page tool card, a README tool-table row, a repo-root ROADMAP Shipped row, and a CLAUDE.md folder-structure entry — matching the System Architecture Explorer precedent for each.

Purpose: A tool that is not linked, listed, or documented is not shipped. This closes the standard GSD ship steps for a new tool. Surfaces D-01 (cocktail pipeline), D-03 (concurrency slider / A1-A2), D-11 (headline metrics) in user-facing copy.

Output: Four chrome/doc files updated; the tool is discoverable and documented.
</objective>

<execution_context>
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/workflows/execute-plan.md
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-dispense-choreography-throughput-simulator-tool-interactive-/08-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Landing-page card + README row</name>
  <files>index.html, README.md</files>
  <read_first>
    - index.html (lines ~650-680 the Tools grid `section-index 01`; the System Architecture Explorer card block `tool-card`; the GSD card at `--rd: 0.42s` — this tool's card follows the GSD card in the Tools grid, so use the next stagger step)
    - README.md (lines 13-22 the `| Tool | Description | Status |` table; the System Architecture Explorer row as the format analog)
    - .planning/phases/08-dispense-choreography-throughput-simulator-tool-interactive-/08-PATTERNS.md (landing-page card + README row analog blocks with ready copy)
  </read_first>
  <action>
    In root `index.html`, add a new `.tool-card` in the Tools grid (`section-index 01`) linking `tools/dispense-throughput-simulator/index.html`, with a distinct `card-icon` emoji NOT already used (⚙ 🧫 〰 🔌 ⬡ 🗓 🃏 are taken — use ⏱ or ⏳), a `card-title` "Dispense Choreography & Throughput Simulator", and a `card-desc` covering: configure up to 6 liquids + flow params, concurrency slider 1..N (A1 lockstep vs A2 independent endpoints), row-per-station Gantt with bottleneck highlight, headline metrics (total run time, bottleneck, A1-vs-A2 time saved, throughput), illustrative rack animation — the empirical U5 answer (D-01, D-03, D-11). The landing page IS bilingual (`data-i18n`): add matching `card-title-dispense-sim` / `card-desc-dispense-sim` keys to BOTH `LANG.en` and `LANG.it` in the landing page's own LANG object (keep IT parity — do not leave orphan keys), and set the card's reveal `--rd` to the next stagger step after the last Tools-grid card.
    In `README.md`, add a new row to the Tools table (after the System Architecture Explorer row, before Prototype Design Space — matching the table's roughly ship-order convention) with the tool link and a one-paragraph description, status `✅ Live`.
  </action>
  <verify>
    <automated>bash -c 'grep -q "tools/dispense-throughput-simulator/index.html" index.html && grep -Eq "card-(title|desc)-dispense-sim" index.html && grep -q "tools/dispense-throughput-simulator/index.html" README.md && echo CARD_README_OK'</automated>
    <automated>bash -c 'en=$(grep -c "card-title-dispense-sim\|card-desc-dispense-sim" index.html); if [ "$en" -lt 4 ]; then echo "FAIL: expected EN+IT i18n keys (4 occurrences), got $en"; exit 1; fi; echo I18N_PARITY_OK'</automated>
    <human-check>Serve the landing page: the new card appears in the Tools grid with a unique icon, links to the tool, and renders correctly in both EN and IT; no horizontal scroll at 1280px/375px.</human-check>
  </verify>
  <acceptance_criteria>
    - `index.html` has a Tools-grid card linking `tools/dispense-throughput-simulator/index.html` with a unique icon and a reveal `--rd` stagger step.
    - `card-title-dispense-sim` and `card-desc-dispense-sim` exist in BOTH `LANG.en` and `LANG.it` (no orphan keys, 4 total occurrences).
    - `README.md` tool table has a new `✅ Live` row for the tool.
  </acceptance_criteria>
  <done>The tool is reachable from the landing page (EN+IT) and listed in the README.</done>
</task>

<task type="auto">
  <name>Task 2: Repo-root ROADMAP row + CLAUDE.md folder structure</name>
  <files>ROADMAP.md, CLAUDE.md</files>
  <read_first>
    - ROADMAP.md (lines 9-19 the `## Shipped` table; the System Architecture Explorer row as the format analog; the `## Planned` placeholder row stays empty)
    - CLAUDE.md (the `tools/` folder-structure tree; the `tools/system-architecture-explorer/` block as the format analog — append after it in ship-order)
    - .planning/phases/08-dispense-choreography-throughput-simulator-tool-interactive-/08-PATTERNS.md (ROADMAP row + CLAUDE.md folder-structure analog blocks with ready copy)
  </read_first>
  <action>
    In repo-root `ROADMAP.md`, add a new row to the `## Shipped` table describing the tool: cocktail-pipeline scheduler for a 6-nozzle indexing line (8×4-sample rack), concurrency slider 1..N with A1/A2 endpoint markers, lockstep-vs-independent mode toggle, stroke-quantized dose timing, full wall-clock accounting (fill/steady/drain + rack changes), row-per-station steady-state Gantt with bottleneck highlight, A1-vs-A2 time-saved headline, illustrative rack animation; answers U5 empirically (D-01, D-03, D-11). Do NOT add a `## Planned` row (leave that placeholder empty, matching prior tools).
    In `CLAUDE.md`, add a `tools/dispense-throughput-simulator/` block to the folder-structure tree (append after the `tools/system-architecture-explorer/` block, ship-order convention) with `index.html` and `SPEC.md` one-line descriptions matching PATTERNS.md's proposed block. Update any file counts / tool lists in CLAUDE.md prose if they enumerate tools.
  </action>
  <verify>
    <automated>bash -c 'grep -q "Dispense Choreography" ROADMAP.md && grep -q "dispense-throughput-simulator" CLAUDE.md && grep -Eq "SPEC.md" CLAUDE.md && echo ROADMAP_CLAUDE_OK'</automated>
    <automated>bash -c 'awk "/## Planned/{p=1} p&&/add tools here/{print \"PLANNED_PLACEHOLDER_INTACT\"}" ROADMAP.md | grep -q PLANNED_PLACEHOLDER_INTACT && echo PLANNED_OK'</automated>
  </verify>
  <acceptance_criteria>
    - Repo-root `ROADMAP.md` `## Shipped` table has a Dispense Choreography row; the `## Planned` placeholder is untouched.
    - `CLAUDE.md` folder tree documents `tools/dispense-throughput-simulator/` with `index.html` + `SPEC.md` entries.
  </acceptance_criteria>
  <done>The tool is recorded in the repo-root roadmap and the CLAUDE.md folder structure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Documentation/chrome-only change (landing card, README, ROADMAP, CLAUDE.md). No new executable logic, no inputs, no network, no persistence. ASVS V5 and all STRIDE categories are N/A here — the tool's only input-validation surface is implemented and threat-modelled in Plan 08-01. Web threats (XSS/CSRF/injection/SSRF) are N/A: the added card copy is static authored text, not user-supplied content.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Tampering | static doc/markup edits | accept (N/A) | Authored static content only; no attack surface |
</threat_model>

<verification>
- Landing card link, README row, ROADMAP row, and CLAUDE.md entry present (grep).
- EN/IT i18n key parity for the new card (no orphans).
- No horizontal scroll at 1280px / 375px (human-check).
</verification>

<success_criteria>
The tool is discoverable from the landing page in EN and IT, listed in README and repo-root ROADMAP, and documented in CLAUDE.md — completing the standard new-tool ship steps.
</success_criteria>

<output>
Create `.planning/phases/08-dispense-choreography-throughput-simulator-tool-interactive-/08-04-SUMMARY.md` when done.
</output>
