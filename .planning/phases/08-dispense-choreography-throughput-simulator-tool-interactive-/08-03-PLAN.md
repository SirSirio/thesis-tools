---
phase: 08-dispense-choreography-throughput-simulator
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - tools/dispense-throughput-simulator/SPEC.md
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12]
must_haves:
  truths:
    - "A co-located SPEC.md documents every input, output, and the scheduling formulas as the canonical reference"
    - "SPEC.md records the benchmark (16.6667 s serial, 10 s ceiling, 583.33/376.67/206.67 full totals) as the design point"
    - "SPEC.md footnotes the fill/steady/drain 32+(N-1) cycle reading, the 3× (not 4×) rack-change convention, and the A1/A2 fixed-K equality"
  artifacts:
    - path: "tools/dispense-throughput-simulator/SPEC.md"
      provides: "Canonical tool spec (inputs, outputs, formulas, constants, assumptions, benchmark)"
      contains: "doseTime"
      min_lines: 60
  key_links: []
---

<objective>
Author the co-located canonical `tools/dispense-throughput-simulator/SPEC.md` documenting the tool's inputs, outputs, scheduling formulas, fixed constants, the benchmark of record, and the resolved-assumption footnotes — mirroring the site's SPEC.md standard (rotor-solver precedent).

Purpose: SPEC.md is the permanent reference the thesis and CLAUDE.md point to. It must faithfully record the engine's model so the benchmark and the D-07/rack-change/A1-A2 modelling choices are defensible. Documents all of D-01..D-12.

Output: A complete `SPEC.md` in the tool folder.
</objective>

<execution_context>
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/workflows/execute-plan.md
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-dispense-choreography-throughput-simulator-tool-interactive-/08-CONTEXT.md
@.planning/phases/08-dispense-choreography-throughput-simulator-tool-interactive-/08-RESEARCH.md
@tools/dispense-throughput-simulator/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the canonical SPEC.md</name>
  <files>tools/dispense-throughput-simulator/SPEC.md</files>
  <read_first>
    - tools/rotor-solver/SPEC.md (the SPEC structure to copy: Purpose / Inputs table / Outputs+formulas / constants / feasibility / assumptions / known values)
    - tools/dispense-throughput-simulator/index.html (the shipped tool — read the actual input IDs, defaults, and engine formulas so the SPEC matches the code exactly)
    - .planning/phases/08-dispense-choreography-throughput-simulator-tool-interactive-/08-RESEARCH.md (Assumptions Log A2/A3/A4/A5/A6 — the footnotes to record; the full worked totals table)
    - .planning/phases/08-dispense-choreography-throughput-simulator-tool-interactive-/08-PATTERNS.md (SPEC.md analog section — the exact structure and the assumption footnotes to include)
  </read_first>
  <action>
    Write `tools/dispense-throughput-simulator/SPEC.md` following rotor-solver's SPEC shape. Sections: (1) Purpose — the U5 (concurrency) decider for the 6-nozzle line / 32-sample rack. (2) Inputs table — one row per actual input element ID (vol0..vol5, rollers, uLPerStroke, rpm, concK, mode) with type/range/default (600/200/175/25, 4, 5, 180, 1, a1) and the fixed constants SAMPLE_SHIFT=1 s, RACK_CHANGE=5 s, RACKS=4, SAMPLES=32 (D-02, D-03, D-04, D-08). (3) Derived constants + scheduling formulas — write out `strokesPerSec = (rpm/60)*rollers`, `doseTime_i = ceil(V_i/uLPerStroke)/strokesPerSec` (D-06), the LPT grouping rule (D-03 concurrency), the unified cycle model `totalCycles = 32 + (N-1)` with `i = c - j + 1` active-station test (D-01, D-05, D-07), and the flat additive `numRackChanges * RACK_CHANGE` overhead. (4) Outputs — the four D-11 metrics (total run time, bottleneck, A1-vs-A2 delta, throughput), the D-09/D-10 Gantt window, the D-12 illustrative animation. (5) Known values at the current design point — the benchmark: doseTimes [10, 3.3333, 2.9167, 0.4167], serial 16.6667 s/sample, pipelined ceiling 10 s, full totals 583.33 s (K=1) / 376.67 s (K=4), delta 206.67 s (~35%), throughput 18.23 / 11.77 s/sample. (6) Assumptions & footnotes — a one-line footnote each for: the fill/steady/drain `32+(N-1)` cycle reading vs the literal `32+2(N-1)` (RESEARCH A2), the 3× (not 4×, rack change does not precede rack 1) convention (A3), rack changes as flat additive overhead (A4), global flow params across all stations (A6), and the A1/A2 fixed-K numerical equality under global params (A5). Cross-link to `prototypes/Prototype-1-Pump-Module/multi-liquid-architecture/ARCHITECTURE-DECISION.md` (U5) and the System Architecture Explorer.
  </action>
  <verify>
    <automated>bash -c 'f=tools/dispense-throughput-simulator/SPEC.md; test -f "$f" && grep -q "doseTime" "$f" && grep -Eq "16.6667|16\\.67" "$f" && grep -Eq "583.33" "$f" && grep -Eq "376.67" "$f" && grep -Eq "206.67" "$f" && grep -Eqi "32 *[+] *\\(?N *- *1" "$f" && grep -Eqi "ARCHITECTURE-DECISION" "$f" && echo SPEC_OK'</automated>
    <automated>bash -c 'f=tools/dispense-throughput-simulator/SPEC.md; grep -Eqi "rack change" "$f" && grep -Eqi "global" "$f" && grep -Eqi "same (time|rate)|A1.*A2|lockstep" "$f" && echo FOOTNOTES_OK'</automated>
  </verify>
  <acceptance_criteria>
    - `SPEC.md` exists with Purpose, Inputs (matching real element IDs/defaults), scheduling formulas, Outputs, Known-values benchmark, and Assumptions sections.
    - Benchmark numbers present verbatim: 16.6667, 10, 583.33, 376.67, 206.67.
    - Footnotes present for: the 32+(N-1) cycle reading, the 3× rack-change convention, flat-additive rack changes, global flow params, and the A1/A2 fixed-K equality.
    - Cross-links to ARCHITECTURE-DECISION.md (U5) and the System Architecture Explorer.
  </acceptance_criteria>
  <done>The tool has a canonical SPEC.md that matches the shipped code and records the benchmark and every resolved modelling assumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Documentation-only change (`SPEC.md`). No executable surface, no inputs, no network, no persistence. All STRIDE categories — including ASVS V5 input validation — are N/A for this plan; the input-validation controls are implemented and threat-modelled in Plan 08-01. Web threats (XSS/CSRF/injection/SSRF) are N/A (no code shipped here).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Tampering | markdown doc content | accept (N/A) | No executable content; no attack surface |
</threat_model>

<verification>
- `SPEC.md` present, benchmark numbers and assumption footnotes verified by grep.
</verification>

<success_criteria>
SPEC.md is complete, matches the shipped tool's IDs/defaults/formulas, and records the benchmark plus the five resolved-assumption footnotes.
</success_criteria>

<output>
Create `.planning/phases/08-dispense-choreography-throughput-simulator-tool-interactive-/08-03-SUMMARY.md` when done.
</output>
