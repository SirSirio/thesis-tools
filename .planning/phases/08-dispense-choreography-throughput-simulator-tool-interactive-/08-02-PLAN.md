---
phase: 08-dispense-choreography-throughput-simulator
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - tools/dispense-throughput-simulator/index.html
autonomous: true
requirements: [D-01, D-03, D-04, D-09, D-10, D-11, D-12]
must_haves:
  truths:
    - "User sees four headline metric cards: total run time, bottleneck station, A1-vs-A2 delta, throughput (D-11)"
    - "The A1-vs-A2 delta reads 206.67 s saved at the benchmark, computed from the K=1 vs K=N slider endpoints (D-11.3)"
    - "Toggling the mode at fixed K shows explanatory copy why the numbers match under global params (D-04)"
    - "A row-per-station Gantt renders a steady-state window with the bottleneck row highlighted and NO horizontal scroll at 1280px/375px (D-09, D-10)"
    - "An illustrative rack-indexing animation loops, decoupled from timing, and stops under prefers-reduced-motion (D-12)"
  artifacts:
    - path: "tools/dispense-throughput-simulator/index.html"
      provides: "Four D-11 metric cards, the D-09/D-10 Gantt, and the D-12 rack animation, all reading from simulateSchedule"
      contains: "prefers-reduced-motion"
  key_links:
    - from: "#metrics / #gantt"
      to: "window.__sim / simulateSchedule() return"
      via: "render functions read the single engine object"
      pattern: "simulateSchedule\\(|__sim"
    - from: "A1-vs-A2 delta card"
      to: "simulateSchedule K=1 and K=N"
      via: "endpoint re-run, independent of mode toggle"
      pattern: "simulateSchedule\\("
---

<objective>
Populate the empty `#metrics`, `#gantt`, and `#rackAnim` containers from Plan 08-01 with the four D-11 headline metric cards, the D-09/D-10 row-per-station steady-state Gantt (bottleneck row highlighted, no horizontal scroll), and the D-12 illustrative rack-indexing animation — all reading from the single `simulateSchedule` engine.

Purpose: Turn the correct engine into the tool's visible U5 verdict. The A1-vs-A2 delta (D-11.3) is the "money" output and must read the K=1 vs K=N slider endpoints directly. Implements D-01, D-03, D-04, D-09, D-10, D-11, D-12.

Output: A fully rendered tool — four live metric cards, a steady-state Gantt, and a looping rack animation — reproducing the benchmark verdict (206.67 s saved, ~35%).
</objective>

<execution_context>
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/workflows/execute-plan.md
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dispense-choreography-throughput-simulator-tool-interactive-/08-CONTEXT.md
@.planning/phases/08-dispense-choreography-throughput-simulator-tool-interactive-/08-RESEARCH.md
@.planning/phases/08-dispense-choreography-throughput-simulator-tool-interactive-/08-PATTERNS.md
@tools/dispense-throughput-simulator/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Four headline metric cards + A1/A2 teaching copy</name>
  <files>tools/dispense-throughput-simulator/index.html</files>
  <read_first>
    - tools/dispense-throughput-simulator/index.html (Plan 08-01 engine + recompute() + #metrics container)
    - tools/rotor-solver/index.html (lines 181-211 .mc/.summary stat-card CSS; lines 780-786 the #summary.innerHTML render call to mirror)
    - tools/thesis-timeline/index.html (.stat/.stat-strip card shape — functionally identical, no i18n)
    - .planning/phases/08-dispense-choreography-throughput-simulator-tool-interactive-/08-RESEARCH.md (Full worked totals table; the A1/A2 lockstep-vs-independent math section — the mode-toggle landmine + recommended teaching copy; Pitfall 2)
    - .planning/phases/08-dispense-choreography-throughput-simulator-tool-interactive-/08-CONTEXT.md (D-11 four metrics, D-04 toggle)
  </read_first>
  <action>
    In recompute(), render four `.mc` stat cards into `#metrics` from the engine object (D-11): (1) TOTAL RUN TIME — `sim.totalRunTime` formatted as `Xm Ys` (e.g. "9m 43.3s") plus raw seconds (D-11.1, D-07). (2) BOTTLENECK STATION — the station with `max(doseTime)`, labelled with its liquid and per-cycle cost, e.g. "600 µL to 10.0 s" (D-11.2), read from `sim.bottleneck`. (3) A1 vs A2 DELTA — compute `simulateSchedule` at K=1 and at K=N (the two pinned slider endpoints, per D-03/D-11.3) INDEPENDENT of the mode toggle's current position; show serial total, parallel total, and time saved "206.67 s saved (approx 35%)" at the benchmark. (4) THROUGHPUT — at the CURRENT K: `sim.totalRunTime/SAMPLES` s/sample and `3600/(perSample)` samples/hr (D-11.4; benchmark K=1 gives 18.23 s/sample, 197.5 samples/hr; K=N gives 11.77 s/sample, 305.7 samples/hr). Re-declare `.mc`/`.summary` (or `.stat-strip`) CSS inline. Add mode-toggle teaching copy near the toggle that appears whenever A1 and A2 give the same number at the current K, stating (paraphrase RESEARCH): lockstep (A1) and independent (A2) take the same time here because every pump is configured at the same rate — A1's real cost only appears when a protocol needs pumps at genuinely different rates (D-04, "represented honestly rather than flattering A1"). Do NOT invent a numeric lockstep penalty. Replace the Plan 08-01 plain-text `#metrics` stash-render with these cards.
  </action>
  <verify>
    <automated>bash -c 'cd "D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page" && node .planning/phases/08-dispense-choreography-throughput-simulator-tool-interactive-/engine-harness.cjs metrics'</automated>
    <automated>bash -c 'f=tools/dispense-throughput-simulator/index.html; grep -Eqi "same (time|rate)|configured at the same|only appears when" "$f" && echo TEACHING_COPY_OK || (echo "FAIL: A1/A2 teaching copy missing"; exit 1)'</automated>
    <human-check>Serve: four cards show at K=1 total approx 9m 43s, bottleneck "600 µL to 10.0 s", A1-vs-A2 "206.67 s saved (~35%)", throughput 18.23 s/sample / 197.5 samples/hr. Toggle A1 to A2 at an interior K (e.g. 2): numbers stay equal and the explanatory copy is visible. Drag slider to N: throughput updates, delta card unchanged.</human-check>
  </verify>
  <acceptance_criteria>
    - Four `.mc` cards render into `#metrics` for total run time, bottleneck, A1-vs-A2 delta, throughput (D-11.1-4).
    - A1-vs-A2 delta is computed from `simulateSchedule(K=1)` vs `simulateSchedule(K=N)` regardless of mode toggle state, printing 206.67 s at the benchmark.
    - Throughput reflects the CURRENT K (18.23 s/sample at K=1, 11.77 at K=N for the benchmark).
    - Mode-toggle teaching copy explaining the fixed-K equality is present (D-04).
  </acceptance_criteria>
  <done>All four D-11 metrics are live and benchmark-accurate; the mode toggle is honest and explained rather than silently inert.</done>
</task>

<task type="auto">
  <name>Task 2: Row-per-station steady-state Gantt (bottleneck highlight, no scroll)</name>
  <files>tools/dispense-throughput-simulator/index.html</files>
  <read_first>
    - tools/dispense-throughput-simulator/index.html (engine + #gantt container)
    - tools/thesis-timeline/index.html (lines 260-322 .row/.row-label/.row-track/.bar CSS; lines 574-577 pct() positioning; lines 636-660 row/track/bar DOM build; lines 663-690 hover-tooltip wiring; line 304 .bar.active highlight to adapt for bottleneck)
    - .planning/phases/08-dispense-choreography-throughput-simulator-tool-interactive-/08-PATTERNS.md (Analog B — the CRITICAL anti-pattern at lines 142-147: do NOT copy .gantt-scroll{overflow-x:auto} or min-width:940px)
    - .planning/phases/08-dispense-choreography-throughput-simulator-tool-interactive-/08-RESEARCH.md (Pattern 3; Pitfall 5 steady-state window selection; Pitfall 6 responsive label column)
  </read_first>
  <action>
    Render a row-per-station Gantt into `#gantt` from `sim.cycles` (D-09): one `.row` per active nozzle/station (N rows), a responsive `.row-label` width `clamp(70px, 22vw, 130px)` (NOT thesis-timeline's fixed 210px), and a `.row-track` holding absolutely-positioned `.bar` divs. Positions via elapsed-SECONDS `pct(t) = (t - windowStart)/(windowEnd - windowStart)*100` — no `Date` objects. Select the steady-state window explicitly (D-10, Pitfall 5): start at cycle N (first fully-occupied cycle), render approx 3 full-occupancy cycles through approx cycle N+3, EXCLUDING fill (cycles 1..N-1) and drain (cycles M+1..M+N-1). Give the timeline container `width:100%` with NO `min-width` and NO `overflow-x` wrapper (CLAUDE.md hard rule + D-10). Colour `.bar` per liquid; wire hover tooltips (station / liquid / dose-time / cycle) reusing thesis-timeline's mouseenter/mousemove/mouseleave pattern. Highlight the bottleneck station's row/bars with an `.active`-style class (adapt `.bar.active` box-shadow) so D-09's "bottleneck row is highlighted" is visible. Add a small caption noting the chart is a steady-state window and that full fill/steady/drain totals are shown in the cards (D-10).
  </action>
  <verify>
    <automated>bash -c 'f=tools/dispense-throughput-simulator/index.html; if grep -Eiq "overflow-x[[:space:]]*:[[:space:]]*(auto|scroll)" "$f"; then echo "FAIL: overflow-x auto/scroll present"; exit 1; fi; if grep -Eiq "min-width[[:space:]]*:[[:space:]]*[0-9]+px" "$f"; then echo "FAIL: fixed min-width present (Gantt must be width:100%)"; exit 1; fi; grep -q "row-track" "$f" && grep -q "clamp(" "$f" && echo GANTT_STRUCT_OK'</automated>
    <automated>bash -c 'cd "D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page" && node .planning/phases/08-dispense-choreography-throughput-simulator-tool-interactive-/engine-harness.cjs core'</automated>
    <human-check>Serve at 1280px and 375px: the Gantt shows N station rows over a steady-state window with N bars per cycle, the bottleneck (600 µL) row is visibly highlighted, hover tooltips work, and there is NO horizontal scrollbar at either width.</human-check>
  </verify>
  <acceptance_criteria>
    - `#gantt` renders one `.row` per station with percentage-positioned `.bar` divs over an elapsed-seconds window.
    - The rendered window starts at cycle N and excludes fill/drain (each cycle shows N bars at K=N).
    - No `overflow-x` and no `min-width` anywhere in the file; `.row-label` uses a `clamp()` responsive width.
    - The bottleneck station row is highlighted; hover tooltips display station/liquid/dose-time/cycle.
  </acceptance_criteria>
  <done>The Gantt makes idle gaps and the bottleneck visible in a steady-state window that fits without horizontal scroll at 1280px and 375px.</done>
</task>

<task type="auto">
  <name>Task 3: Illustrative rack-indexing animation (D-12)</name>
  <files>tools/dispense-throughput-simulator/index.html</files>
  <read_first>
    - tools/dispense-throughput-simulator/index.html (#rackAnim container)
    - tools/rotor-solver/index.html (buildFigure() lines 574-668 — string-concatenation inline-SVG builder assigned to a wrapper's innerHTML; the schematic-drawing technique to mirror)
    - .planning/phases/08-dispense-choreography-throughput-simulator-tool-interactive-/08-RESEARCH.md (Code Examples — the CSS @keyframes/steps() rack-index snippet + prefers-reduced-motion guard; Don't Hand-Roll — CSS steps() for stop-and-go)
    - .planning/phases/08-dispense-choreography-throughput-simulator-tool-interactive-/08-PATTERNS.md (prefers-reduced-motion guard convention; No-CDN / vendored-only constraint — no GSAP)
  </read_first>
  <action>
    Build an illustrative, looping schematic into `#rackAnim` (D-12): a fixed row of N nozzles above a `.rack-track` of 8 sample wells that indexes discretely left-to-right beneath them, with liquid "drops" appearing at each station. Draw the static layout as hand-built inline SVG (rotor-solver buildFigure() string-concat technique) and drive the discrete motion with CSS `@keyframes` + `animation-timing-function: steps(8, end)` on `transform: translateX()` — no JS tweening, no requestAnimationFrame, and absolutely NO GSAP or any runtime library (landing-page GSAP exception does not extend here). Colour the drops to match the Gantt's per-liquid palette. This animation is DECOUPLED from the real schedule (a concept loop, not synced playback) — do NOT wire it to `simulateSchedule` timing. Guard it with `@media (prefers-reduced-motion: reduce) { .rack-track { animation: none; } }` so it renders a static frame under reduced motion. Add a one-line caption: "Illustrative — conveys how the line indexes; not a timing-accurate playback."
  </action>
  <verify>
    <automated>bash -c 'f=tools/dispense-throughput-simulator/index.html; grep -q "prefers-reduced-motion" "$f" && grep -Eq "steps\\(" "$f" && grep -Eqi "@keyframes" "$f" && grep -Eqi "illustrative" "$f" && echo ANIM_OK'</automated>
    <automated>bash -c 'f=tools/dispense-throughput-simulator/index.html; if grep -qi "gsap" "$f"; then echo "FAIL: GSAP referenced"; exit 1; fi; echo NO_GSAP_OK'</automated>
    <human-check>Serve: the rack wells index discretely (snap-pause-snap) beneath the nozzle row in a smooth loop; with OS "reduce motion" enabled the animation is static. No console errors, no external network requests.</human-check>
  </verify>
  <acceptance_criteria>
    - `#rackAnim` shows a looping hand-built SVG + CSS `steps()` rack-indexing schematic decoupled from the engine.
    - A `@media (prefers-reduced-motion: reduce)` rule disables the animation.
    - No `gsap` / no external library reference; caption marks it as illustrative (D-12).
  </acceptance_criteria>
  <done>The tool communicates how the indexing line works via a clear, reduced-motion-safe illustrative loop that never claims timing fidelity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine output → render | `NaN`/`Infinity` from degenerate input could reach card/Gantt rendering |

Pure client-side static render layer: no server, no network, no auth, no persistence. XSS/CSRF/injection/SSRF are N/A — no untrusted HTML is rendered (card/Gantt text is built from numeric engine output, not user-supplied markup) and no requests are made.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Denial of Service (of the tool's own UI) | metric-card / Gantt render fed `NaN`/`Infinity` | mitigate | Rendering consumes the Plan 08-01 clamped `feasible` engine output; when `feasible=false` show the inline warning state instead of `NaN` cards or a zero-width/blank Gantt (rotor-solver "show don't hide" precedent) — ASVS V5 |
| T-08-04 | Tampering | user volume text reflected into card labels | accept (N/A) | Values are coerced to numbers before display; no raw user string is injected as HTML — no injection surface |
</threat_model>

<verification>
- Node engine harness still passes (engine untouched by this plan): 583.33 / 376.67 / 206.67.
- No `overflow-x` / `min-width` in the file; no `gsap`; `prefers-reduced-motion` guard present.
- No horizontal scroll at 1280px / 375px (human-check).
</verification>

<success_criteria>
Visualization complete when the four D-11 cards, the D-09/D-10 steady-state Gantt (bottleneck highlighted, no scroll), and the D-12 illustrative animation all render from the single engine, the benchmark verdict reads 206.67 s saved, and the mode toggle is explained rather than silently inert.
</success_criteria>

<output>
Create `.planning/phases/08-dispense-choreography-throughput-simulator-tool-interactive-/08-02-SUMMARY.md` when done.
</output>
