---
phase: 09-pump-testing
plan: 04
type: execute
wave: 5
depends_on: ["09-06"]
files_modified:
  - tools/pump-testing/SPEC.md
autonomous: true
requirements: [D-01, D-03]
must_haves:
  truths:
    - "A co-located SPEC.md exists documenting the tool's purpose, the normative values/tables it relies on (as 'inputs'), the formulas, constants, standards cited, assumptions, and the explicitly-deferred scope (D-01 document-first tool, D-03 depth allocation)"
    - "SPEC.md records the dual-frame standards anchor (ISO 23783-2 Annex D test method + ISO 8655 analogical benchmark) and the honest gaps (ISO 23783-3 not held; ISO 8655-2 numbers [unverified])"
    - "SPEC.md documents BOTH layers: the top-layer market-grade protocol AND the bottom-layer actual prototype protocol + justified deviations (the ISO-requirement | prototype-reality | justification table, the 0.1 mg-balance workarounds, the pipette head-to-head), and names the still-deferred live-calculator scope so the tool's boundary is on record"
  artifacts:
    - path: "tools/pump-testing/SPEC.md"
      provides: "Canonical co-located spec for the pump-testing tool"
      min_lines: 40
      contains: "23783-2"
  key_links:
    - from: "tools/pump-testing/SPEC.md"
      to: "tools/pump-testing/index.html"
      via: "documents the shipped page's sections, values, and constants"
      pattern: "pump-testing|index.html"
---

<objective>
Author the co-located `tools/pump-testing/SPEC.md` per the site's tool-spec standard: purpose and scope, the normative values/tables the page relies on (here "inputs" are the standards content, not calculator inputs), the formulas written out, the hardware/normative constants, the standards cited, the assumptions, and the explicitly-deferred scope.

This documents the document-first tool (D-01) and its depth allocation (D-03) so SPEC.md is the canonical reference the CONTEXT/planning files can point to.

Purpose: Keep the tool-spec standard (every tool has a co-located SPEC.md) satisfied.
Output: `tools/pump-testing/SPEC.md`.
</objective>

<execution_context>
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/workflows/execute-plan.md
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-pump-testing/09-CONTEXT.md
@.planning/phases/09-pump-testing/09-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write tools/pump-testing/SPEC.md documenting purpose, normative values, formulas, standards, assumptions, and deferred scope</name>
  <files>tools/pump-testing/SPEC.md</files>
  <read_first>
    - CLAUDE.md ("Tool spec standard" — the required SPEC.md contents: purpose, inputs with types/ranges/defaults, outputs with formulas written out, hardware constants/lookup tables, assumptions, known values)
    - tools/peristaltic-roller-displaced-volume-model/SPEC.md (a sibling SPEC.md for section-structure precedent)
    - .planning/phases/09-pump-testing/09-RESEARCH.md (the verified values: Table 3, Table D.1, Annex A formulas + Tanaka constants, evaporation >=10, pre-rinse >=5x, thermal +-2C/>=2h, the [unverified] ISO 8655-2 item, the ISO 23783-3 gap)
    - .planning/phases/09-pump-testing/09-CONTEXT.md (D-01..D-04, deferred ideas)
    - tools/pump-testing/index.html (the SHIPPED page from plans 09-01..09-03 AND 09-06 — cross-check SPEC.md against the actual sections, values, and anchor ids present, so SPEC.md documents what shipped rather than re-deriving from the same upstream docs and silently drifting)
    - prototypes/Prototype-1-Pump-Module/proto-02-5ul-4roller-v2/TEST-PROTOCOL.md (the bottom-layer source authored into the page by 09-06: §7 deviation table, §2 balance workarounds, §6 pipette head-to-head, §11 honest scope — SPEC.md documents this second layer too)
  </read_first>
  <action>
    Create `tools/pump-testing/SPEC.md` following the site tool-spec standard, adapted to a document-first page. Include: Purpose and scope (a document-first, QR-citable dispensing-accuracy qualification protocol; deep accuracy metrology core + lighter mapped go-to-market dimensions; AND a bottom layer documenting the actual prototype protocol + justified deviations). Standards anchor: the dual frame — ISO 23783-2 Annex D as the applicable gravimetric TEST METHOD and ISO 8655 (Parts 2/6) as the analogical PERFORMANCE BENCHMARK — with the honest gaps stated (ISO 23783-2 Part 2 sets no device tolerance; §9 delegates to ISO 23783-3 which the project does not hold; ISO 8655-2:2022 exact permissible-error numbers are [unverified — obtain via DTU library]). Normative values / "inputs": Table 3 balance grades (the 0.5-20 microlitre -> 0.001 mg semi-micro compliant band), Table D.1 environmental conditions, preparation constants (pre-rinse >=5x, thermal equilibrium +-2C/>=2h, balance settle >=6s), evaporation blank >=10 cycles. Formulas written out: Annex A.1 buoyancy conversion, A.2 CIPM-2007 air density, A.3 Tanaka water density (with constants a1..a5), A.4 shortcut V=m*Z, plus trueness and CV definitions. Constants: the Tanaka constants and the 8.0 g/mL calibration-weight density. Assumptions: the analogy (pump is not a piston apparatus; benchmarked to pipette-equivalence), gravimetric is close to the only applicable method for a single-outlet contact-delivery pump, GRM/slope-method distinction, thermal-expansion correction not applicable (no calibrated beta). Bottom layer (now authored, not deferred): document the ISO 23783-2 requirement | prototype reality | justification deviation table (TEST-PROTOCOL.md §7), the two-axis accuracy-calibratable/precision-not model, the 0.1 mg-balance binding constraint and its three workarounds (slope method from >=200 microlitre compliant points, per-stroke CV via root-N, same-instrument interleaved pipette comparison), the pipette head-to-head thesis claim, and the honest can/cannot-claim scope. Still-deferred scope: the live results calculator / DoE planner / CSV export (deferred to a future phase, D-01/D-02) — the bottom layer itself is authored in this phase (09-06), NOT deferred. Cross-links: sibling tools (rotor-solver 5 microlitre design point, displaced-volume model / KaTeX precedent) and proto-02 TEST-PROTOCOL.md + PROTOTYPE.md as the bottom-layer sources. Keep SPEC.md in sync with the shipped `index.html` sections (both layers).
  </action>
  <acceptance_criteria>
    - `tools/pump-testing/SPEC.md` exists and contains "23783-2", "8655", and "Annex A".
    - SPEC.md records the [unverified] ISO 8655-2 status and the ISO 23783-3 gap (contains "unverified" and "23783-3").
    - SPEC.md documents the bottom layer — the actual prototype protocol + justified deviations (contains "deviation" and "prototype").
    - SPEC.md states the still-deferred live-calculator scope (contains "deferred" and "calculator").
    - SPEC.md lists the Tanaka constants or the 0.001 mg semi-micro balance requirement (contains "0.001 mg" or "Tanaka").
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'f=tools/pump-testing/SPEC.md; test -f "$f" && grep -q "23783-2" "$f" && grep -q "8655" "$f" && grep -q "Annex A" "$f" && grep -qi "unverified" "$f" && grep -q "23783-3" "$f" && grep -qi "deviation" "$f" && grep -qi "calculator" "$f" && grep -qi "deferred" "$f" && grep -qi "0.001 mg\|Tanaka" "$f" && echo PASS'</automated>
  </verify>
  <done>SPEC.md documents the tool's purpose, dual-frame standards anchor with honest gaps, normative values, formulas/constants, assumptions, BOTH layers (top protocol + bottom prototype deviations), and the still-deferred live-calculator scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Authoring a static markdown documentation file. No executable surface, no server, no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08 | Tampering / Integrity | Documented normative values | mitigate | SPEC.md carries the same [unverified] tag on ISO 8655-2 numbers and names the ISO 23783-3 gap, keeping the canonical spec honest and consistent with the page. |
</threat_model>

<verification>
- `tools/pump-testing/SPEC.md` exists and is consistent with the shipped `index.html` (same standards, values, deferred scope).
</verification>

<success_criteria>
A canonical co-located SPEC.md documents the document-first pump-testing tool, its dual-frame standards anchor with honest gaps, its normative values and formulas, and its deferred scope.
</success_criteria>

<output>
Create `.planning/phases/09-pump-testing/09-04-SUMMARY.md` when done.
</output>
