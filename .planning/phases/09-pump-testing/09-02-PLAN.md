---
phase: 09-pump-testing
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - tools/pump-testing/index.html
autonomous: true
requirements: [D-01, D-02, D-03]
must_haves:
  truths:
    - "The mass-to-volume section renders the ISO 23783-2 Annex A conversion formulas as typeset KaTeX (buoyancy A.1, Tanaka water-density A.3, air-density A.2, shortcut A.4 V=m*Z) and cites Annex A directly, NOT ISO 8655-6 (D-03 deep metrological core)"
    - "A compact excerpt of Z-factor Table A.1 (around 20C / 1013 hPa plus bracketing rows) is shown, not the full 31x7 grid, respecting the no-horizontal-scroll rule"
    - "The replicates section states the n=10 / three-test-point (10/50/100% of nominal) convention and attributes it to ISO 8655, and distinguishes batch-mean from stroke-to-stroke CV"
    - "The trueness/precision section marks the exact ISO 8655-2:2022 permissible-error numbers as [unverified - obtain via DTU] wherever quoted, and labels ISO 23783-2 Table 1 figures as method-uncertainty, NOT a device spec"
    - "The reporting section names the ISO 23783-3 gap honestly (not obtained) and references EURAMET CG-19 and GUM/ISO IEC Guide 98-3"
    - "No calculator: formulas are rendered for reading/citation only, with no input fields and no live computation (D-01, D-02)"
  artifacts:
    - path: "tools/pump-testing/index.html"
      provides: "Sections 4-7: mass-to-volume Z-factor, replicates, trueness/precision, reporting/uncertainty"
      contains: "unverified"
  key_links:
    - from: "tools/pump-testing/index.html #mass-to-volume"
      to: "katex/katex.min.js"
      via: "renderMathInElement typesets the Annex A formulas"
      pattern: "V_L|Z"
---

<objective>
Author the deep metrological core of the top layer (D-03): Section 4 mass-to-volume conversion (ISO 23783-2 Annex A Z-factor), Section 5 replicates and statistical convention, Section 6 trueness and precision benchmark, and Section 7 reporting and measurement uncertainty.

This is the fully-specified accuracy metrology that D-03 designates as the deep core. It stays document-first (D-01) and builds no calculation logic (D-02) — every formula is typeset for reading and citation, never wired to inputs. The [unverified] discipline from RESEARCH is a hard content rule enforced here.

Purpose: Deliver the trueness/precision/uncertainty backbone that makes the page a citable thesis-grade protocol.
Output: `tools/pump-testing/index.html` Sections 4-7 populated.
</objective>

<execution_context>
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/workflows/execute-plan.md
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-pump-testing/09-CONTEXT.md
@.planning/phases/09-pump-testing/09-RESEARCH.md
@tools/peristaltic-roller-displaced-volume-model/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Section 4 (mass-to-volume, Annex A Z-factor) with typeset KaTeX formulas and a compact Table A.1 excerpt</name>
  <files>tools/pump-testing/index.html</files>
  <read_first>
    - tools/pump-testing/index.html (the file being modified; the KaTeX render config wired in 09-01)
    - .planning/phases/09-pump-testing/09-RESEARCH.md (RQ-4 "Annex A — Mass-to-volume conversion": Formula A.1 buoyancy with density symbols, A.2 air density CIPM-2007, A.3 Tanaka with constants a1..a5, Table A.1 lookup, A.4 shortcut; the CORRECTION that the Z-factor is Annex A self-contained, NOT ISO 8655-6)
    - tools/peristaltic-roller-displaced-volume-model/index.html (how KaTeX delimiters are written for renderMathInElement)
  </read_first>
  <action>
    Fill `#mass-to-volume` (Section 4) with the ISO 23783-2 Annex A conversion, all math as KaTeX (matching the displaced-volume-model delimiter convention so `renderMathInElement` typesets it). Render: Formula A.1 general volume-from-mass buoyancy equation (with the density terms for test liquid, air, and calibration weight, and the Note 2 stainless-steel 8.0 g/mL calibration-weight density); Formula A.2 named as the CIPM-2007 air-density equation with its validity window (15-27C / 600-1100 hPa / 20-80% RH); Formula A.3 the Tanaka pure-water density formula with the constants a1=-3.983035 C, a2=301.797 C, a3=522528.9 C^2, a4=69.34881 C, a5=0.999974950 g/mL; and Formula A.4 the practical shortcut V_i = m_i * Z. Render a COMPACT excerpt of Table A.1 (the 20.0C row across a few barometric-pressure columns such as 950/1000/1013/1050 hPa, plus one bracketing temperature row above and below, e.g. 19.5C and 20.5C) — NOT the full 31-row x 7-column grid — inside a card that wraps/scrolls internally, with a note that the full Table A.1 spans 15.0-30.0C in 0.5C steps and that Formula A.4 is the general shortcut for other conditions. Cite ISO 23783-2 Annex A directly and explicitly state the correction that the Z-factor basis is Annex A, not ISO 8655-6. Add NO input fields and NO computation — the formulas are for reading and citation only (D-01, D-02).
  </action>
  <acceptance_criteria>
    - `#mass-to-volume` contains the Tanaka constant token "0.999974950" and "522528.9".
    - The section names "Annex A" and contains the shortcut form (contains "Z" and a mass-times-Z relationship) and the string "1013" (Table A.1 excerpt pressure column).
    - The section explicitly states Annex A is self-contained / not ISO 8655-6 (contains "Annex A" and "8655-6" in a corrective sentence).
    - Section 4 introduces no `<input>` element.
    - Table A.1 is rendered as a compact excerpt (fewer than the full grid) inside a card; no page-level horizontal scroll at 1280px/375px.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'f=tools/pump-testing/index.html; grep -q "0.999974950" "$f" && grep -q "522528.9" "$f" && grep -q "Annex A" "$f" && grep -q "1013" "$f" && grep -q "8655-6" "$f" && ! grep -qi "<input" "$f" && echo PASS'</automated>
  </verify>
  <done>Section 4 typesets the Annex A Z-factor formulas and a compact Table A.1 excerpt, citing Annex A directly and correcting the ISO 8655-6 misattribution.</done>
</task>

<task type="auto">
  <name>Task 2: Author Section 5 (replicates and statistical convention)</name>
  <files>tools/pump-testing/index.html</files>
  <read_first>
    - tools/pump-testing/index.html (the file being modified)
    - .planning/phases/09-pump-testing/09-RESEARCH.md (RQ-4 "Replicate count"; the dual-frame-in-practice note: method from Part 2, replicate convention n=10 / three points from ISO 8655; proto-02 batch-mean vs stroke-to-stroke setup)
    - .planning/phases/09-pump-testing/09-CONTEXT.md (D-03 deep core)
  </read_first>
  <action>
    Fill `#replicates` (Section 5) as the dual-frame in action: state that ISO 23783-2 Annex D.5.3(g) itself leaves replicate count open ("perform as many measurements as required", a Part 3 matter), and that the operative convention comes from ISO 8655 — a minimum of ten measurements per volume at three test points (100%, 50%, 10% of nominal). Render the trueness (systematic error) and precision (CV, coefficient of variation) definitions as KaTeX (mean delivered volume vs nominal; standard deviation over mean). Explain the batch-mean vs stroke-to-stroke-CV distinction (both because it is metrologically correct and because it sets up the deferred bottom-layer's rig split), noting that pinning the mean with a small n is not the same as characterising single-dose CV, which needs the n=10 single-dose convention. Reiterate the evaporation-blank >=10 cycle prerequisite (cross-reference Section 2). Keep it document-first: no inputs, no computation.
  </action>
  <acceptance_criteria>
    - `#replicates` contains "n=10" or "ten measurements" and the three test points "100%", "50%", "10%".
    - The section attributes the replicate convention to ISO 8655 (contains "8655") and notes Part 2 / Annex D leaves count open.
    - The section renders CV / trueness definitions (contains "CV" and "coefficient of variation" or a KaTeX standard-deviation-over-mean expression).
    - No `<input>` added.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'f=tools/pump-testing/index.html; grep -qi "ten measurements\|n=10" "$f" && grep -q "100%" "$f" && grep -q "10%" "$f" && grep -q "8655" "$f" && grep -qi "coefficient of variation\|CV" "$f" && ! grep -qi "<input" "$f" && echo PASS'</automated>
  </verify>
  <done>Section 5 states the ISO 8655 n=10 / three-point convention applied to the Annex D method and defines trueness/precision, with no calculator.</done>
</task>

<task type="auto">
  <name>Task 3: Author Section 6 (trueness and precision benchmark) with [unverified] tagging, and Section 7 (reporting and uncertainty)</name>
  <files>tools/pump-testing/index.html</files>
  <read_first>
    - tools/pump-testing/index.html (the file being modified)
    - .planning/phases/09-pump-testing/09-RESEARCH.md (RQ-1 category-error warning; RQ-4 "[unverified]" discipline and Open Question 1; Table 1 method-uncertainty figures 1.4%/0.6% at 0.5-20 microlitre; §9/§8.2 reporting, EURAMET CG-19, GUM/ISO IEC Guide 98-3)
    - .planning/phases/09-pump-testing/09-CONTEXT.md (D-03 deep core)
  </read_first>
  <action>
    Fill `#trueness-precision` (Section 6): present the pass/fail benchmark as the ISO 8655 pipette-grade trueness/CV convention, and mark every exact ISO 8655-2:2022 permissible-error percentage with the literal tag "[unverified - obtain via DTU library]" wherever a specific number is quoted (or omit specific numbers and describe only the confirmed structure: two bounded error types, tightening toward larger volumes, three test points, n=10). Alongside, present ISO 23783-2 Table 1's own typical achievable figures (systematic 1.4% / random 0.6% at 0.5-20 microlitre) and label them clearly as the measurement method's own achievable uncertainty, NOT a device pass/fail spec — explicitly flag the category error that conflating "the method can resolve to X" with "the device must be accurate to X" would be. Fill `#reporting-uncertainty` (Section 7): state that ISO 23783-2 §9 delegates specification/reporting/traceability/measurement uncertainty to ISO 23783-3, and name that gap honestly (Part 3 not obtained this session); outline the two MSU approaches from §8.2 (whole-system statistical vs measurement-model/GUM per ISO IEC Guide 98-3) and reference EURAMET CG-19 for gravimetric-procedure MSU; add the one-line note that the §7 thermal-expansion correction is not applicable (no calibrated beta for the 3D-printed pump). Document-first: no inputs, no computation.
  </action>
  <acceptance_criteria>
    - `#trueness-precision` contains the literal string "unverified" applied to ISO 8655-2 numbers, and contains "1.4%" and "0.6%" labeled as method-uncertainty / not a device spec (contains "method" and "not a device spec" or equivalent corrective phrasing).
    - The section names the category-error warning (contains "category error" or an explicit "not a device specification" statement).
    - `#reporting-uncertainty` names "23783-3" as the delegated/unavailable part and contains "EURAMET" and "GUM" (or "Guide 98-3").
    - No `<input>` added.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'f=tools/pump-testing/index.html; grep -qi "unverified" "$f" && grep -q "1.4%" "$f" && grep -q "0.6%" "$f" && grep -q "23783-3" "$f" && grep -qi "EURAMET" "$f" && grep -qi "GUM\|Guide 98-3" "$f" && ! grep -qi "<input" "$f" && echo PASS'</automated>
  </verify>
  <done>Section 6 enforces [unverified] tagging and the method-uncertainty-vs-device-spec distinction; Section 7 names the ISO 23783-3 gap and the MSU approaches, with no calculator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none of consequence) | Adds static prose, static tables, and typeset KaTeX formulas to an existing offline page. No server, API, auth, persistence, or untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering / Integrity | Quoted normative values | mitigate | Content-level rule: every unverifiable ISO 8655-2 number carries an explicit `[unverified]` tag; method-uncertainty figures are labeled as such and not as a device spec — prevents a factual mis-citation reaching the thesis. |
| T-09-05 | Elevation / XSS | KaTeX rendering | accept | KaTeX typesets only author-written math strings; no untrusted input path exists. |
</threat_model>

<verification>
- Sections 4-7 render offline with formulas typeset from the local `katex/` folder.
- Every ISO 8655-2:2022 exact number carries `[unverified - obtain via DTU]`.
- ISO 23783-2 Table 1 figures are labeled method-uncertainty, not device spec.
- No `<input>` elements anywhere; no horizontal scroll at 1280px/375px.
</verification>

<success_criteria>
The deep metrological core (Sections 4-7) is authored: Annex A Z-factor formulas typeset and cited to Annex A, the n=10 three-point convention attributed to ISO 8655, the trueness/precision benchmark with strict [unverified] tagging and the method-vs-device distinction, and the reporting section naming the ISO 23783-3 gap — all document-first with no calculator.
</success_criteria>

<output>
Create `.planning/phases/09-pump-testing/09-02-SUMMARY.md` when done.
</output>
