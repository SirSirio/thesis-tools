---
phase: 09-pump-testing
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - tools/pump-testing/index.html
autonomous: true
requirements: [D-01, D-03, D-04]
must_haves:
  truths:
    - "The alternate-methods section presents a trimmed comparison table with the explicit 'why gravimetric, not photometric/hybrid/dimensional/GRM' rationale for this contact-delivery single-outlet pump (D-03)"
    - "GRM (Annex E) is stated as non-contact/free-flying-droplet only and therefore NOT applicable; proto-02's slope method is described as inspired-by but explicitly NOT labeled the ISO GRM"
    - "IEC 60601-2-24 trumpet curves are mentioned as considered-and-rejected (continuous-flow framing mismatched to discrete strokes), not adopted as a frame"
    - "The go-to-market section maps the lighter dimensions (safety/reliability, biocompatibility ISO 10993 scoped to contact type, QMS ISO 13485 / CLIA waiver / ISO 22870 / ISO 15189) with visibly lighter styling than the deep core (D-03)"
    - "The bottom-layer slot is a clearly-labeled, visually-distinct, UN-POPULATED placeholder with an anchor id, a one-paragraph 'what goes here' description of the ISO-requirement | prototype-reality | justification table, and forward-pointers to proto-02 sections 2/8.1/8.2/11 — with NO actual protocol content authored this phase (D-04)"
  artifacts:
    - path: "tools/pump-testing/index.html"
      provides: "Sections 8-10: alternate methods, mapped go-to-market dimensions, deferred bottom-layer slot"
      contains: "prototype-protocol-slot"
  key_links:
    - from: "tools/pump-testing/index.html #prototype-protocol-slot"
      to: "prototypes/Prototype-1-Pump-Module/proto-02-5ul-4roller-v2/PROTOTYPE.md"
      via: "forward-pointer references to sections 2/8.1/8.2/11"
      pattern: "proto-02|PROTOTYPE"
---

<objective>
Author the final top-layer sections and the deferred bottom-layer slot: Section 8 alternate-methods comparison, Section 9 mapped go-to-market dimensions (D-03 lighter map), and Section 10 the bottom-layer slot (D-04 — slot only, not populated).

D-04 is the load-bearing constraint here: this plan builds a clean, obvious, clearly-labeled placeholder for the actual prototype protocol + justified deviations, but does NOT design or write that protocol content. The slot is the Phase 9 deliverable for the bottom layer.

Purpose: Complete the market-grade top layer and leave the correct scaffold for the future prototype-protocol phase.
Output: `tools/pump-testing/index.html` Sections 8-10 populated (Section 10 as an empty-by-design slot).
</objective>

<execution_context>
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/workflows/execute-plan.md
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-pump-testing/09-CONTEXT.md
@.planning/phases/09-pump-testing/09-RESEARCH.md
@prototypes/Prototype-1-Pump-Module/proto-02-5ul-4roller-v2/PROTOTYPE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Section 8 (alternate-methods comparison + why gravimetric + GRM/trumpet-curve rejections)</name>
  <files>tools/pump-testing/index.html</files>
  <read_first>
    - tools/pump-testing/index.html (the file being modified)
    - .planning/phases/09-pump-testing/09-RESEARCH.md (RQ-2 alternates table and applicability verdicts; the GRM non-contact restriction and the slope-method-is-not-GRM correction; RQ-1 IEC 60601-2-24 rejection row)
    - .planning/phases/09-pump-testing/09-CONTEXT.md (D-03 deep core includes a compact alternates comparison)
  </read_first>
  <action>
    Fill `#alternate-methods` (Section 8) with a trimmed reader-facing comparison table derived from the RQ-2 table: for each method (single-channel gravimetric Annex D; gravimetric regression GRM Annex E; dual-dye and single-dye photometric Annex B/C; fluorescence Annex H; photometric/gravimetric hybrid Annex F; optical image analysis Annex G/I; titrimetric ISO 8655-7) give the standard reference, the volume range, and an "applicable to this pump?" verdict with a one-line reason. State the explicit rationale that gravimetric single-channel (Annex D) is close to the ONLY fit because the pump has one outlet delivering by contact into a vessel, while all plate/photometric/fluorescence methods assume a microplate-format multi-well liquid handler. State that GRM (Annex E) is explicitly restricted to non-contact devices delivering free-flying droplets or jets and therefore does NOT apply, and that proto-02's "slope method" is inspired by the same linear-extrapolation logic but is NOT the ISO GRM procedure and must not be labeled as such. Add a short considered-and-rejected note that IEC 60601-2-24 infusion-pump trumpet curves were weighed and rejected because they characterise continuous/near-continuous flow-rate accuracy over long observation windows, mismatched to this pump's discrete ~5 microlitre strokes. Keep the table inside a card that wraps/scrolls internally (no page-level horizontal scroll). No inputs, no computation.
  </action>
  <acceptance_criteria>
    - `#alternate-methods` contains a table naming at least "Annex E" (GRM), "Annex B" or "photometric", and "gravimetric".
    - The section states GRM is non-contact / free-flying droplet only and NOT applicable (contains "free flying" or "non-contact" and "GRM").
    - The section states the slope method is NOT the ISO GRM (contains "slope method" and a negation such as "not" near "GRM").
    - The section mentions "60601-2-24" or "trumpet" as considered-and-rejected.
    - No `<input>` added; no page-level horizontal scroll at 1280px/375px.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'f=tools/pump-testing/index.html; grep -q "Annex E" "$f" && grep -qi "gravimetric" "$f" && grep -qi "free flying\|free-flying\|non-contact" "$f" && grep -qi "slope method" "$f" && grep -qi "trumpet\|60601-2-24" "$f" && ! grep -qi "<input" "$f" && echo PASS'</automated>
  </verify>
  <done>Section 8 presents the alternates table with the why-gravimetric rationale and the GRM/slope-method and trumpet-curve rejections.</done>
</task>

<task type="auto">
  <name>Task 2: Author Section 9 (mapped go-to-market dimensions, lighter styling)</name>
  <files>tools/pump-testing/index.html</files>
  <read_first>
    - tools/pump-testing/index.html (the file being modified)
    - .planning/phases/09-pump-testing/09-RESEARCH.md (RQ-3 relevance map: biocompatibility ISO 10993 scoped to contact type, safety/reliability map-only, QMS ISO 13485, CLIA waiver, ISO 22870/15189)
    - .planning/phases/09-pump-testing/09-CONTEXT.md (D-03: these dimensions are covered lighter, not at equal depth, not omitted)
  </read_first>
  <action>
    Fill `#go-to-market` (Section 9) as a compact card grid or table with VISIBLY lighter styling than the deep metrological sections (a distinct lighter section class — less "deep" treatment) to honor D-03's depth allocation. Cover: safety / reliability / lifetime as a "what would be assessed" map only (no single named deep standard); biocompatibility per ISO 10993 (series; -1 general principles, -5 cytotoxicity), scoped explicitly to the device's actual contact type (external-communicating, limited-duration, non-blood-contact fluid path — NOT the full blood-contact hemocompatibility panel of -4), so the tool does not imply testing the device does not need; QMS / regulatory pathway per ISO 13485 (required only if productized, not for a thesis prototype); the CLIA waiver bar as a framing device ("simple, accurate, low risk, untrained-operator-usable") matching the point-of-care intent; and ISO 22870 / ISO 15189 as POCT quality/competence framing (procedural, no dispensing-accuracy numbers). Add a one-line lead sentence framing this as "what full market-readiness also demands" beyond dispensing accuracy. No inputs, no computation.
  </action>
  <acceptance_criteria>
    - `#go-to-market` names "10993", "13485", "CLIA", and "22870" (or "15189").
    - The biocompatibility entry scopes to the actual contact type (contains "non-blood" or "external-communicating" or an explicit statement that the full blood-contact panel is not required).
    - The section carries a distinct lighter-styling class (different from the deep-core section class), evidencing D-03 depth allocation.
    - No `<input>` added.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'f=tools/pump-testing/index.html; grep -q "10993" "$f" && grep -q "13485" "$f" && grep -qi "CLIA" "$f" && grep -q "22870\|15189" "$f" && grep -qi "non-blood\|external-communicating\|blood-contact" "$f" && ! grep -qi "<input" "$f" && echo PASS'</automated>
  </verify>
  <done>Section 9 maps the go-to-market dimensions with lighter styling and correctly scopes biocompatibility to the device's contact type.</done>
</task>

<task type="auto">
  <name>Task 3: Author Section 10 — the DEFERRED bottom-layer slot (placeholder only, D-04)</name>
  <files>tools/pump-testing/index.html</files>
  <read_first>
    - tools/pump-testing/index.html (the file being modified)
    - .planning/phases/09-pump-testing/09-CONTEXT.md (D-04: do NOT design the bottom layer; leave a clean, obvious slot)
    - .planning/phases/09-pump-testing/09-RESEARCH.md ("Recommended Top-Layer Section Structure" item 10 — the slot spec and forward-pointers)
    - prototypes/Prototype-1-Pump-Module/proto-02-5ul-4roller-v2/PROTOTYPE.md (confirm sections 2, 8.1, 8.2, 11 exist as the forward-pointer targets)
  </read_first>
  <action>
    Fill `#prototype-protocol-slot` (Section 10) as a clearly-labeled, visually-DISTINCT, UN-POPULATED placeholder — a titled section such as "Actual Prototype Protocol and Deviations — pending user input" with a distinct placeholder style (e.g. a dashed-border / muted card that reads unmistakably as "to be filled"). Include exactly one paragraph describing WHAT will go here: an ISO-requirement | prototype-reality | justification deviation table, seeded from proto-02's already-documented deviations, to be authored in a later phase once the user supplies the actual protocol. Add forward-pointers (as links or explicit text references) to proto-02 `PROTOTYPE.md` section 2 (targets and pass criteria), section 8.1 (execution protocol and blockers), section 8.2 (first result), and section 11 (diagnosis). Do NOT author any actual protocol content, deviation rows, or justification prose — the slot itself is the entire deliverable for this layer (D-04). Do NOT build the deviation table with real rows. No inputs, no computation.
  </action>
  <acceptance_criteria>
    - `#prototype-protocol-slot` exists and reads as a placeholder (contains "pending" or "to be" and a phrase like "Actual Prototype Protocol").
    - The section names the intended structure "ISO-requirement" / "prototype-reality" / "justification" (deviation table description) as future content, not populated rows.
    - The section contains forward-pointers referencing proto-02 (contains "proto-02" or "PROTOTYPE") and the section markers 2, 8.1, 8.2, 11.
    - The section contains NO populated deviation table (no real requirement-vs-reality rows) — it is a described-only slot.
    - No `<input>` added.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'f=tools/pump-testing/index.html; grep -qi "pending\|to be" "$f" && grep -qi "prototype protocol" "$f" && grep -qi "justification" "$f" && grep -qi "proto-02\|PROTOTYPE" "$f" && grep -q "8.1" "$f" && grep -q "8.2" "$f" && ! grep -qi "<input" "$f" && echo PASS'</automated>
  </verify>
  <done>Section 10 is a clearly-labeled, distinctly-styled, un-populated slot describing the future deviation table and pointing to proto-02 sections 2/8.1/8.2/11 — no actual protocol content authored (D-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none of consequence) | Adds static prose and tables to an existing offline page. No server, API, auth, persistence, or untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Tampering / Scope | Bottom-layer slot | mitigate | Acceptance criteria enforce that Section 10 stays an un-populated placeholder (no deviation rows) — prevents accidental scope creep past D-04 into designing the bottom layer this phase. |
| T-09-07 | Information Disclosure | Content | accept | All content is author-controlled static text; no PII, no secrets, no user data. |
</threat_model>

<verification>
- Sections 8-10 render offline.
- Section 10 is a clearly-labeled, distinctly-styled placeholder with forward-pointers and NO populated protocol content (D-04).
- Go-to-market section uses lighter styling than the deep core (D-03).
- No `<input>` elements anywhere; no horizontal scroll at 1280px/375px.
</verification>

<success_criteria>
The top layer is complete: alternates comparison with the why-gravimetric rationale and GRM/trumpet rejections, the lighter go-to-market map, and a clean deferred bottom-layer slot pointing to proto-02 — the slot un-populated per D-04.
</success_criteria>

<output>
Create `.planning/phases/09-pump-testing/09-03-SUMMARY.md` when done.
</output>
