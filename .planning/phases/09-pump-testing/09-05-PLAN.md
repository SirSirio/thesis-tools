---
phase: 09-pump-testing
plan: 05
type: execute
wave: 4
depends_on: ["09-03"]
files_modified:
  - index.html
  - README.md
  - ROADMAP.md
  - CLAUDE.md
autonomous: true
requirements: [D-01, D-03]
must_haves:
  truths:
    - "The landing page (index.html) shows a new tool-card for the Pump Testing Protocol linking tools/pump-testing/index.html, with EN+IT data-i18n keys matching the bilingual landing-page convention"
    - "README.md tool table has a new row for the Pump Testing Protocol marked Live"
    - "The repo-root ROADMAP.md Shipped section has a row for the Pump Testing Protocol"
    - "CLAUDE.md folder-structure tree documents tools/pump-testing/ (index.html + SPEC.md + katex/), and the closing chrome describes the tool as document-first / no-calculator (D-01) with the deep-accuracy + lighter-map framing (D-03)"
  artifacts:
    - path: "index.html"
      provides: "Landing-page tool-card for the pump-testing tool"
      contains: "tools/pump-testing/index.html"
    - path: "README.md"
      provides: "Tool-table row"
    - path: "ROADMAP.md"
      provides: "Shipped-section row"
    - path: "CLAUDE.md"
      provides: "Folder-structure entry"
  key_links:
    - from: "index.html"
      to: "tools/pump-testing/index.html"
      via: "tool-card href"
      pattern: "tools/pump-testing/index.html"
---

<objective>
Integrate the new pump-testing tool into the site chrome: add the landing-page tool-card (EN+IT i18n), the README tool-table row, the repo-root ROADMAP.md Shipped row, and the CLAUDE.md folder-structure entry.

This is the standard closing step every tool phase performs. Copy accurately describes the document-first (D-01), deep-accuracy-plus-lighter-map (D-03) tool — no calculator language.

Purpose: Make the tool discoverable and documented in the site's index, README, roadmap, and project map.
Output: Updated `index.html`, `README.md`, `ROADMAP.md`, `CLAUDE.md`.
</objective>

<execution_context>
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/workflows/execute-plan.md
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-pump-testing/09-CONTEXT.md
@CLAUDE.md
@README.md
@ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the landing-page tool-card with EN+IT i18n keys</name>
  <files>index.html</files>
  <read_first>
    - index.html (the Tools grid around lines 639-663 — the dispense-throughput-simulator card is the current last card at `--rd: 0.5s`; and the LANG.en / LANG.it inline dictionaries where `card-title-dispense-sim` / `card-desc-dispense-sim` are defined)
    - .planning/phases/09-pump-testing/09-CONTEXT.md (D-01 document-first, D-03 depth framing — the card copy must describe a protocol document, not a calculator)
  </read_first>
  <action>
    Append a new `<a class="tool-card reveal">` after the dispense-throughput-simulator card (before the closing `</div>` of the tools grid at line 663), linking `tools/pump-testing/index.html`, with the next stagger step (`--rd: 0.58s`) and a fitting icon glyph (a balance/scale glyph such as U+2696 ⚖ to signal gravimetric weighing). Give the title an `data-i18n="card-title-pumptest"` and the description `data-i18n="card-desc-pumptest"`, and add the `card-link` "Open tool" span using the existing `card-link-tool` key. Add the two new keys `card-title-pumptest` and `card-desc-pumptest` to BOTH the LANG.en and LANG.it inline dictionaries (identical key sets — the landing page is bilingual). The copy must describe a document-first, QR-citable dispensing-accuracy qualification protocol: the market-grade ISO 23783-2 gravimetric method + ISO 8655 pipette-equivalence benchmark as the deep core, the lighter go-to-market map, and a deferred prototype-protocol slot — with NO calculator/planner language (D-01). Provide a faithful Italian translation for the IT dictionary entries.
  </action>
  <acceptance_criteria>
    - `index.html` contains an `<a>` with `href="tools/pump-testing/index.html"` and class `tool-card`.
    - The card uses `data-i18n="card-title-pumptest"` and `data-i18n="card-desc-pumptest"`.
    - Both `card-title-pumptest` and `card-desc-pumptest` appear at least twice each in `index.html` (once in LANG.en, once in LANG.it).
    - The card copy contains no calculator/planner wording (no "calculator", no "planner").
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'f=index.html; grep -q "tools/pump-testing/index.html" "$f" && grep -q "card-title-pumptest" "$f" && [ "$(grep -c "card-title-pumptest" "$f")" -ge 2 ] && [ "$(grep -c "card-desc-pumptest" "$f")" -ge 2 ] && echo PASS'</automated>
  </verify>
  <done>The landing page shows a bilingual pump-testing tool-card linking the new tool, with no calculator language.</done>
</task>

<task type="auto">
  <name>Task 2: Add README row, repo-root ROADMAP row, and CLAUDE.md folder-structure entry</name>
  <files>README.md, ROADMAP.md, CLAUDE.md</files>
  <read_first>
    - README.md (tool table at lines 13-22; the dispense-throughput-simulator row is line 20)
    - ROADMAP.md (repo-root; `## Shipped` section at line 7 and the Planned/Ideas sections)
    - CLAUDE.md (the folder-structure `tools/` tree — dispense-throughput-simulator is the current last tool block; and the shared-resources / tool-spec-standard prose)
    - .planning/phases/09-pump-testing/09-CONTEXT.md (D-01/D-03 for accurate, no-calculator description)
  </read_first>
  <action>
    In `README.md`, add a new tool-table row (same column shape: `[Pump Testing Protocol](tools/pump-testing/index.html) | <description> | ✅ Live`) after the dispense-throughput-simulator row; description = a document-first ISO 23783-2 Annex D gravimetric + ISO 8655 pipette-equivalence dispensing-accuracy qualification protocol (deep accuracy metrology core: balance grades, environmental limits, evaporation handling, mass->volume Z-factor, replicate convention, trueness/precision, reporting) plus a lighter go-to-market map and a deferred prototype-protocol slot; English interface; no calculator. In the repo-root `ROADMAP.md`, add a matching row to the `## Shipped` table (same two-column shape as the existing Shipped rows). In `CLAUDE.md`, add a `tools/pump-testing/` block to the folder-structure tree (index.html — the protocol page; SPEC.md — co-located spec; katex/ — local KaTeX fallback copied from the displaced-volume model), appended after the dispense-throughput-simulator block, and adjust the tree connector on the dispense-throughput-simulator block from terminal to mid-list so pump-testing becomes the new last tool block (mirror the connector convention used previously). Keep copy consistent with D-01 (document-first, no calculator) and D-03 (deep accuracy + lighter map). Do NOT edit `assets/style.css`.
  </action>
  <acceptance_criteria>
    - `README.md` contains a row with `tools/pump-testing/index.html` marked `✅ Live`.
    - `ROADMAP.md` (repo root) contains a Shipped row referencing the Pump Testing Protocol.
    - `CLAUDE.md` folder-structure tree contains `tools/pump-testing/` with both `index.html` and `SPEC.md` and a `katex/` note.
    - None of the three files contains calculator/planner wording for this tool.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'grep -q "tools/pump-testing/index.html" README.md && grep -qi "pump testing\|pump-testing" ROADMAP.md && grep -q "tools/pump-testing/" CLAUDE.md && grep -qi "SPEC.md" CLAUDE.md && echo PASS'</automated>
  </verify>
  <done>README, repo-root ROADMAP, and CLAUDE.md all document the new pump-testing tool consistently, with no calculator language.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Edits static HTML/markdown index, readme, roadmap, and project-map files. No executable surface, no server, no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | Tampering / Integrity | Landing-page i18n dictionaries | mitigate | Acceptance requires both `card-title-pumptest` and `card-desc-pumptest` to appear in both LANG.en and LANG.it so a missing IT key cannot ship a broken bilingual card. |
| T-09-10 | Tampering | assets/style.css byte-stability | mitigate | Action forbids editing `assets/style.css`; card reuses existing `.tool-card` classes only. |
</threat_model>

<verification>
- Landing page renders the new bilingual card; clicking it opens `tools/pump-testing/index.html`.
- README, repo-root ROADMAP, and CLAUDE.md are updated consistently.
- `assets/style.css` is byte-unchanged.
- No calculator/planner language anywhere in the chrome copy.
</verification>

<success_criteria>
The pump-testing tool is discoverable from the landing page (bilingual card), documented in README and the repo-root ROADMAP Shipped section, and recorded in the CLAUDE.md folder-structure tree — all copy consistent with the document-first, deep-accuracy-plus-lighter-map framing.
</success_criteria>

<output>
Create `.planning/phases/09-pump-testing/09-05-SUMMARY.md` when done.
</output>
