---
phase: 09-pump-testing
plan: 06
type: execute
wave: 4
depends_on: ["09-03"]
files_modified:
  - tools/pump-testing/index.html
autonomous: true
requirements: [D-01, D-02, D-04]
must_haves:
  truths:
    - "Section 10 (#prototype-protocol-slot) is populated as a visually-distinct Layer-2 block titled as the actual prototype protocol and justified deviations — no longer a placeholder (D-04, superseded 2026-07-23 once the user supplied TEST-PROTOCOL.md)"
    - "The bottom layer presents the two-axis model — accuracy (mean uL/stroke) is calibratable, precision (CV = SD/mean) is NOT — as the reason precision is the fundamental figure of merit"
    - "The bottom layer states the 0.1 mg balance binding constraint and its three workarounds: mean-from-slope (>=200 uL compliant points), per-stroke CV via root-N, and same-instrument interleaved pipette comparison"
    - "The ISO 23783-2 requirement | prototype reality | justification deviation table is rendered with real rows (balance grade, evaporation blanks, pre-rinse, thermal logging, Z-factor, n=10) and cross-links each row to the top-layer section it deviates from"
    - "The pipette head-to-head thesis claim is stated: precision is the un-fakeable metric, so a pump CV <= manual-pipette CV at 5 uL means the device beats the human it replaces even if the mean needs calibration"
    - "An honest can/cannot-claim scope is present (cannot certify CV <= 5% at n=10; single build; root-N inference; ideal water-only conditions), keeping the tool document-first (no calculator, no input fields) per D-01/D-02"
  artifacts:
    - path: "tools/pump-testing/index.html"
      provides: "Layer-2 bottom section: actual proto-02 v2.3 test protocol + justified deviations"
      contains: "prototype-protocol-slot"
  key_links:
    - from: "tools/pump-testing/index.html #prototype-protocol-slot"
      to: "tools/pump-testing/index.html #balance-requirements"
      via: "deviation-table row cross-links to the top-layer section each deviation departs from"
      pattern: "#balance-requirements|#method-gravimetric|#mass-to-volume|#replicates"
    - from: "tools/pump-testing/index.html #prototype-protocol-slot"
      to: "prototypes/Prototype-1-Pump-Module/proto-02-5ul-4roller-v2/TEST-PROTOCOL.md"
      via: "the bottom layer is authored from the supplied prototype test protocol"
      pattern: "TEST-PROTOCOL|proto-02"
---

<objective>
Populate Section 10 (`#prototype-protocol-slot`) with the LAYER-2 bottom layer: the actual test protocol used on proto-02 v2.3 and the reasoned justification for every deviation from the market-grade ideal documented in the top layer. This is the two-layer counterpart the user asked to preserve literally.

The user supplied the actual protocol (`prototypes/Prototype-1-Pump-Module/proto-02-5ul-4roller-v2/TEST-PROTOCOL.md`) on 2026-07-23, which supersedes D-04's original deferral: the bottom layer is now IN scope and is this plan's deliverable. The centerpiece is the ISO 23783-2 requirement | prototype reality | justification deviation table (TEST-PROTOCOL.md §7), each row cross-linked to the top-layer section it departs from.

This stays document-first (D-01): prose + spec tables + expandable sections, KaTeX for the few formulas, NO calculator and NO input fields. It is a thesis artifact defending why the prototype's method justifiably deviates (D-02).

Purpose: Deliver the actual-protocol layer as the honest, reasoned counterpart to the righteous top layer.
Output: `tools/pump-testing/index.html` Section 10 populated as a visually-distinct Layer-2 block.
</objective>

<execution_context>
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/workflows/execute-plan.md
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-pump-testing/09-CONTEXT.md
@.planning/phases/09-pump-testing/09-RESEARCH.md
@prototypes/Prototype-1-Pump-Module/proto-02-5ul-4roller-v2/TEST-PROTOCOL.md
@prototypes/Prototype-1-Pump-Module/proto-02-5ul-4roller-v2/PROTOTYPE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Open the Layer-2 block — framing, the two-axis accuracy/precision model, and the 0.1 mg-balance constraint + three workarounds</name>
  <files>tools/pump-testing/index.html</files>
  <read_first>
    - tools/pump-testing/index.html (the file being modified — Section 10 shell #prototype-protocol-slot from 09-01; the top-layer anchors #balance-requirements, #method-gravimetric, #mass-to-volume, #replicates, #trueness-precision to link to)
    - prototypes/Prototype-1-Pump-Module/proto-02-5ul-4roller-v2/TEST-PROTOCOL.md (§1 two-axis accuracy-vs-precision model; §2 the binding-constraint balance analysis and the three tricks — slope method, per-stroke CV via root-N, same-instrument pipette comparison)
    - .planning/phases/09-pump-testing/09-CONTEXT.md (D-01 document-first, D-02 thesis artifact, D-04 superseded — bottom layer now in scope)
  </read_first>
  <action>
    Convert `#prototype-protocol-slot` (Section 10) from the empty shell into a populated, visually-DISTINCT Layer-2 block (a distinct wrapper class so it reads unmistakably as the second layer — the actual method, not the ideal). Title it clearly, e.g. "Layer 2 — The Actual Prototype Protocol and Justified Deviations", with a one-paragraph bridge: the protocol above is the market-grade ideal; below is the method actually used on proto-02 v2.3 and the reasoned justification for every deviation, at prototype stage (single build, coarse balance). State the measured baseline that motivates it: v2.3 delivered 2.95 uL/stroke at 180 rpm (-41 percent), with no missed motor steps confirmed (revolution-count check) — so the deficit is volumetric, a real property to characterise, not a step-loss artefact.

    Then author the two-axis model as a small table plus prose: Accuracy (mean uL/stroke vs 5 uL) is calibratable — a step-count multiplier, off by 41 percent means command 41 percent more steps; Precision (CV = SD/mean) is NOT calibratable — random scatter cannot be tuned away. Conclude that precision is therefore the fundamental figure of merit (the CV <= 5 percent pass gate) while accuracy only needs to be known, not perfect. Render CV = SD/mean with KaTeX.

    Then the binding constraint: the lab balance reads to 0.1 mg (0.0001 g). Present the Table-3-vs-our-balance compliance comparison (link to the top-layer `#balance-requirements` Table 3): our balance is metrologically honest only for deliveries >= 200 uL; a single 5 uL dose = 5 mg against a ~0.1 mg per-weighing floor is a ~2-3 percent floor on every reading. Then the three workarounds that make a coarse balance sufficient: (1) mean-from-slope — fit delivered = m*strokes + b using only the compliant (>=200 uL) points, so the per-stroke volume m rides on the slope, not on a 5 mg reading; (2) per-stroke CV via root-N — measure CV on a large 300-stroke collection where the balance is clean, then CV_stroke = CV(collection) * sqrt(N), valid if strokes are independent; (3) same-instrument pipette comparison — weigh the pump's and a calibrated pipette's 5 uL on the SAME balance, same method, so the scale's systematic error biases both equally and cancels in the comparison. Render the root-N scaling and the variance-split SD_observed^2 = SD_pump^2 + SD_scale^2 with KaTeX. No `<input>` elements; expand/collapse via `<details>`/`<summary>` only; keep tables inside cards that wrap/scroll internally (no page-level horizontal scroll).
  </action>
  <acceptance_criteria>
    - `#prototype-protocol-slot` is populated (no "pending"/"to be filled" placeholder language remains for this section) and carries a distinct Layer-2 wrapper class.
    - The two-axis model is present: contains "calibratable" and "CV" and states precision is not calibratable (a negation near "calibratable").
    - The measured baseline is stated (contains "2.95" and "-41" or "41 percent" / "41%").
    - The binding-constraint section names the "0.1 mg" balance and cross-links to the top-layer balance section (contains "#balance-requirements").
    - The three workarounds are all present (contains "slope", "sqrt" or "root-N" or "√N", and "pipette").
    - No `<input>` element exists anywhere on the page.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'f=tools/pump-testing/index.html; grep -qi "calibratable" "$f" && grep -q "2.95" "$f" && grep -q "0.1 mg" "$f" && grep -q "#balance-requirements" "$f" && grep -qi "slope" "$f" && grep -qi "pipette" "$f" && grep -qi "sqrt\|root-n\|√n" "$f" && ! grep -qi "<input" "$f" && echo PASS'</automated>
  </verify>
  <done>Section 10 opens as a distinct Layer-2 block with the two-axis model, the measured baseline, the 0.1 mg constraint, and the three workarounds — cross-linked to the top-layer balance section.</done>
</task>

<task type="auto">
  <name>Task 2: The deviation table (ISO requirement | prototype reality | justification) cross-linked to top-layer sections, plus condensed design rationale</name>
  <files>tools/pump-testing/index.html</files>
  <read_first>
    - tools/pump-testing/index.html (the file being modified — top-layer anchors to cross-link: #balance-requirements, #method-gravimetric, #mass-to-volume, #replicates)
    - prototypes/Prototype-1-Pump-Module/proto-02-5ul-4roller-v2/TEST-PROTOCOL.md (§7 the deviation table — the centerpiece; §3 why these volumes; §4 the speed check / refill knee; §5 why fewer replicates at low volume)
    - .planning/phases/09-pump-testing/09-RESEARCH.md (the top-layer section anchors and what each documents, so the cross-links point to the correct section)
  </read_first>
  <action>
    Author the deviation table inside the Layer-2 block as a three-column table titled to match TEST-PROTOCOL.md §7: "ISO 23783-2 requires | We do | Justification". Include these six real rows, cross-linking the requirement cell to the top-layer section that documents that requirement: (1) Balance 0.001 mg at 5 uL, Table 3 (link `#balance-requirements`) -> we use 0.1 mg -> slope method + same-scale pipette comparison sidestep absolute low-volume accuracy; (2) >=10 evaporation blanks, D.5.2 (link `#method-gravimetric`) -> 3-5 blanks/session, fast weigh -> cheap, saves the low-volume data, fast-tare limits evaporation; (3) Pre-rinse tube >=5x, 6.3 (link `#method-gravimetric`) -> keep -> free, fixes the run-to-run non-stationarity seen 2026-07-16; (4) Thermal equilibrium 2 h + log T/RH/P, 6.3 / Table D.1 (link `#method-gravimetric`) -> note room temp start/end -> enough to flag drift at prototype scale; (5) Z-factor density + buoyancy, Annex A (link `#mass-to-volume`) -> assume 1.00 mg/uL -> <0.3 percent vs a 5 percent budget, negligible and documented; (6) n=10 per volume, Part 3 / ISO 8655 convention (link `#replicates`) -> n = 3 / 5 / 10 by volume -> prototype means, not certification, stated as such. Add a one-line lead framing the table as "deviations documented, not hidden — a stronger methods position than pretending to a compliance we do not have."

    Then a CONDENSED design-rationale block (expandable `<details>` acceptable) capturing the three most defensible choices from TEST-PROTOCOL.md §3-5 in brief: (a) volumes are set by stroke count (1 / 100 / 300 strokes) so the DELIVERED volume lands >= 200 uL on the compliance map even at the pessimistic ~3 uL/stroke, giving >=2 compliant points for the slope; (b) the 60/120/180 (+optional 240) rpm speed sweep probes tube-refill limitation — a dynamic deficit invisible to calipers and the prime remaining suspect for the -41 percent once step-loss is ruled out; (c) replicates go where the scatter is real signal (n=3 low, n=5 medium, n=10 high) because replicates fight randomness, not resolution — at 5 uL the scatter is scale-dominated, so more low-volume reps buy little for the pump CV. Keep it tight; do not reproduce the full document. No `<input>`; tables wrap/scroll inside their card.
  </action>
  <acceptance_criteria>
    - The deviation table exists with the header cells "We do" and "Justification" (or an unambiguous equivalent) and at least the six requirement rows.
    - The table cross-links to the top-layer sections: contains "#balance-requirements", "#method-gravimetric", "#mass-to-volume", and "#replicates".
    - The Z-factor row states the 1.00 mg/uL assumption and the <0.3 percent negligible-vs-5-percent-budget justification (contains "1.00" and "0.3").
    - The n=10 row states the prototype n = 3 / 5 / 10 by volume (contains "3 / 5 / 10" or "3/5/10").
    - The condensed rationale names the refill/speed mechanism (contains "refill") and the replicates-fight-randomness-not-resolution point (contains "resolution").
    - No `<input>` added.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'f=tools/pump-testing/index.html; grep -qi "justification" "$f" && grep -q "#balance-requirements" "$f" && grep -q "#method-gravimetric" "$f" && grep -q "#mass-to-volume" "$f" && grep -q "#replicates" "$f" && grep -q "1.00" "$f" && grep -qi "refill" "$f" && grep -qi "resolution" "$f" && grep -Eq "3 ?/ ?5 ?/ ?10" "$f" && ! grep -qi "<input" "$f" && echo PASS'</automated>
  </verify>
  <done>The deviation table renders six real rows cross-linked to the top-layer sections, plus a condensed volumes/speed/replicates rationale.</done>
</task>

<task type="auto">
  <name>Task 3: The pipette head-to-head thesis claim and the honest can/cannot-claim scope</name>
  <files>tools/pump-testing/index.html</files>
  <read_first>
    - tools/pump-testing/index.html (the file being modified — link back to the top-layer `#trueness-precision` benchmark section)
    - prototypes/Prototype-1-Pump-Module/proto-02-5ul-4roller-v2/TEST-PROTOCOL.md (§6 the pipette head-to-head and its validity guards; §8 the screen-then-lock two-stage strategy; §11 honest scope — can/cannot)
    - .planning/phases/09-pump-testing/09-CONTEXT.md (D-02 thesis artifact; the deviations should read as reasoned engineering judgment, not apology)
  </read_first>
  <action>
    Author the pipette head-to-head as the thesis point of the Layer-2 block: the device exists to replace a human pipetting by hand, so the fair benchmark is the human it replaces, not ISO perfection. State the three-step argument: (1) accuracy/mean is calibratable so a low mean is not a real defect; (2) precision/CV is NOT calibratable so it is the true honest quality of the mechanism; (3) therefore if the pump's CV at 5 uL is <= the manual-pipette CV at 5 uL, the device is more consistent than the person it replaces on the one metric that cannot be faked. State why it is valid on a coarse balance (both weighed on the SAME 0.1 mg balance, same vessel, interleaved, so the scale's systematic error cancels in the comparison) and the guards that keep it honest (the ~0.1 mg floor sits under both CVs — the 50 uL pipette point checks separability; frame as "vs manual pipetting under matched conditions" and back it with the ISO 8655 pipette band CV ~1.5-3 percent at 5 uL; n=10 each). Link the ISO 8655 benchmark reference to the top-layer `#trueness-precision` section. Frame the whole thing as reasoned engineering judgment, not apology.

    Then the honest scope as a two-column "Can claim / Cannot claim" block: CAN — characterise per-stroke volume and its speed/volume dependence to good precision on the mean; locate the operating point and retro-explain the -41 percent; show head-to-head whether the pump's precision at the operating dose is comparable-to-or-better-than manual pipetting. CANNOT (state before an examiner does) — certify CV <= 5 percent (at n=10 the CV carries ~+-25 percent; a defensible pass needs the Stage-2 n~30 deep-dive, so report "CV ~ X percent, prototype estimate" not "meets <= 5 percent"); prove the design (single build/tube/session, no cross-build reproducibility); directly measure single-dose precision (it is inferred via root-N, assuming independent strokes, guarded by the CV-vs-volume plateau test); speak to real-use conditions (water only, no backpressure/needle/real reagent). Optionally note the two-stage strategy: Stage 1 screens and finds the operating point; Stage 2 spends the replicate budget (n~30) at the winning condition to CLAIM <= 5 percent, and the lasting output is a fixed reusable reference protocol. Keep document-first — no `<input>`, no computation; `<details>` for any collapsible detail.
  </action>
  <acceptance_criteria>
    - The pipette head-to-head is present: contains "pipette" and states the CV-comparison claim (contains "cannot be faked" or an equivalent statement that precision is the un-fakeable metric) and the ISO 8655 pipette band "1.5" and "3" percent at 5 uL.
    - The head-to-head links back to the top-layer benchmark section (contains "#trueness-precision").
    - A Can/Cannot scope block is present: contains "Cannot" (or "cannot claim") and states CV <= 5 percent cannot be certified at n=10 (contains "n=30" or "n ~ 30" or "30" near a certify/deep-dive statement, and "25 percent" or "+-25").
    - The scope states water-only / ideal conditions as a limit (contains "water only" or "backpressure").
    - No `<input>` added; no page-level horizontal scroll at 1280px/375px.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'f=tools/pump-testing/index.html; grep -qi "pipette" "$f" && grep -qi "cannot be faked\|un-fakeable\|cannot be faked\|not.*calibratable" "$f" && grep -q "1.5" "$f" && grep -q "#trueness-precision" "$f" && grep -qi "cannot" "$f" && grep -Eqi "n ?[~=] ?30|n~30|\b30\b" "$f" && grep -qi "water only\|backpressure" "$f" && ! grep -qi "<input" "$f" && echo PASS'</automated>
  </verify>
  <done>The Layer-2 block closes with the pipette head-to-head thesis claim (linked to the top-layer benchmark) and an honest can/cannot-claim scope — document-first, no calculator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none of consequence) | Adds static prose, a deviation table, and KaTeX-rendered formulas to an existing offline page. No server, API, auth, persistence, or untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | Tampering / Integrity | Documented prototype values | accept | All values are transcribed from the author's own TEST-PROTOCOL.md; static author-controlled content, no user data, no secrets. |
| T-09-10 | Elevation / XSS | Client-side KaTeX rendering | accept | KaTeX renders author-written math only; no untrusted input is rendered. Consistent with 09-01's disposition. |
| T-09-11 | Scope | Document-first boundary (D-01) | mitigate | Acceptance criteria assert zero `<input>` elements and `<details>`-only interactivity, keeping the bottom layer document-first (no live calculator — that stays deferred to a future phase). |
</threat_model>

<verification>
- Section 10 renders offline as a distinct Layer-2 block with formulas from the local katex/ folder.
- The deviation table has real rows cross-linked to the top-layer sections (#balance-requirements, #method-gravimetric, #mass-to-volume, #replicates).
- The pipette head-to-head and the can/cannot scope are present and honest.
- No `<input>` elements anywhere (document-first, D-01/D-02); no horizontal scroll at 1280px/375px.
- assets/style.css is byte-unchanged.
</verification>

<success_criteria>
The bottom layer is authored: a visually-distinct Layer-2 block presenting proto-02 v2.3's actual test method, the two-axis accuracy/precision model, the 0.1 mg-balance workarounds, the ISO-requirement | prototype-reality | justification deviation table cross-linked to the top layer, the pipette head-to-head thesis claim, and the honest can/cannot-claim scope — document-first, no calculator, faithful to the user-supplied TEST-PROTOCOL.md.
</success_criteria>

<output>
Create `.planning/phases/09-pump-testing/09-06-SUMMARY.md` when done.
</output>
