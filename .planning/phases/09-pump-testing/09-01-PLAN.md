---
phase: 09-pump-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/pump-testing/index.html
  - tools/pump-testing/katex/katex.min.css
  - tools/pump-testing/katex/katex.min.js
  - tools/pump-testing/katex/auto-render.min.js
autonomous: true
requirements: [D-01, D-02]
must_haves:
  truths:
    - "A reader opens tools/pump-testing/index.html and sees a document-first protocol page (no calculator, no input fields) with the site's dark glassmorphic chrome and a '← All tools' back-link (D-01, D-02)"
    - "The page presents the dual-frame verdict as visible prose: ISO 23783-2 Annex D is the applicable gravimetric TEST METHOD; ISO 8655 is the analogical PERFORMANCE BENCHMARK"
    - "All ten top-layer section anchors exist so later plans can fill them without restructuring"
    - "The gravimetric method section states the verified Annex D preparation and environment values (Table D.1, pre-rinse >=5x, thermal equilibrium +-2C/>=2h, evaporation blank >=10 cycles)"
    - "The balance-requirements section renders Table 3 as a static table with the 0.5-20 microlitre row (0.001 mg semi-micro) highlighted"
    - "KaTeX resolves from the local katex/ folder, so formulas render with no network (offline/USB safe)"
  artifacts:
    - path: "tools/pump-testing/index.html"
      provides: "Document-first protocol page shell + framing + gravimetric method + balance requirements"
      min_lines: 200
      contains: "prototype-protocol-slot"
    - path: "tools/pump-testing/katex/katex.min.js"
      provides: "Vendored KaTeX (local, no CDN dependency)"
  key_links:
    - from: "tools/pump-testing/index.html"
      to: "tools/pump-testing/katex/katex.min.css"
      via: "local stylesheet link"
      pattern: "katex/katex.min"
    - from: "tools/pump-testing/index.html"
      to: "../../index.html"
      via: "All tools nav back-link"
      pattern: "All tools"
---

<objective>
Build the document-first tool-page shell for `tools/pump-testing/`, vendor KaTeX locally, and author the first three top-layer sections: the dual-frame framing intro (Section 1), the single-channel gravimetric method (Section 2, ISO 23783-2 Annex D), and the balance requirements (Section 3, Table 3).

This plan honors D-01 (document-first: prose + spec tables + expandable sections, no calculator, no interactive planner) and D-02 (thesis artifact; no speculative calculation logic). The page is a written protocol citable via QR like a thesis chapter.

Purpose: Establish the offline-safe page skeleton and the full ten-section anchor scaffold so the metrological-core plan (09-02), the alternates/go-to-market plan (09-03), and the Layer-2 prototype-protocol plan (09-06) fill sections without touching structure.
Output: `tools/pump-testing/index.html` (shell + Sections 1-3), `tools/pump-testing/katex/` (vendored KaTeX).
</objective>

<execution_context>
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/workflows/execute-plan.md
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-pump-testing/09-CONTEXT.md
@.planning/phases/09-pump-testing/09-RESEARCH.md
@CLAUDE.md
@tools/peristaltic-roller-displaced-volume-model/index.html
@tools/rotor-solver/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the tool folder, vendor KaTeX, build the page shell and ten-section anchor scaffold</name>
  <files>tools/pump-testing/index.html, tools/pump-testing/katex/katex.min.css, tools/pump-testing/katex/katex.min.js, tools/pump-testing/katex/auto-render.min.js</files>
  <read_first>
    - tools/rotor-solver/index.html (site conventions: head, `← All tools` nav bar, inline `<style>` reusing `../../assets/style.css` tokens, blobs + animate-in pattern)
    - tools/peristaltic-roller-displaced-volume-model/index.html (KaTeX load block near lines 9-11 and 1051-1066, `renderMathInElement` config, and the `katex/` local-folder precedent)
    - CLAUDE.md (offline/no-CDN rule, no-horizontal-scroll rule, keep assets/style.css byte-stable, co-located SPEC.md standard)
    - .planning/phases/09-pump-testing/09-RESEARCH.md ("Recommended Top-Layer Section Structure" — the 10-section ordering)
  </read_first>
  <action>
    Create the `tools/pump-testing/` folder. Copy the three existing files from `tools/peristaltic-roller-displaced-volume-model/katex/` (katex.min.css, katex.min.js, auto-render.min.js) into `tools/pump-testing/katex/` — this is a file copy of already-vendored assets, no download, no package manager. Build `tools/pump-testing/index.html` as a self-contained page linking only `../../assets/style.css` (shared) plus an inline `<style>` block for tool-local rules; do NOT edit `assets/style.css`. Include the standard `← All tools` nav bar linking `../../index.html` (use the 'All tools' label per the thesis-timeline precedent), the shared blobs + fade-up animate-in pattern, and a `<title>` of the form "Pump Testing Protocol — Thesis Tools". Load KaTeX from the LOCAL `katex/` path as the primary source (href `katex/katex.min.css`, script src `katex/katex.min.js` and `katex/auto-render.min.js`), NOT a bare CDN reference, so the page renders offline from USB (CLAUDE.md offline mandate); wire `renderMathInElement(document.body, ...)` on load mirroring the displaced-volume-model config. Lay down the full ten-section scaffold as empty `<section>` shells each carrying its anchor id, a heading, and a short placeholder comment, in this exact order and with these exact ids: `#framing` (1), `#method-gravimetric` (2), `#balance-requirements` (3), `#mass-to-volume` (4), `#replicates` (5), `#trueness-precision` (6), `#reporting-uncertainty` (7), `#alternate-methods` (8), `#go-to-market` (9), `#prototype-protocol-slot` (10). Add a compact table-of-contents / on-page nav linking those ten anchors. The page is English-only (per RESEARCH i18n recommendation matching the displaced-volume-model precedent) — do NOT add a language toggle. Introduce NO `<input>` elements and NO compute/recompute scripts (D-01 document-first, D-02 no speculative calculation). Expand/collapse, where used, must use `<details>`/`<summary>`, not JS-driven input state.
  </action>
  <acceptance_criteria>
    - `tools/pump-testing/index.html` and `tools/pump-testing/katex/katex.min.js` both exist.
    - `index.html` references `katex/katex.min` (local path) for both CSS and JS; the page does not depend on a CDN as the sole KaTeX source.
    - `index.html` contains the exact anchor ids: framing, method-gravimetric, balance-requirements, mass-to-volume, replicates, trueness-precision, reporting-uncertainty, alternate-methods, go-to-market, prototype-protocol-slot.
    - `index.html` contains the `← All tools` nav linking `../../index.html`.
    - `index.html` links `../../assets/style.css` and contains no edit to `assets/style.css`.
    - `index.html` contains zero `<input` elements (document-first, no calculator).
    - No language-toggle control (`data-i18n` dictionary / lang `<select>`) is present.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'f=tools/pump-testing/index.html; test -f "$f" && test -f tools/pump-testing/katex/katex.min.js && grep -q "katex/katex.min" "$f" && for a in framing method-gravimetric balance-requirements mass-to-volume replicates trueness-precision reporting-uncertainty alternate-methods go-to-market prototype-protocol-slot; do grep -q "id=\"$a\"" "$f" || { echo "missing anchor $a"; exit 1; }; done && grep -q "All tools" "$f" && ! grep -qi "<input" "$f" && echo PASS'</automated>
  </verify>
  <done>Folder + vendored KaTeX exist; page shell renders offline with all ten anchors, nav, and no input fields.</done>
</task>

<task type="auto">
  <name>Task 2: Author Section 1 (dual-frame framing) and Section 2 (gravimetric method, Annex D)</name>
  <files>tools/pump-testing/index.html</files>
  <read_first>
    - tools/pump-testing/index.html (the file being modified)
    - .planning/phases/09-pump-testing/09-RESEARCH.md (RQ-1 verdict prose; RQ-4 verified Table D.1, prep §6.3, evaporation-blank D.5.2; "Recommended Top-Layer Section Structure" items 1-2)
    - .planning/phases/09-pump-testing/09-CONTEXT.md (D-01 document-first, the two-layer framing the user asked to preserve literally)
  </read_first>
  <action>
    Fill `#framing` (Section 1) with visible prose stating the dual-frame verdict as the tool's thesis-defensible reasoning: ISO 23783-2 Annex D is the literally-applicable single-channel gravimetric TEST METHOD (the pump is an automated liquid handling system by §1 scope), while ISO 8655 (Parts 2/6) is the analogically-applicable PERFORMANCE BENCHMARK (the device replaces a hand pipette). State honestly the correction from RESEARCH: ISO 23783-2 Part 2 does NOT set device pass/fail tolerances (§9 delegates to ISO 23783-3, which the project does not hold), and Table 1 figures are the method's own achievable uncertainty, not a device spec. Introduce the two-layer structure (top = market-grade righteous protocol; bottom = the actual prototype protocol and justified deviations, Layer 2, authored by plan 09-06 into `#prototype-protocol-slot`). Fill `#method-gravimetric` (Section 2) with the Annex D single-channel gravimetric procedure: apparatus, test liquid (ISO 3696 grade 3 water), preparation per §6.3 (pre-rinse tips >=5x to waste on every tip change, thermal equilibrium +-2C for >=2h, balance settle time >=6s), environmental conditions rendered as a static Table D.1 (water 17-30C / change <=1C; air 17-30C / change <=3C; RH 45-70% / change <=10%), and the evaporation-blank workflow (>=10 blank cycles at the same cycle time before test-liquid measurements begin, per D.5.2). Cite ISO 23783-2 Annex D throughout. Keep all wide tables inside a card that wraps or scrolls internally so there is no page-level horizontal scroll at 1280px and 375px.
  </action>
  <acceptance_criteria>
    - `#framing` contains prose naming both "23783" and "8655" and the words "test method" and "benchmark", and states that Part 2 does not set device pass/fail tolerances (mentions 23783-3 as the delegated part).
    - `#method-gravimetric` renders a Table D.1 with the values 17, 30, 45, 70 present and states the evaporation blank is >=10 cycles and pre-rinse >=5.
    - Section 2 cites "Annex D" and "ISO 3696".
    - No page-level horizontal scroll is introduced (wide tables wrap/scroll within their card).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'f=tools/pump-testing/index.html; grep -q "23783" "$f" && grep -q "8655" "$f" && grep -qi "test method" "$f" && grep -qi "benchmark" "$f" && grep -q "23783-3" "$f" && grep -q "Annex D" "$f" && grep -q "ISO 3696" "$f" && grep -q "17" "$f" && grep -q "70" "$f" && echo PASS'</automated>
  </verify>
  <done>Sections 1-2 present the dual-frame verdict and the Annex D method with verified prep/environment/evaporation values.</done>
</task>

<task type="auto">
  <name>Task 3: Author Section 3 (balance requirements, Table 3) with the compliant band highlighted</name>
  <files>tools/pump-testing/index.html</files>
  <read_first>
    - tools/pump-testing/index.html (the file being modified)
    - .planning/phases/09-pump-testing/09-RESEARCH.md (RQ-4 "Table 3 — Minimum requirements for balances"; the note that ~5 microlitre falls in the 0.5-20 band -> 0.001 mg semi-micro; proto-02 §8.1 0.1 mg balance failure)
    - .planning/phases/09-pump-testing/09-CONTEXT.md (D-01: a static lookup is allowed only if it earns its place — render Table 3 static, not a widget)
  </read_first>
  <action>
    Fill `#balance-requirements` (Section 3) with the verified Table 3 rendered as a STATIC four-row table (no widget, no input): rows for <0.5 microlitre (readability 0.0001 mg), 0.5 to <20 microlitre (readability 0.001 mg, repeatability 0.006 mg, expanded uncertainty in use k=2 0.012 mg), 20 to <200 microlitre (0.01 mg), and 200 to 10000 microlitre (0.1 mg). Visually highlight the 0.5-20 microlitre row (a distinct row class) as the pump's compliant band, and add a one-line note that the pump's ~5 microlitre/stroke target requires a 0.001 mg (semi-micro) balance. Add a brief forward-reference sentence that the prototype's actual 0.1 mg balance fails this requirement by ~2 orders of magnitude, linking down to the Layer-2 prototype protocol at `#prototype-protocol-slot`. Include Table 3 note (a) ("assumes one delivery of test liquid from a single channel"). Cite ISO 23783-2 Table 3.
  </action>
  <acceptance_criteria>
    - `#balance-requirements` contains the strings "0.001 mg", "0.012 mg", and "0.0001 mg".
    - The 0.5-20 microlitre row carries a distinct highlight class and the section states "0.001 mg" as the compliant requirement and "semi-micro".
    - The section forward-references the prototype slot (contains an anchor link or the text "prototype-protocol-slot" / "0.1 mg").
    - Table 3 is a static table (no `<input>` added anywhere on the page).
    - Section cites "Table 3".
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'f=tools/pump-testing/index.html; grep -q "0.001 mg" "$f" && grep -q "0.012 mg" "$f" && grep -q "0.0001 mg" "$f" && grep -qi "semi-micro" "$f" && grep -q "0.1 mg" "$f" && grep -q "Table 3" "$f" && ! grep -qi "<input" "$f" && echo PASS'</automated>
  </verify>
  <done>Section 3 renders the static Table 3 with the 0.5-20 microlitre compliant band highlighted and a forward-reference to the prototype slot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none of consequence) | Static, offline, single HTML page. No server, no API, no authentication, no persistence, no processing of untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | Vendored KaTeX files copied into `tools/pump-testing/katex/` | accept | Files are copied verbatim from an already-vendored in-repo folder (`tools/peristaltic-roller-displaced-volume-model/katex/`) — no download, no package manager, no new supply-chain surface. |
| T-09-02 | Information Disclosure | Page content | accept | All content is author-controlled static text; no PII, no secrets, no user data collected. |
| T-09-03 | Elevation / XSS | Client-side rendering | accept | XSS surface limited to hard-coded page content the author controls; no untrusted input is rendered; KaTeX renders author-written math only. |

No npm/pip/cargo installs occur in this phase (KaTeX is vendored by file copy), so no package-legitimacy gate applies.
</threat_model>

<verification>
- `tools/pump-testing/index.html` opens offline (via `serve.bat`) with formulas rendering from the local `katex/` folder.
- All ten section anchors are present; Sections 1-3 are populated with verified values.
- No `<input>` elements anywhere (document-first, D-01).
- No horizontal scroll at 1280px and 375px.
- `assets/style.css` is byte-unchanged.
</verification>

<success_criteria>
The tool page shell exists offline-safe with vendored KaTeX, the full ten-section anchor scaffold, and the first three top-layer sections (dual-frame framing, Annex D gravimetric method, Table 3 balance requirements) authored with verified normative values and no calculator.
</success_criteria>

<output>
Create `.planning/phases/09-pump-testing/09-01-SUMMARY.md` when done.
</output>
