---
phase: 07-thesis-showcase-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prototypes/Prototype-1-Pump-Module/proto-02-5ul-4roller-v2/pump-head-web.mp4
  - prototypes/Prototype-1-Pump-Module/proto-02-5ul-4roller-v2/pump-head-poster.jpg
  - assets/fonts/geist/Geist-Bold.woff2
  - assets/fonts/geist/LICENSE.txt
autonomous: true
requirements: []

must_haves:
  truths:
    - "Per D-09, the committed web video is the proto-02 pump-head clip, not the alignment module clip"
    - "Per D-10, only the re-encoded video is committed; the 9.9 MB original stays untracked as the master"
    - "Per D-10, the committed video has no audio track and is between 1 and 3 MB"
    - "Per D-20, a subsetted Geist-Bold woff2 is vendored locally with its SIL OFL 1.1 license file alongside it"
    - "Per D-20, no CDN or Google Fonts URL is used to obtain or serve the font"
  artifacts:
    - path: "prototypes/Prototype-1-Pump-Module/proto-02-5ul-4roller-v2/pump-head-web.mp4"
      provides: "Re-encoded, audio-stripped, faststart H.264 pump-head clip (D-10)"
      max_bytes: 3145728
    - path: "prototypes/Prototype-1-Pump-Module/proto-02-5ul-4roller-v2/pump-head-poster.jpg"
      provides: "Poster frame extracted from the same source clip (no visual jump on play)"
      max_bytes: 102400
    - path: "assets/fonts/geist/Geist-Bold.woff2"
      provides: "Subsetted headline font (D-20)"
      max_bytes: 51200
    - path: "assets/fonts/geist/LICENSE.txt"
      provides: "SIL OFL 1.1 license text — mandatory for redistribution via GitHub Pages and USB (D-20)"
      contains: "SIL OPEN FONT LICENSE"
  key_links:
    - from: "pump-head-web.mp4"
      to: "PrototypePumpHeadV2.3Dispensing.mp4"
      via: "ffmpeg authoring-time re-encode, one-time, not a build step (D-10)"
    - from: "assets/fonts/geist/Geist-Bold.woff2"
      to: "github.com/vercel/geist-font"
      via: "raw.githubusercontent.com download of the official Geist-Bold.ttf, then pyftsubset"
---

<objective>
Produce the three binary assets the landing-page remodel consumes, before any markup work begins.
This plan touches NO HTML — it is pure authoring-time asset preparation, which is why it runs
alone in Wave 1 with zero dependencies.

Purpose: D-10 and D-20 both require one-time local tool runs (ffmpeg, pyftsubset) whose outputs
are committed binaries. Separating them from the markup work keeps the remodel plans focused on
`index.html` and keeps these shell steps independently re-runnable if a size target is missed.

Output: `pump-head-web.mp4`, `pump-head-poster.jpg`, `assets/fonts/geist/Geist-Bold.woff2`,
`assets/fonts/geist/LICENSE.txt`.
</objective>

<execution_context>
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/workflows/execute-plan.md
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-thesis-showcase-landing-page/07-CONTEXT.md
@.planning/phases/07-thesis-showcase-landing-page/07-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-encode the pump-head clip and extract its poster frame</name>
  <files>
    prototypes/Prototype-1-Pump-Module/proto-02-5ul-4roller-v2/pump-head-web.mp4,
    prototypes/Prototype-1-Pump-Module/proto-02-5ul-4roller-v2/pump-head-poster.jpg
  </files>
  <read_first>
    - .planning/phases/07-thesis-showcase-landing-page/07-RESEARCH.md — section "Concrete ffmpeg Command (D-10)" has the verified source specs (720x1280 PORTRAIT, 60fps, 23.24s, 9,899,129 bytes, H.264 + AAC) and the exact command with per-flag rationale; section "Poster Frame (Claude's Discretion — resolved)" has the extraction command and the reasoning for rejecting head-result.jpeg
    - .planning/phases/07-thesis-showcase-landing-page/07-CONTEXT.md — D-09, D-10, and the Claude's Discretion entry on poster choice
  </read_first>
  <action>
    Confirm ffmpeg is present (`ffmpeg -version` exits 0). The source is
    `prototypes/Prototype-1-Pump-Module/proto-02-5ul-4roller-v2/PrototypePumpHeadV2.3Dispensing.mp4`
    and it MUST remain untracked and unmodified — it is the master (D-10).

    Run the re-encode exactly as RESEARCH specifies: scale filter `540:960` (half of the real
    720x1280 portrait source, preserving 9:16 — the source is PORTRAIT, do not assume or force
    16:9 anywhere), `-c:v libx264 -preset slow -crf 26`, `-an` to strip the AAC track per D-10's
    no-audio requirement, `-movflags +faststart`, `-pix_fmt yuv420p`. Output to `pump-head-web.mp4`
    in the same proto-02 folder.

    Check the output size against D-10's 1–3 MB target. If it exceeds 3 MB, re-run with `-crf 28`;
    if it lands under 1 MB with visible quality loss, re-run with `-crf 24`. RESEARCH also offers
    `-r 30` as a secondary size lever (60fps is unnecessary for a slow mechanical subject) — use it
    only if CRF alone cannot reach the target.

    Extract the poster from the SAME source clip with `-ss 8 -frames:v 1 -q:v 3` and the same
    `scale=540:960`, output `pump-head-poster.jpg`. RESEARCH assumption A3 flags the 8s timestamp
    as an unverified guess — view the extracted frame; if the mechanism is blurred, badly framed,
    or mid-camera-settle, adjust `-ss` and re-extract until a frame clearly showing the rotor and
    tube is captured. Do NOT use the untracked repo-root `head-result.jpeg` as the poster: it is a
    result image, not a mechanism frame, and would cause a visual jump the instant playback starts.

    Stage ONLY the two new files. Do not `git add` the 9.9 MB original.
  </action>
  <verify>
    <automated>cd "D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page" && D=prototypes/Prototype-1-Pump-Module/proto-02-5ul-4roller-v2 && test -f "$D/pump-head-web.mp4" && test -f "$D/pump-head-poster.jpg" && S=$(stat -c%s "$D/pump-head-web.mp4") && test "$S" -ge 1000000 -a "$S" -le 3145728 && ffprobe -v error -select_streams a -show_entries stream=codec_type -of csv=p=0 "$D/pump-head-web.mp4" | grep -q . && echo "FAIL: audio track present" && exit 1 || ffprobe -v error -select_streams v:0 -show_entries stream=width,height -of csv=p=0 "$D/pump-head-web.mp4" | grep -q "540,960" && git status --porcelain "$D/PrototypePumpHeadV2.3Dispensing.mp4" | grep -q '^??' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `pump-head-web.mp4` exists in the proto-02 folder and its byte size is >= 1,000,000 and <= 3,145,728 (D-10's 1–3 MB target)
    - `ffprobe -select_streams a` on `pump-head-web.mp4` returns NO audio stream (D-10 `-an`)
    - `ffprobe -select_streams v:0 -show_entries stream=width,height` on `pump-head-web.mp4` returns exactly `540,960` (portrait 9:16 preserved)
    - `pump-head-poster.jpg` exists and is <= 102,400 bytes (same weight class as the 43,102-byte `decks/lab-meeting-2026-06/assets/alignment_poster.jpg` precedent)
    - `git status --porcelain` on `PrototypePumpHeadV2.3Dispensing.mp4` still reports `??` (untracked) — the master was NOT committed (D-10)
    - `head-result.jpeg` is not referenced or copied by this task
  </acceptance_criteria>
  <done>A committed-ready 540x960 audio-free portrait clip in the 1–3 MB band plus a poster frame extracted from that same clip; the 9.9 MB master remains untracked.</done>
</task>

<task type="auto">
  <name>Task 2: Vendor Geist-Bold — download, subset, and ship its OFL license</name>
  <files>assets/fonts/geist/Geist-Bold.woff2, assets/fonts/geist/LICENSE.txt</files>
  <read_first>
    - .planning/phases/07-thesis-showcase-landing-page/07-RESEARCH.md — "Pattern 1: Self-Hosted Subsetted Web Font (D-20)", "Pitfall 4" (the generous-subset command and the reasoning for why an exact-glyph subset is the WRONG call before Sirio's copy edits land), and the verified download URL under Pitfall 4
    - .planning/phases/07-thesis-showcase-landing-page/07-CONTEXT.md — D-20, and the vendoring-shape note pointing at the katex precedent
    - tools/peristaltic-roller-displaced-volume-model/katex/ — list this folder to see the established local-vendoring shape (a plain folder of third-party files next to no build step)
  </read_first>
  <action>
    Confirm `pyftsubset --help` exits 0 (RESEARCH verified fonttools 4.25.0 is installed via the
    project's Anaconda Python — do NOT pip install, and do NOT reach for npx/npm, which this project
    does not have).

    Download the official `Geist-Bold.ttf` (128,824 bytes) from
    `https://raw.githubusercontent.com/vercel/geist-font/main/packages/next/dist/fonts/geist-sans/Geist-Bold.ttf`
    to a scratch location — this is the authoritative vercel/geist-font repo, not a CDN scrape or a
    third-party mirror. Bold/700 is the correct and only weight: it matches `.site-title`'s existing
    `font-weight: 700` exactly (RESEARCH open question 2 resolves Geist to the H1 hero title only).

    Download the license from `https://raw.githubusercontent.com/vercel/geist-font/main/LICENSE.txt`
    and write it verbatim to `assets/fonts/geist/LICENSE.txt`. This is NOT optional housekeeping:
    Geist is SIL OFL 1.1 (RESEARCH verified this against the repo's own LICENSE.txt), the OFL
    requires the license to travel with any redistributed copy of the font, and this site is
    redistributed both via GitHub Pages and on a USB drive handed to people. D-20 calls license
    verification mandatory and non-negotiable.

    Subset with pyftsubset using the GENEROUS range from RESEARCH's Pitfall 4 option (a), not an
    exact-glyph subset: `--unicodes="U+0020-007E,U+00A0-00FF,U+2014,U+2018-201D"`, plus
    `--flavor=woff2 --layout-features='*' --desubroutinize`, output to
    `assets/fonts/geist/Geist-Bold.woff2`. Rationale to preserve: U+00A0-00FF covers the Italian
    accented characters D-18's IT parity needs (à è é ì ò ù); U+2014 is the em-dash, which is NOT in
    Latin-1 Supplement and MUST be included explicitly because Sirio's voice uses em-dashes site-wide
    and CONTEXT.md's Deferred section explicitly rejects stripping them; `--layout-features='*'`
    keeps kerning, which a headline face needs. An exact-glyph subset would silently break the moment
    D-19's Sirio copy-edit pass introduces a character outside the drafted headline.

    Do not modify `assets/style.css` — the `@font-face` declaration is wired inline in `index.html`
    by plan 07-02, not here (D-07, D-20).
  </action>
  <verify>
    <automated>cd "D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page" && test -f assets/fonts/geist/Geist-Bold.woff2 && test -f assets/fonts/geist/LICENSE.txt && grep -qi "SIL OPEN FONT LICENSE" assets/fonts/geist/LICENSE.txt && S=$(stat -c%s assets/fonts/geist/Geist-Bold.woff2) && test "$S" -gt 5000 -a "$S" -le 51200 && python -c "from fontTools.ttLib import TTFont; f=TTFont('assets/fonts/geist/Geist-Bold.woff2'); c=f.getBestCmap(); assert f.flavor=='woff2', 'not woff2'; [c[o] for o in (0x2014,0x00E0,0x00E9,0x2019)]; print('glyphs OK')" && git diff --quiet assets/style.css && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `assets/fonts/geist/Geist-Bold.woff2` exists, is valid woff2 (`TTFont(...).flavor == 'woff2'`), and is <= 51,200 bytes (the unsubset Bold woff2 is 46.7 KB; a subset must not exceed it)
    - The subsetted font's cmap contains U+2014 (em-dash), U+00E0 (à), U+00E9 (é), and U+2019 (curly apostrophe) — asserted by the fontTools check in `<verify>`; missing U+2014 is a hard fail per the Deferred em-dash decision
    - `assets/fonts/geist/LICENSE.txt` exists and contains the string `SIL OPEN FONT LICENSE` (case-insensitive)
    - `git diff --quiet assets/style.css` exits 0 — the shared stylesheet is untouched (D-07)
    - No `<link>` to fonts.googleapis.com, fonts.gstatic.com, or any CDN was introduced anywhere
    - The font was fetched from `raw.githubusercontent.com/vercel/geist-font/main/...`, the official repo
  </acceptance_criteria>
  <done>A valid, generously-subsetted Geist-Bold woff2 under 50 KB sits next to its verbatim SIL OFL 1.1 license in `assets/fonts/geist/`, with `assets/style.css` untouched and no CDN reference anywhere.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream GitHub → local repo | Third-party binary assets (Geist TTF, OFL license text) cross into a repo that is then redistributed to others via GitHub Pages and USB |

This plan has no runtime, no server, no user input, and no network calls at page-load time. The only
boundary that exists is the provenance of the assets being vendored.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Geist-Bold.ttf download | mitigate | Fetch only from `raw.githubusercontent.com/vercel/geist-font/main/...` — the authoritative upstream repo verified by RESEARCH — never a CDN mirror, font-scraper site, or online subsetting service that would require uploading/downloading the font through an untrusted third party |
| T-07-02 | Repudiation | Font license provenance | mitigate | Ship `LICENSE.txt` verbatim from upstream alongside the woff2; OFL redistribution terms are unmet without it, and this site IS redistributed (Pages + USB). Asserted by an acceptance criterion, not left to reviewer memory |
| T-07-03 | Information disclosure | Committed video content | accept | The clip shows lab hardware the thesis publishes anyway; it is the subject matter, not sensitive. Audio is stripped per D-10 — which incidentally removes any incidental bench conversation, though byte size, not privacy, is the stated reason |
| T-07-SC | Tampering | npm/pip/cargo installs | n/a — no installs | This plan installs ZERO packages. `ffmpeg` (2023-06-19 build) and `pyftsubset` (fonttools 4.25.0) were both verified already present on the machine by direct execution during research. No package-manager trust chain is entered, so the slopcheck gate and its blocking human checkpoint do not apply. If either tool is unexpectedly absent at execution time, STOP and report — do not `pip install` to work around it |
</threat_model>

<verification>
- `ffprobe` confirms the committed video is 540x960, video-only, and 1–3 MB
- `fontTools` confirms the woff2 is valid and carries the em-dash and Italian accented glyphs
- `git status` confirms the 9.9 MB master is still untracked
- `git diff --quiet assets/style.css` confirms the shared stylesheet is untouched
- No HTML file is modified by this plan at all — `git status --porcelain '*.html'` should be empty
</verification>

<success_criteria>
All four artifacts exist and pass their size, format, and content assertions; the video master stays
untracked; `assets/style.css` is byte-identical; no CDN reference and no package install was introduced.
</success_criteria>

<output>
Create `.planning/phases/07-thesis-showcase-landing-page/07-01-SUMMARY.md` when done. Record the
final CRF used, the actual output byte sizes, the poster `-ss` timestamp that was accepted, and the
subsetted font size — plan 07-02 and 07-03 reference these files by path and need to know they landed.
</output>
