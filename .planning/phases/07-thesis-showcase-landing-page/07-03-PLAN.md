---
phase: 07-thesis-showcase-landing-page
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - index.html
autonomous: false
requirements: ["LANG-05"]

must_haves:
  truths:
    - "Per D-13, a stranger scrolling from the hero meets problem → device → proof → journey, in that order, before reaching any calculator"
    - "Per D-16, the device beat is carried by a NEW simplified six-module graphic — module names and liquid/data flow only, no payload chips, no interactivity — linking out to the System Architecture Explorer"
    - "Per D-08, the pump-head video sits below the fold as the 'it's real' beat: idealized mechanism first, hardware reality second"
    - "Per D-09 and D-10, the video played is the committed re-encoded proto-02 pump-head clip"
    - "Per D-11, the video autoplays muted and looping when scrolled into view and pauses on exit, driven by the IntersectionObserver already in index.html — and under prefers-reduced-motion it stays a static poster with a play button"
    - "Per D-15, the prototype journey is a narrative beat linking to prototypes/index.html, not a directory card"
    - "Per D-17, none of these four narrative sections carries a numbered eyebrow — they are a story and read like one"
    - "Per D-19, the narrative prose is a Claude DRAFT sourced from PROJECT.md and the repo's own records, explicitly flagged for Sirio's edit, and not lifted from the written thesis"
    - "Per D-12, the portrait video and the module graphic are laid out desktop-first with no horizontal scroll at 375px (LANG-05)"
    - "Per D-01, the six-module graphic is hand-built SVG with no runtime and no iframe of the architecture tool"
  artifacts:
    - path: "index.html"
      provides: "Four narrative sections (problem, device+graphic, proof+video, journey) between the hero and the resource sections"
      contains: "buildModuleSchema"
  key_links:
    - from: "index.html <video>"
      to: "prototypes/Prototype-1-Pump-Module/proto-02-5ul-4roller-v2/pump-head-web.mp4"
      via: "relative src, resolves under file:// and GitHub Pages"
      pattern: "pump-head-web\\.mp4"
    - from: "existing IntersectionObserver callback"
      to: "the <video> element"
      via: "branch on target identity — play/pause, never unobserve (D-11)"
      pattern: "\\.play\\(\\)"
    - from: "six-module graphic section"
      to: "tools/system-architecture-explorer/index.html"
      via: "See the full architecture link (D-16)"
      pattern: "system-architecture-explorer"
    - from: "journey section"
      to: "prototypes/index.html"
      via: "narrative beat link (D-15)"
      pattern: "prototypes/index\\.html"
---

<objective>
Insert the narrative between the hero and the resource sections: why point-of-care liquid dispensing
matters, what the device is (a new six-module graphic), see it running (the video), and the prototype
journey.

Purpose: D-13's whole argument is that a stranger gets motive, object, and evidence before ever
meeting a calculator. This plan is that argument made real — it is the difference between a tool
directory with a nice hero and an actual thesis showcase.

Output: A modified `index.html` with four eyebrow-less narrative sections carrying drafted EN/IT
prose, a hand-built six-module graphic, and the autoplaying portrait pump-head video.

Wave 3: depends on 07-02 having established the hero and on 07-01 having produced the video assets.
Sequenced rather than parallel because it edits the same `index.html`.
</objective>

<execution_context>
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/workflows/execute-plan.md
@D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-thesis-showcase-landing-page/07-CONTEXT.md
@.planning/phases/07-thesis-showcase-landing-page/07-RESEARCH.md
@.planning/phases/07-thesis-showcase-landing-page/07-01-SUMMARY.md
@.planning/phases/07-thesis-showcase-landing-page/07-02-SUMMARY.md
@CLAUDE.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft the four narrative sections and their EN/IT copy</name>
  <files>index.html</files>
  <read_first>
    - index.html — READ IN FULL. You are inserting between `.site-header` (as restructured by 07-02) and the `.resources-sections` div at line 296. Note the existing `.reveal` + `--rd` stagger idiom and the `.section-head`/`.section-index` eyebrow markup you must NOT copy into these sections
    - .planning/phases/07-thesis-showcase-landing-page/07-02-SUMMARY.md — the hero structure and class names you are extending
    - .planning/PROJECT.md — PRIMARY source for the problem and device sections: thesis framing, core value, constraints, hardware under development (D-19)
    - prototypes/REQUIREMENTS-CRITERIA.md — the canonical device requirements and weighted criteria; RESEARCH open question 1 names this as source material for the problem section's argument
    - prototypes/PROTOTYPES.md — the shallow prototype registry; scan this for the journey beat rather than opening the deep PROTOTYPE.md files
    - .planning/phases/07-thesis-showcase-landing-page/07-CONTEXT.md — D-13, D-15, D-17, D-18, D-19, and the Deferred section's em-dash rejection
  </read_first>
  <action>
    Insert four sections in D-13's order between the hero and `.resources-sections`: problem, device,
    proof, journey. The device and proof sections get their graphic and video in Task 2 — here,
    build the section shells and all the prose.

    Give these four sections NO numbered eyebrow and no `.section-index` span (D-17). The eyebrows
    survive only below, on the three resource sections, as a deliberate register shift: story ends,
    index begins. Reuse the `.reveal` + `--rd` stagger idiom already in the file so these sections
    animate in on scroll like everything else.

    Draft the prose from PROJECT.md, prototypes/REQUIREMENTS-CRITERIA.md, and PROTOTYPES.md (D-19).
    Do NOT lift prose from the written thesis — that text is written for examiners, and this page is
    for a stranger. The problem section makes the argument for why point-of-care liquid dispensing
    matters; the device section says what the thing IS in plain language; the journey section frames
    the prototypes as a story of iterations and links to prototypes/index.html (D-15 — it is story,
    not a directory entry; prototypes/index.html itself is untouched).

    This prose is a DRAFT. D-19 is explicit: Claude drafts, Sirio edits, and Sirio's voice wins on
    every conflict. Do not present this copy as final — Task 3 is a blocking review checkpoint, and
    the SUMMARY must list every drafted string so Sirio can find them. Write in Sirio's register as
    seen elsewhere on the site, and USE EM-DASHES where they fit: CONTEXT.md's Deferred section
    deliberately rejects taste-skill's em-dash ban because em-dashes are his actual voice. Do not
    strip them anywhere.

    Every user-visible string gets a `data-i18n` key with BOTH an `en` and an `it` entry added to the
    inline LANG dict (D-18). Write the Italian as real Italian in the same register, not a
    machine-literal calque of the English. A page that switches to Italian and hits English
    paragraphs looks broken — partial coverage is worse than none. Follow the existing key-naming
    convention in the dict (lowercase, hyphenated, prefixed by area).

    Keep taste-skill's layout-family variety in mind (RESEARCH marks "at least 4 different layout
    families" APPLICABLE): these four sections should not all be the same prose-block shape stacked
    four times.
  </action>
  <verify>
    <automated>cd "D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page" && python -c "
import io,re
s=io.open('index.html',encoding='utf-8').read()
m=re.search(r'const LANG = \{(.*?)\n    \};', s, re.S); assert m, 'LANG dict not found'
d=m.group(1); en_s,it_s = d.split('it: {')[0], d.split('it: {')[1]
en=set(re.findall(r\"'([a-z0-9-]+)':\s\", en_s)); it=set(re.findall(r\"'([a-z0-9-]+)':\s\", it_s))
assert en==it, 'EN/IT mismatch: only-EN=%s only-IT=%s'%(sorted(en-it),sorted(it-en))
keys=set(re.findall(r'data-i18n=\"([a-z0-9-]+)\"', s))
missing=keys-en; assert not missing, 'data-i18n keys with no dict entry: %s'%sorted(missing)
assert 'prototypes/index.html' in s, 'journey beat does not link to prototypes (D-15)'
assert not re.search(r'<iframe', s), 'iframe present — D-16 forbids iframing the architecture tool'
print('OK keys=%d'%len(en))
" && git diff --quiet assets/style.css && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Four new `<section>` elements exist between the hero and `.resources-sections`, in D-13's order: problem, device, proof, journey
    - NONE of those four sections contains a `.section-index` span (D-17 — eyebrows are below-the-fold only)
    - The journey section contains an `href` to `prototypes/index.html` (D-15)
    - Every `data-i18n` attribute value in the document has a matching key in `LANG.en` — asserted programmatically in `<verify>`
    - `LANG.en` and `LANG.it` key sets are exactly equal — asserted programmatically (D-18, LANG-01..05)
    - `index.html` contains no `<iframe>` element (D-16 forbids iframing the architecture tool)
    - Em-dash characters appear in the drafted copy and no existing em-dash was removed (Deferred decision)
    - The drafted prose contains no sentence copied verbatim from the written thesis (D-19)
    - `git diff --quiet assets/style.css` exits 0 (D-07)
  </acceptance_criteria>
  <done>Four eyebrow-less narrative sections carry drafted EN/IT prose sourced from the repo's own records, with full key parity and a journey link to prototypes/index.html.</done>
</task>

<task type="auto">
  <name>Task 2: Build the six-module graphic and wire the portrait video to the existing observer</name>
  <files>index.html</files>
  <read_first>
    - index.html — the inline `<script>`, specifically the IntersectionObserver block at lines 541-554 which you are extending (note it currently calls `io.unobserve(en.target)` on every reveal — that is one-shot semantics you must NOT apply to the video)
    - .planning/phases/07-thesis-showcase-landing-page/07-RESEARCH.md — "Six-Module Graphic Content (D-16)" (the six modules, their one-line purposes, the connectivity, and the verified flow colours); "Existing IntersectionObserver block to extend (D-11)"; "Concrete ffmpeg Command" for the PORTRAIT dimensions; "Pitfall 2" (designing the video slot for landscape when the clip is portrait)
    - tools/system-architecture-explorer/SPEC.md — the source of truth for the six modules and the liquid/data flow definitions. RESEARCH already extracted the content; use SPEC.md to confirm
    - decks/lab-meeting-2026-06/index.html around line 173 — the `<video>` attribute precedent (muted, loop, playsinline, poster, local relative src) for D-11
    - .planning/phases/07-thesis-showcase-landing-page/07-01-SUMMARY.md — the actual committed video and poster filenames and sizes
    - .planning/phases/07-thesis-showcase-landing-page/07-CONTEXT.md — D-08, D-09, D-11, D-16
  </read_first>
  <action>
    SIX-MODULE GRAPHIC (D-16): write a NEW `buildModuleSchema()` in the inline `<script>` using the
    same template-string SVG technique. Do NOT port `buildSchema()` from the architecture explorer
    and do NOT iframe the tool — D-16 forbids both; the tool's version is engineering-grade detail
    aimed at someone who just arrived, and duplicating it guarantees drift.

    Content, per RESEARCH's extraction from SPEC.md: six boxes — Pump (meters each liquid by roller
    displacement), Alignment (moves the sample racks under the nozzles), Nozzle (holds the nozzles
    and shakes droplets loose in short bursts), Storage (holds the liquids; senses level), UI (runs
    the GUI; protocol set on a laptop and loaded from the SD card), Software & Electronics (the brain
    and bus). Draw only the connectivity — SPEC.md itself notes spatial layout carries no meaning,
    only the drawn connections do: liquid flow Storage → Pump → Nozzle → Alignment as a SOLID BLUE
    #4a90d9 line; data flow Software & Electronics → every other module as a DASHED GREEN #3ec06b
    line. Reuse those exact colours — they are the established vocabulary one hop away in the tool.

    Carry over NO payload chips (no 6× NEMA17, no MPR121, no LM75), no pin-budget, no cost or BOM
    data, and no interactivity — no hover state, no click reveal (D-16). Render the boxes as rounded
    rects in the site's glass card language. Add one caption line below linking to
    `tools/system-architecture-explorer/index.html` for the real thing. Emit with `viewBox` +
    `width:100%` + `max-width`, never fixed pixel dimensions (LANG-05).

    VIDEO (D-08/D-09/D-11): add a `<video>` in the proof section with
    `src="prototypes/Prototype-1-Pump-Module/proto-02-5ul-4roller-v2/pump-head-web.mp4"`,
    `poster="...pump-head-poster.jpg"`, plus `muted`, `loop`, `playsinline`, and `preload="metadata"`,
    following the deck's precedent. Relative paths only — they must resolve from both file:// on USB
    and GitHub Pages.

    THE CLIP IS PORTRAIT: 540x960, 9:16. Style the container for a tall narrow frame — constrain by
    height (e.g. a `max-height` around 70vh with `width: auto`) and centre it with generous side
    space. Do NOT write `aspect-ratio: 16/9` or a wide `max-width` with no height constraint;
    RESEARCH's Pitfall 2 exists precisely because CONTEXT.md never had the real dimensions.

    AUTOPLAY (D-11): extend the EXISTING IntersectionObserver at line 541 — do NOT construct a second
    `new IntersectionObserver`. Branch inside the existing callback on target identity: if the target
    is the video, call `.play()` / `.pause()` on enter/exit and NEVER `unobserve` it; otherwise keep
    the current reveal behaviour of adding `in-view` and unobserving. Guard the play call with
    `.play().catch(() => {})` — some browsers reject the play promise when autoplay is blocked or a
    rapid scroll interrupts it, and an unhandled rejection would surface as a console error.

    Note explicitly: RESEARCH (section "Existing IntersectionObserver block to extend") RECOMMENDS a
    second observer instance, arguing the reveal observer's one-shot unobserve is incompatible with
    repeated play/pause. That recommendation is REJECTED — D-11 is a locked decision and says to use
    the observer already present. The incompatibility is not real: unobserve is called per-target
    inside the callback, so branching on target identity gives both semantics from one instance. Keep
    the existing `{ threshold: 0.12, rootMargin: '0px 0px -8% 0px' }` options and do not add a second
    observer to get a different threshold.

    Preserve the existing `else` fallback branch that adds `in-view` to all reveal elements when
    IntersectionObserver is unsupported.

    REDUCED MOTION (D-11): under `prefers-reduced-motion: reduce` the video must NOT autoplay — it
    stays a static poster with a visible play button so the user can start it deliberately. Detect
    with `window.matchMedia('(prefers-reduced-motion: reduce)')` in the script and skip the play call;
    add the `controls` attribute (or an explicit play affordance) in that case. This respects the
    opt-out without hiding the content. Extend the existing reduced-motion CSS block; do not add a
    second one.
  </action>
  <verify>
    <automated>cd "D:/03. DTU - Offline/03. MASTER THESIS/02. GitHub Thesis Tools Page" && python -c "
import io,re
s=io.open('index.html',encoding='utf-8').read()
assert 'buildModuleSchema' in s, 'buildModuleSchema() missing'
assert s.count('new IntersectionObserver')==1, 'expected exactly 1 IntersectionObserver (D-11), found %d'%s.count('new IntersectionObserver')
assert re.search(r'\.play\(\)\s*\.catch', s) or re.search(r'\.play\(\)\.catch', s), 'play() not guarded with .catch'
assert '.pause()' in s, 'no pause-on-exit (D-11)'
assert 'pump-head-web.mp4' in s and 'pump-head-poster.jpg' in s, 'video or poster not wired'
assert 'prefers-reduced-motion' in s and 'matchMedia' in s, 'reduced-motion autoplay opt-out missing (D-11)'
assert not re.search(r'aspect-ratio:\s*16\s*/\s*9', s), 'video styled 16:9 but the clip is PORTRAIT 540x960'
assert '#4a90d9' in s and '#3ec06b' in s, 'flow colours not reused (D-16)'
assert not re.search(r'NEMA17|MPR121|LM75', s), 'payload chips carried over — D-16 forbids'
assert 'buildSchema' not in s, 'buildSchema() ported from the architecture explorer — D-16 forbids'
assert not re.search(r'<iframe', s), 'iframe present — D-16 forbids'
for m in ['Pump','Alignment','Nozzle','Storage']: assert m in s, 'module %s missing'%m
print('OK')
" && grep -q 'tools/system-architecture-explorer/index.html' index.html && git diff --quiet assets/style.css && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `index.html` defines `buildModuleSchema` and does NOT contain the identifier `buildSchema` (D-16: new function, not a port)
    - The module graphic SVG names all six modules and contains both `#4a90d9` (liquid, solid) and `#3ec06b` (data, dashed) — the exact colours verified from SPEC.md
    - `index.html` contains no occurrence of `NEMA17`, `MPR121`, or `LM75` — no payload chips carried over (D-16)
    - No `<iframe>` anywhere; the graphic links out to `tools/system-architecture-explorer/index.html` via a normal href (D-16)
    - The module graphic has no hover or click handler bound to its module boxes (D-16: no interactivity)
    - `index.html` contains exactly ONE `new IntersectionObserver` occurrence — the existing instance was extended, not duplicated (D-11). This is asserted programmatically because RESEARCH actively recommends the opposite
    - The observer callback calls `.play()` guarded by `.catch(...)` and `.pause()`, and does NOT `unobserve` the video target
    - The `<video>` carries `muted`, `loop`, `playsinline`, a `poster` pointing at `pump-head-poster.jpg`, and a `src` pointing at `pump-head-web.mp4`, all relative paths (D-09, D-11)
    - `index.html` contains NO `aspect-ratio: 16/9` rule — the clip is portrait 540x960 (RESEARCH Pitfall 2)
    - The script contains a `matchMedia('(prefers-reduced-motion: reduce)')` check that suppresses autoplay and leaves poster + play affordance (D-11)
    - Exactly ONE `@media (prefers-reduced-motion: reduce)` CSS block exists
    - `git diff --quiet assets/style.css` exits 0 (D-07)
  </acceptance_criteria>
  <done>A new hand-built six-module graphic carries the device beat with correct flow colours and zero interactivity, and the portrait pump-head clip autoplays on scroll via the single existing observer, with a poster-plus-play-button path under reduced motion.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Sirio reviews and edits the narrative draft</name>
  <read_first>
    - index.html — the drafted narrative you are presenting
    - .planning/phases/07-thesis-showcase-landing-page/07-CONTEXT.md — D-13, D-16, D-19
  </read_first>
  <action>
    Pause. Present the narrative and wait for Sirio. D-19 makes his voice authoritative over the
    draft — expect rewrites and treat them as the normal outcome. Capture his exact wording verbatim
    into the SUMMARY so plan 07-04 can apply it to both EN and IT without re-interpreting it.
  </action>
  <what-built>
    Four narrative sections between the hero and the tools — problem, device (with a new six-module
    graphic), proof (with the autoplaying portrait pump-head video), and journey — with drafted
    EN/IT copy and no numbered eyebrows.
  </what-built>
  <how-to-verify>
    Run `.\serve.bat` and open `http://localhost:7331`. NEVER use VS Code Live Preview — it rewrites
    relative paths and strips inline `<script>` blocks, which would corrupt index.html on save.

    THE COPY IS A DRAFT AND YOUR VOICE WINS (D-19). The SUMMARY lists every drafted string and its
    LANG key. Rewrite anything that does not sound like you — that is the expected outcome here, not
    a failure. If you change an EN string, its IT twin needs the same change (D-18).

    1. Scroll from the hero. Does the order land — problem, then device, then proof, then journey?
       Does a stranger get why this matters before meeting a calculator (D-13)?
    2. The six-module graphic: are the six names and the liquid (blue solid) / data (green dashed)
       flows right? It should be deliberately simpler than the System Architecture Explorer (D-16).
    3. The video should start playing by itself as it scrolls into view, muted and looping, and pause
       when you scroll away. It is a PORTRAIT clip — does the tall frame sit well on a wide screen?
    4. Resize to 1280px then 375px. No horizontal scrollbar at either.
    5. Toggle EN/IT and scroll the whole page. No English text stranded in the Italian view.
    6. DevTools → Rendering → `prefers-reduced-motion: reduce`, reload: the video must NOT autoplay;
       it stays a poster with a play button you can click.
  </how-to-verify>
  <acceptance_criteria>
    - Sirio has replied with "approved" or with rewritten copy
    - Any rewritten string is recorded verbatim in the SUMMARY with its LANG key
    - The page was reviewed via serve.bat at http://localhost:7331, never VS Code Live Preview
  </acceptance_criteria>
  <resume-signal>Type "approved", or paste your rewritten copy / describe what to change</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inline LANG dict → DOM | New narrative keys flow through the same `applyLang()` `innerHTML` assignment |
| local media file → `<video>` decoder | A committed MP4 is decoded by the browser |

Still a static, server-less, input-less page. No auth, no sessions, no network calls, no user data.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Tampering | New `data-i18n` narrative keys via `innerHTML` | accept | Same disposition as T-07-04 in plan 07-02: the values are author-controlled constants in the same document, not user or network input. This plan adds keys to that trusted dict; it does not introduce any new value source. Do not add a runtime fetch of copy from any file — RESEARCH's Architectural Responsibility Map explicitly flags that no fetch tier should be introduced here |
| T-07-08 | Denial of service | `<video>` autoplay on scroll | mitigate | `preload="metadata"` avoids fetching ~2 MB before the section is reached; playback pauses on exit rather than looping off-screen forever; `prefers-reduced-motion` suppresses autoplay entirely. The clip is same-origin and committed — no third-party embed, no tracking pixel, no external player |
| T-07-09 | Tampering | Media file provenance | accept | The MP4 and JPEG were produced locally by plan 07-01 from a clip Sirio filmed, re-encoded with a local ffmpeg. No third-party binary enters the page |
| T-07-SC | Tampering | npm/pip/cargo installs | n/a — no installs | Zero packages, zero `<script src>`, zero CDN references added. The no-gsap/no-CDN grep from plan 07-02 continues to hold over this plan's edits |
</threat_model>

<verification>
- Programmatic assert of exactly one `new IntersectionObserver` (D-11 — RESEARCH recommends otherwise; the decision wins)
- Programmatic assert that `buildSchema` is absent and `buildModuleSchema` is present (D-16)
- Programmatic assert of no `NEMA17`/`MPR121`/`LM75` and no `<iframe>` (D-16)
- Programmatic assert of no `aspect-ratio: 16/9` (the clip is portrait)
- Programmatic EN/IT key-set equality and every `data-i18n` key resolving in the dict (D-18)
- Manual pass at `http://localhost:7331` via `serve.bat` — never VS Code Live Preview
- Reduced-motion check: video does not autoplay, poster + play button remain
</verification>

<success_criteria>
A stranger scrolling from the hero meets problem → device → proof → journey before any calculator;
the six-module graphic is new, simple, non-interactive, and links out; the portrait video autoplays
on scroll from the single existing observer and respects reduced motion; EN/IT parity holds; no
horizontal scroll at 1280px or 375px; assets/style.css untouched; Sirio has reviewed and edited the
draft copy.
</success_criteria>

<output>
Create `.planning/phases/07-thesis-showcase-landing-page/07-03-SUMMARY.md` when done. It MUST list
every drafted narrative string with its LANG key so Sirio can find and rewrite them (D-19), and
record the final section class names — plan 07-04 restructures the sections below these and needs
the resulting DOM shape.
</output>
